# 006 — Typed API-клиент как отдельный iife-бандл, не полная миграция фронтенда на Vite

**Статус**: принято, реализовано (20.0.0), суперсед частично в 20.12.0
(старт настоящего SPA-переезда — см. README §22, эпоха 20.14+).

## Контекст

Фронтенд (`backend/frontend/js/*.js`) — 20 классических non-module
`<script>`-файлов, делящих одну глобальную область видимости
(порядок подключения важен — есть отдельный smoke-тест именно на это).
Нужен был способ добавить типизированный слой доступа к API, не
переписывая всё разом.

## Решение

Один TypeScript ES-модуль (`frontend/src/api-client.ts`), собираемый
Vite'ом в формате **iife** (не `es`/`umd`) в единый бандл
`frontend/dist/api-client.bundle.js`, подключаемый как обычный
`<script src=...>` перед остальными легаси-файлами. IIFE сохраняет ту же
семантику загрузки, что и классические скрипты — не требует
`type="module"`, не ломает `scripts/smoke-frontend.mjs` (тот матчит
`<script src="...">` жёстким regexp'ом, не учитывающим `type="module"`).

## Альтернативы

| Вариант | Вердикт | Почему |
|---|:---:|---|
| Полная миграция всего фронтенда на Vite/ES-модули разом (20.0.0) | ❌ отклонено на старте | `01-core.js` держит расшаренное мутируемое состояние (`me`, `adminViewOrgId`, `METRICS`), на которое прямо ссылаются все остальные 19 файлов — полная модуляризация была бы большим, рискованным заходом без пилота |
| Узкий iife-слой (сеть) сначала, оркестрация/состояние/роутинг отдельным заходом | ✅ принято | Пилотируемый риск, не разовый большой переезд |

## Последствия

- Эпоха 20 (Frontend Foundation, 20.0.0→20.7.0) полностью перевела сетевой
  слой всех 20 файлов на typed-клиент, не тронув DOM/оркестрацию/роутинг —
  честная, узкая граница результата, задокументированная как таковая.
- 20.11.0 констатировал: `pages/`/`features/`/`entities/`/`app/router.ts`/
  `app/state.ts` — это не результат простого перемещения файлов, а
  настоящий SPA-переезд сравнимого размера с самой эпохой 20 — начат как
  отдельный, более длинный проект (см. README §22), не смешан с
  backend/docs-реструктуризацией той же версии.
- 20.12.0 — первый реальный срез: `app/router.ts` (typed page registry,
  сознательно НЕ URL/hash-based — см. сам файл), `app/state.ts` (типизированный
  доступ к сессии поверх легаси-глобала `me`, без дублирования источника
  правды), `features/send-network-digest/` (первый `addEventListener`
  вместо `onclick=` — не требует `unsafe-inline` для своей кнопки),
  `pages/reports/` заменяет `frontend/js/19-reports.js` файл-в-файл (тот же
  глобальный `window.loadReportsPage`, легаси `switchPage()`/`loadPage()`
  не тронуты — 19 остальных страниц продолжают работать как раньше). Каждый
  мигрированный файл получает свой отдельный iife-бандл (Rollup не
  поддерживает несколько точек входа в одной iife/umd-сборке) — второй
  `vite build` (`vite.pages.config.ts`, `emptyOutDir: false`) добавлен в
  `build:frontend` рядом с уже существующим для `api-client.ts`.

## Связанные документы

- [ARCHITECTURE.md](../ARCHITECTURE.md) — текущее состояние `frontend/src/`
  vs `frontend/js/`.
