## Скопируйте в keystore.properties (рядом, в этой же папке) и заполните
## реальными значениями — сам keystore.properties в .gitignore, в репозиторий
## не попадает. Подробности и команда генерации ключа — docs/ANDROID-RELEASE.md.
storeFile=t2sales-release.jks
storePassword=CHANGE_ME
keyAlias=t2sales
keyPassword=CHANGE_ME
