# История версий

> Вынесено из README §21 при сжатии корня (20.16.0) — README держит только
> последние несколько записей и ссылку сюда. Полная построчная история
> проекта от первых Google Sheets-отчётов до сегодня, версия за версией —
> здесь, ничего не сокращено при переносе.

| Версия | Суть |
|--------|------|
| Sheets + Apps Script | первые отчёты |
| v3–v5 | Mini App, планы, BFQ, bulk |
| v8 | access gate, supervisor |
| v12 | ЛК, касса, multi-metric |
| **v13** | смены, NLP, offline, live, insights |
| **13.3** | месяц+архив, промо, gate UI |
| **13.4** | tutorial employee + manager |
| **14.0–14.1** | Кабинет супервайзера, PNG-отчёты, кастомные метрики |
| **14.2** | initData HMAC-проверка, закрыты открытые роуты, убрана SQL-инъекция в offline sync, XSS-фиксы во фронтенде, отчёты/дашборд считают метрики динамически (кастомные больше не пропадают), убран мёртвый код (routes-v4/v6/v7/promos → routes-employees.ts) |
| **14.3** | Рефакторинг: index.ts (963 строки) разбит на routes-core/sales/schedules/stats/cash/promos/reports.ts; index.html (6091 строка) разбит на styles.css + 13 файлов в frontend/js/. Заодно найден и исправлен баг: `replyTicket` была объявлена дважды (кнопка «Ответить» в разделе «Поддержка» не работала) |
| **14.3.1–14.3.2** | Хотфиксы после разбивки 14.3: 26 GET-запросов во фронтенде не слали `X-Telegram-Init-Data` (получали 401 на защищённых роутах); `todayMoscow()` вызывалась в `01-core.js` до того, как определялась в `02-nav-utils.js` — падал весь скрипт, ломая план/график/кабинет/команду разом |
| **14.4.0** | Техдолг перед AI-эпохой: убрана мёртвая ветка `shift_open`/`shift_close` в `/sync/batch` (офлайн-очередь умеет только `sale`, смены туда никогда не попадали); `middleware-auth-v8.ts` (был просто ре-экспортом) убран, `routes-v8.ts` импортирует `middleware-auth.ts` напрямую; удалены осиротевшие `bot-messages.ts` и `index-snippet.txt`; добавлен `npm run smoke:frontend` — vm-тест порядка подключения `frontend/js/*.js`, ловит класс бага из 14.3.1 |
| **14.5.0** | Command Center v1: виджет «Сеть за минуту» на главной для manager/supervisor/admin (health-score + топ-3 просадки, через `/supervisor/health` — эндпоинт для виджетов существовал с 14.x, но не был подключён ни к одному экрану); в кабинете супервайзера впервые появился список алертов (`/alerts`) с подтверждением (`/alerts/:id/ack`) — раньше эти роуты не имели UI вообще, алерты уходили только админу в личку и накапливались без возможности снять; удалены осиротевшие `supervisor-page.html`/`supervisor-ui.js`/`supervisor-ui.css` (не подключены к index.html, реальный кабинет супервайзера — `08-access-supervisor.js`) |
| **14.6.0** | Смена как сессия: закрытие смены вместо тоста «идеальная / нет» показывает разбор — score, факт/план по SIM/MNP/ПА/Комбо, явную причину «до идеальной смены не хватило» (`ideal_missing` от `/shifts/close`), плюс XP/уровень/стрик за эту смену (`evaluateShiftClose` в gamification.ts считал это и раньше, просто не возвращал наружу — экран его игнорировал) |
| **14.6.1** | Хотфикс: вкладка «Заявки на доступ» перекидывала на главную — в `index.html` отсутствовал контейнер `page-access` (потерялся при разбивке 14.3.0), `switchPage()` не находил элемент и молча падал на home. Кнопка живёт на вкладке «Команда» → «Управление», не на главной |
| **14.7.0** | Отчёт-история: итог дня в чат теперь уходит альбомом из 3 картинок вместо одной — план дня → факт → фокус на завтра (цели + кто в графике + подсказка по пиковому часу из `store_hour_profile`). Тот же resvg-рендерер и та же цепочка фолбэков (PNG → SVG-документ → текст), просто на 3 кадра; при сбое media group откатывается на старый одиночный финал. Превью-страница «Отчёт-картинка» показывает то же самое, что реально уйдёт в чат. Микро-отчёты не менялись |
| **14.8.0** | What-if v2: сценарий из нескольких переносов разом вместо одного (бэкенд `simulateScheduleMoves` уже принимал массив — не хватало только UI: список переносов с добавлением/удалением). Сравнение сценариев A/B: посчитал один набор переносов → «Сохранить как A» → очистил, собрал другой → авто-сравнение при пересчёте. Метрика сравнения — не сумма Δ SIM по сети (перенос внутри сети всегда даёт в сумме ноль, сравнивать по этому бессмысленно), а худшая просевшая точка: какой сценарий меньше вредит своему самому слабому месту |
| **14.9.0** | Автоанонс обновлений: важные версии (minor-эпики, не хотфиксы) сами приходят в рабочий чат картинкой с кратким описанием при старте сервера — тем же resvg-пайплайном, что и отчёты. Список версий для анонса — `src/changelog.ts`; какая версия уже анонсирована — `app_settings.last_announced_version`, отправка отмечается только при успехе. Заодно: `.btn-ghost` использовался в 5 местах (в т.ч. до этой сессии) без единого объявления в CSS — рендерился браузерным дефолтом; добавлен стиль в тон `.btn-main`. Плановый эпик «Порог перед ИИ» сдвинут на 14.10.0 |
| **14.10.0** | Порог перед ИИ: `db/index.ts` и `bot/index.ts` читали `process.env` на верхнем уровне модуля — из-за хостинга ES-импортов это выполнялось раньше `dotenv.config()` в `index.ts`, так что `npm run dev` тихо не видел `.env` (в Railway не заметно — там переменные уже в окружении до старта node). Добавлен `src/env.ts`, первый импорт index.ts, гарантирующий порядок. Убран дублирующий SVG-рендерер в `routes-reports.ts` (`buildDayReportSvgInline`) — параллельная реализация того же пути к тем же данным, не настоящий фолбэк. Статус мультитенантности явно задокументирован в `tenant.ts`: сейчас только branding, изоляция данных — сознательно отложенный эпик мультитенанта. Полная регрессия по 17 разделам приложения на реальных данных — без единой ошибки |
| **15.0.0** | AI Copilot v1, первая ИИ-эпоха. Сознательно бесплатно и без чата/tool-calling — оба сценария одношаговые, бэкенд сам собирает контекст кодом, модель вызывается через Groq (бесплатный тариф, `llama-3.3-70b-versatile`, без карты): (1) итог смены сотруднику — короткий комментарий сразу после `/shifts/close`, на основе факта/плана/XP; (2) комментарий к просадке точки — встроен в существующий cron итоговых отчётов (`cron/reports.ts`): если факт дня < 85% от плана, модель даёт гипотезу вместо голой цифры; если план закрыт — берётся случайная заготовленная фраза-похвала без вызова модели. Комментарий уходит и в подпись к отчёту в чат, и в БД — оттуда его подтягивает Command Center на главной (`/supervisor/health`, поле `drops[].ai_comment`). Новая таблица `ai_audit` — лог всех промптов/ответов модели. Без ключа `GROQ_API_KEY` обе функции тихо no-op'ают (summary → `null`, комментарий → заготовленный фолбэк-текст), остальное приложение не затронуто |
| **15.0.1** | Хотфикс: касса (`GET /cash/table`, `PUT /cash`) была ошибочно закрыта на `requireManager` — 403 для обычного employee, хотя фронтенд (`09-cash-metrics.js`) всегда показывал форму внесения кассы всем сотрудникам точки, не только управляющим. Заменено на `requireActive` (любой активный сотрудник); неиспользуемый фронтендом `GET /cash` (список) оставлен manager-only |
| **15.0.2** | Итоговый отчёт-альбом (план/факт/фокус на завтра) больше не подписывает каждую из 3 картинок по отдельности — заголовок кадра и так есть на самой картинке. Вместо трёх caption — одно сообщение под альбомом с итогом дня и AI-комментарием (`cron/reports.ts`) |
| **15.0.3** | Manager/admin теперь могут отменить ошибочно внесённую метрику за день (`PUT /sales/:id/zero`, кнопка «Удалить» в карточке сотрудника → «Исправить ошибочный ввод») — `sales` аддитивная, поэтому это обнуление конкретной колонки с записью в `sales_audit`, а не удаление всей записи за день. AI-гипотеза при просадке точки теперь смотрит на все ~18 метрик `store_plans`, а не только SIM/MNP/ПА/Комбо — раньше просадка по, например, страховкам или аксессуарам была для модели невидима |
| **15.0.4** | Удаление кастомных метрик плана получило UI — `DELETE /metrics/:id` существовал на бэкенде (soft-delete, базовые метрики защищены), но был недоступен из приложения. «Управление» → «Метрики плана» теперь показывает список своих метрик с кнопкой «Удалить» перед формой создания новой |
| **15.1.0** | Обучение v3 — «игровой уровень» с персонажем Арбузычем (🍉). Два визуальных режима: полноэкранные cutscene-главы (`#tutorialScreen`) для вступления/переходов/квиза/финала и доработанный coach-оверлей (`#tutorialOverlay`, сильнее затемнение, аватар-бабл) для «нажми сюда» на реальном UI. Оба трека (employee 22 шага, manager 17 шагов) переписаны и разбиты на главы с мини-праздником (`confettiBurst()`) между ними. Добавление продажи в главе «Продажи» теперь тренируют на настоящей форме (`07-add-sale.js`) в dry-run режиме — `window.__tutorialDryRun` перехватывает `submitSale()` до реального `POST /sales`, ничего не пишется в базу и в чат. Новый `POST /me/tutorial-complete` идемпотентно начисляет XP и бейдж (`tutorial_done`/`tutorial_mgr_done`) через уже существующие `addXp`/`grantBadge` — раньше прохождение обучения вообще не было известно бэкенду, только `localStorage` |
| **15.7.0** | «Кого куда поставить» (`services/forecast.ts` → `getStaffingHints()`). Абсолютной меры «нужно N человек» нет — прогноз в штуках метрик, не в человеко-часах, поэтому эвристика относительная: нагрузка на человека в графике (прогноз / headcount по `schedules`) сравнивается со средней по сети на ту же дату; кто выше среднего на 30%+ или вообще без headcount при ненулевом прогнозе — попадает в подсказку. Новый `GET /staffing-hints` (manager/admin), блок на странице «Прогноз» на 7 дней вперёд, кнопка «Предложить перенос» переиспользует `proposeMoveForStore()` из 15.4.0 (расширен вторым параметром — датой). Честно помечено как эвристика, не точный расчёт |
| **15.6.0** | Объявления сети (`/announcements`) не работали вообще — бэкенд отдаёт голый массив (как `/sales`, `/employees`), а фронтенд ждал `{items:[...]}`; `data.items` на массиве всегда `undefined`, экран показывал «Нет объявлений» независимо от реальных данных (в базе на момент фикса лежало 5 штук). Заодно добавлена настоящая причина заводить read-tracking — новый `GET /announcements/:id/reads` (manager/admin) и кнопка «Кто прочитал» показывают по именам, кто уже видел обязательное объявление, а кто ещё нет; раньше read-статус был виден только самому сотруднику про себя |
| **15.5.0** | Единообразие метрик. Метки одной и той же метрики раньше жили в шести независимых копиях (`01-core.js`, `03-home.js`, `04-schedule.js`, `05-my-plan.js` — три штуки в одном файле, `06-team-bfq.js`, `11-v13.js`) и на бэкенде тоже в двух (`routes-metrics.ts` и `services/metrics-catalog.ts`) — расходились по деталям, например «Аксессуары» на одном экране и «Аксы» на другом для одного и того же показателя. Бэкенд: `GET /metrics` теперь просто вызывает `getMetricDefs()` вместо своего отдельного захардкоженного фолбэка. Фронтенд: `METRICS` в `01-core.js` — единственный источник, новые `metricLabel(id)`/`metricShort(id)` читают из него; все экраны переведены на них. Заодно «Мои продажи сегодня» перебирает `METRICS` вместо фиксированного списка из 15 id — кастомные метрики тоже попадают в сводку |
| **15.4.0** | «Действие из просадки»: под каждой карточкой просадки в Command Center (`03-home.js`) и кабинете супервайзера (`08-access-supervisor.js`) — кнопка «Предложить перенос». Ведёт на страницу «Прогноз» и сразу выставляет проблемную точку в поле «На точку» What-if-сценария (`proposeMoveForStore()` в `13-v14.js`, дожидается `fillStoreSelects()` перед подстановкой значения, иначе селект перезатирается) — супервайзеру остаётся выбрать только сотрудника и «с точки». Заодно кабинет супервайзера стал показывать `ai_comment` под просадкой, как и главная — раньше это было только в Command Center |
| **15.3.0** | Прогноз (`services/forecast.ts`) переписан с нуля. Старая модель брала среднее строго по тому же дню недели за последние 8 недель — на датасете короче 8 недель (как сейчас) подходящих точек нет вообще, отсюда прогноз всегда 0 для любой точки. Новая модель: простое экспоненциальное сглаживание (`α=0.3`) для уровня тренда × сезонная поправка на день недели с усадкой к нейтральной 1.0 (`k=3`), пока по дню недели мало наблюдений — даёт осмысленное число с первого дня и уточняется по мере накопления истории. Ответ `/forecast/:storeId` дополнен `history_days`; фронтенд показывает предупреждение, если истории меньше 14 дней. Проверено на реальных данных: Космонавтов (4 дня истории) — 10-13 SIM/день вместо нуля; Калинина 11 (0 продаж в базе вообще) — честный 0 с пояснением вместо молчаливо «сломанного» экрана |
| **15.2.2** | Хотфикс: время открытия смены в «Мой» показывало UTC вместо МСК (открыл в 9:55 — писало 6:55) — `sess.opened_at.slice(11,16)` резал сырую UTC ISO-строку без конвертации часового пояса. Новый `timeMoscow()` в `01-core.js` форматирует явно через `Intl.DateTimeFormat` с `timeZone: 'Europe/Moscow'` |
| **15.2.1** | Хотфикс: `store_plans` материализовался только вручную кнопкой «Записать дневные планы в БД» — если никто не нажал, у точки не было плана на день, и отчёт-картинка/«Фокус на завтра» рендерились пустыми (факт 0, план «—»), как случилось с Калининой 11 на 04-05.08. Теперь `cron/reports.ts` в 06:00 МСК сам материализует план на сегодня и на завтра через уже существующий `materializeStoreDailyPlans()` (`services/plans.ts`). Данные за 05-06.08 пересчитаны вручную через продовый `POST /plans/stores/daily/materialize` |
| **15.2.0** | UI-полировка heatmap/прогноз/касса/команда под общий стиль приложения (Command Center, `.cc-*`/`.mt-*` токены вместо инлайн-стилей): heatmap — классы `.hm-cell` вместо сырых inline `rgba()`; прогноз — исправлена вёрстка (`.mt-grid` был 3-колоночным на 4 метрики, теперь `.mt-grid-4`); касса — настоящая строка заголовков колонок; команда — цветной кружок-инициал вместо статичной 👤, тон = есть ли у сотрудника продажи сегодня. Заодно найден и исправлен `var(--muted)` — использовался в 8 местах (`.sv-*`), нигде не был определён. Добавлен `@media (prefers-reduced-motion: reduce)` — раньше не учитывался нигде, при этом две бесконечные анимации (Арбузыч, spotlight-пульс) крутились всегда. Пустые состояния heatmap/промокодов/live-сети больше не показывают технические заметки вроде «Нужен sales_events (sql/v8-0-roadmap.sql)» |
| **15.8.0** | Эпик мультитенанта, фаза A — секторы и сети точек. *(в записях этого сеанса раньше называлось «эпик 17.0» как внутреннее кодовое имя — не путать с реальной версией 17.0.0+, это другой, более поздний эпик про CI/тесты)* `organizations`/`org_id` существовали в схеме с 14.x, но были write-orphaned (ни один INSERT/UPDATE их не трогал) и влияли только на брендинг — реальные данные читались по всей сети без фильтра. Новая таблица `sectors`, `organizations` получили `sector_id`/`chat_id` (= «сеть точек» по смыслу, переиспользована как есть), `supervisor_sectors` заменяет ручное назначение точек супервайзеру (`supervisor_stores`) — теперь назначение на сектор целиком, супервайзер сразу видит все точки всех сетей внутри (`middleware-auth.ts` → `getUserStoreIds`, `services/supervisor-analytics.ts` → `resolveSupervisorStores`). Уведомления (продажа, микро/итог-отчёты, алерты 14:00/16:00, анонс релиза) теперь роутятся по чату **точки** — `getStoreChatId()`/`getOrgChatId()` в `tenant.ts`, `notifyChat*` уже принимали `chatId`, просто никто его не передавал. Алерты 14:00/16:00 раньше слали одно агрегированное сообщение по всей сети в общий чат — теперь группируются по сети и шлют по сообщению на сеть в её чат. Убран хардкоженный TS-массив `SCHEDULES` в `cron/reports.ts` (3 точки, расписание отчётов руками в коде) — читается из `stores.micro_report_times`/`skip_sunday_micro_times`/`close_time_weekday`/`close_time_sunday`, которые уже были в схеме, но нигде не читались; новая точка получает расписание сразу, без правки кода. `POST /employees`/`POST /stores` и bot-approval пишут `org_id` (раньше не писали никогда); закрыта дыра в `listStoresForOrg()` — молча фолбэчилась на все точки при пустом фильтре. Обратная совместимость: единственная существующая сеть перенесена в `sector='default'`/`org='default'` с тем же `chat_id`, что был в `CHAT_ID` — поведение не изменилось. Изоляция данных на уровне экранов (сотрудники/график/промокоды/касса видны только своей сети) — фаза B, следующим заходом |
| **15.9.0** | Полная лестница ролей вместо плоского `employee/manager/supervisor/admin`: `Стажёр(trainee) → Продавец(employee) → Старший продавец(senior) → Руководитель(manager) → Супервайзер(supervisor) → Admin`. `senior` — новая роль, операционно как руководитель (сотрудники/точки/график/касса/продажи/экспорты — везде, где раньше `requireManager`), но намеренно без доступа к Command Center и кабинету супервайзера — решили разделить «операционные права» и «видимость аналитики» на два независимых флага (`isManager()`/`canManage()` расширены до senior, новый `canViewAnalytics()` на фронте и уже существующий `canViewSupervisor()` на бэке — оба явно НЕ включают senior). Назначение роли (`PATCH /employees/:id/role`) раньше не имело вообще никаких ограничений — любой manager/admin мог поставить кому угодно любую роль, включая admin; теперь `canAssignRole()` в `middleware-auth.ts` разрешает назначать только роль строго ниже своей по уровню (admin — без ограничений, как и было). Заодно найдены и исправлены два бага в той же области: кнопка смены роли в карточке сотрудника (`06-team-bfq.js`) стучалась в `PATCH /employees/:id`, а не `/employees/:id/role` — хендлер роль не трогает (по собственному комментарию в коде), кнопка молча ничего не делала; дублирующий эндпоинт `POST /employees/:id/role` в `routes-v3.ts` имел свой allowlist ролей без `supervisor`, расходящийся с версией в `routes-v8.ts` — выровнен через тот же `canAssignRole()` |
| **15.10.0** | Первая проверка эпика 17.0 на реальных вторых данных: заведена настоящая тестовая сеть (`test_network`, свои 2 точки, свои 2 сотрудника, свой `chat_id`) — уведомления/отчёты по ней подтверждённо уходят в свой чат, а не в общий. Заодно закрыт пробел, который эта проверка сразу вскрыла: `GET/POST /employees` были не scoped по сети — «Команда» показывала сотрудников всех сетей вперемешку. Теперь по умолчанию видна только своя сеть (`COALESCE(org_id,'default') = свой org_id`); у admin в «Команде» появился переключатель сети (`GET /orgs`, admin-only) — по умолчанию видит свою рабочую сеть, но может явно заглянуть/завести сотрудника в любой другой (`?org_id=`/`body.org_id`, тоже только для admin). `GET /me` заодно стал отдавать `org_id` — фронту он был нужен для дефолта переключателя |
| **15.11.0** | Эпик мультитенанта, фаза B завершена — график/касса/промокоды тоже scoped по сети, тем же паттерном, что и «Команда» в 15.10.0 (общий хелпер `resolveViewOrgId()` в `middleware-auth.ts`, переиспользован во всех четырёх разделах). `GET /schedules`, `/schedules/month`, `GET /cash/table`, `/cash`, `GET /promos` фильтруются по сети точки (или по `org_id` самого промокода); `POST /schedules` дополнительно проверяет, что точка принадлежит сети менеджера (403, если нет) — но не ограничивает, какого сотрудника туда поставить, потому что подмена в чужой сети остаётся легитимной (эпик мультитенанта, фаза A). `rtk_promocodes` — миграция `org_id` была добавлена ещё в 15.8.0, но роуты её не использовали до сих пор; `GET/POST /promos/:id`, `/:id/use` тоже проверяют `org_id`, чтобы код чужой сети нельзя было забрать по угаданному id. Переключатель сети у admin (появился в 15.10.0 для «Команды») теперь работает и на «Расписании», «Кассе», «Промокодах» |
| **15.12.0** | Проверка на реальной второй сети (`test_network`) вскрыла, что фаза B в 15.11.0 закрыла не все дыры. Дополнительно scoped: `GET /stats/daily` («Сеть сегодня»), `GET /dashboard` («Топ за 7 дней», по точке продажи — не по «домашней» сети сотрудника, как и весь эпик мультитенанта), `GET /plans/employees/month` («Планы и факт за месяц»), `GET /plans/stores/daily` («План дня по точкам»). Самое широкое: `resolveSupervisorStores()` для manager/admin отдавал `scope = null` («вся БД без фильтра») — Command Center на главной и весь кабинет супервайзера показывали буквально все сети сразу; теперь для manager/admin scope — список точек своей сети (или явно выбранной admin'ом). Заодно исправлена вёрстка `computeStoreDailyPlans()`: хардкоженная `STORE_SHARES` на 3 точки заменена на чтение `stores.plan_share` — колонка была в схеме и даже отредактирована в БД вручную, но код её не читал (тот же паттерн, что `micro_report_times` в 15.8.0). Найдена и исправлена регрессия, которую сама эта фаза едва не внесла: `GET /schedules`/`/schedules/month`, отфильтрованные по сети точки, переставали показывать сотруднику его СОБСТВЕННУЮ смену, если сегодня он подменяет в чужой сети — добавлено исключение «своя запись видна всегда» |
| **15.13.0** | План точки — из «доли от суммы планов сотрудников» в независимый ручной ввод. Новая таблица `store_month_plans` (зеркалит `employee_month_plans`, только на точку) + `GET/PUT /plans/stores/:id/month`, гейт — точка должна принадлежать своей сети (как и `POST /schedules`). `computeStoreDailyPlans()` переписан: вместо суммирования остатков планов всех сотрудников сети и деления по `plan_share` — просто `(план точки на месяц − факт точки с начала месяца) / оставшиеся дни`, ровно та же формула, что уже была для сотрудника (`getEmployeeDailyPlan`), только на уровне точки. Старый шаблон `store_plans` с `plan_date IS NULL` (ручная цифра-фолбэк на день) убран из приоритета в `04-schedule.js` — план точки его полностью заменяет. Планы сотрудников как отдельная сущность (личная статистика/BFQ) не тронуты. У точек, заведённых до этой версии, план на месяц пуст — «План дня по точкам» покажет 0, пока управляющий не введёт план вручную (тап по точке в «Управление») |
| **15.13.1** | Хотфикс: команда `/chatid` боту — прямо в группе/теме отвечает `chat.id` и `message_thread_id` текущей темы. Нужно было для подключения РТТ Гуреева (их группа с темами — отдельная тема «продажи», отдельная «отчёты»), пригодится для любой следующей сети |
| **15.14.0** | Подключена вторая реальная сеть — **РТТ Гуреева** (4 точки: Хорошевская 27, Кожевническая 4, Новослободская 10, Комсомольская пл. 3 — круглосуточная, «итог дня» на 21:00; 8 сотрудников). Основная сеть переименована в **РТТ Бижонов**. Их группа в Telegram — форум с темами (отдельная тема «продажи», отдельная «отчёты»), которого раньше в приложении не было вообще — на сеть был только один `chat_id`. Добавлены `sales_thread_id`/`reports_thread_id` на `organizations`, `notifyChat*` в `bot/index.ts` научились передавать `message_thread_id`, новые `getOrgNotifyTarget()`/`getStoreNotifyTarget()` в `tenant.ts` резолвят чат+тему по назначению (продажа → тема продаж, отчёты/алерты → тема отчётов). Заодно найден и исправлен баг: `checkStoreLagAlert()` (алерт отставания 16:00) считал дневные планы только для сети `default` — вторая сеть вообще не получала бы этот алерт |
| **15.14.1** | Структурная чистка без изменения поведения (без анонса — не эпик). Три разросшихся файла-свалки разбиты по темам: `06-team-bfq.js` (802 строки, команда+BFQ+планы+поддержка+модалки) → `06-team-bfq.js`/`06b-plans-bfq.js`/`06c-support-tickets.js`; `routes-v13.ts` (717 строк) → `routes-shifts.ts`/`routes-insights.ts`/`routes-live-alerts.ts`/`routes-comms.ts`/`routes-forecast.ts`; `routes-v3.ts` (557 строк) → `routes-me.ts`/`routes-bfq.ts`/`routes-export.ts`, а `/schedules/bulk`+`DELETE /schedules` переехали в `routes-schedules.ts` — мутации графика были раскиданы по двум файлам. Заодно: дедуп проверки «точка принадлежит вашей сети» в `assertStoreInOrg()` (была продублирована в `routes-schedules.ts` и `routes-plans-v5.ts`); закрыт пробел — `/schedules/bulk` не проверял принадлежность точки сети вообще, хотя одиночный `POST /schedules` уже проверял; удалён мёртвый код (`cellTone()` во фронтенде, нигде не вызывался) |
| **15.15.0** | Изоляция по сети — последние 6 непроверенных мест. `getLiveNetworkMap()` (живая карта) была самой грубой дырой: показывала имена сотрудников, продажи и кассу вообще всех сетей сразу, без единого фильтра — теперь `orgId` параметр. Аналогично scoped: `GET /forecast/:storeId`, `GET /heatmap/precise/:storeId` (реальный heatmap, который дёргает фронтенд — не путать с параллельным `GET /heatmap/:storeId` в `routes-forecast.ts`, тот не используется) через `assertStoreInOrg()`; `GET /staffing-hints`, `GET /alerts`, `GET /export/bi/daily` через `resolveViewOrgId()`. `newbieCohorts(orgId)` — параметр существовал в сигнатуре и раньше, но нигде не читался в самом SQL-запросе (тот же паттерн, что `plan_share`/`micro_report_times` ранее). Заодно найден и починен независимый баг: `newbieCohorts()` падал с 500 на `invalid input syntax for type date`, когда дата бралась из `created_at` (JS `Date` из pg-драйвера) — `String(date).slice(0,10)` резал `"Tue Jul 28"` вместо ISO |
| **15.15.1** | Хотфикс: заголовок карточки-анонса версии в чат обрезался по краю на длинных названиях (SVG не переносит текст сам) — буллеты уже переносились строками, а заголовок оставался одной строкой на 26px. `wrapText()` в `report-image.ts` сделан переиспользуемым (параметр максимума символов в строке вместо константы), заголовок теперь тоже переносится, версия и буллеты сдвигаются вниз на высоту второй строки заголовка |
| **15.16.0** | Изоляция по сети — объявления, каналы, заявки на доступ (`routes-comms.ts`). `announcements.org_id`/`channels.org_id` существовали в схеме и раньше, но ни один запрос их не читал — тот же паттерн, что `plan_share`/`micro_report_times`: сотрудник любой сети видел объявления и мог читать/писать в каналы вообще всех сетей. `GET/POST /announcements` теперь фильтруют/пишут `org_id`; `GET /announcements/:id/reads` проверяет принадлежность объявления сети менеджера; новый `assertChannelInOrg()` гейтит `GET/POST /channels/:id/messages`. `GET /access/requests` (`routes-v8.ts`) теперь фильтрует заявки по уже существующему сотруднику (`claimed_employee_id`) по его сети; заявка от ещё не найденного в системе гостя сети не имеет и видна всем управляющим — осознанное ограничение данных, не дыра. Заодно починена кнопка «Тикеты поддержки» — показывалась всем manager/senior и падала на 403, хотя `routes-support.ts` изначально задуман только для admin (эскалация к разработчику, не менеджерский инбокс сети) |
| **15.16.1** | Хотфикс, найден по жалобе «график сотрудника показывает мою сеть» при переключении admin между сетями. Настоящая причина оказалась серьёзнее: `GET /sales` вообще не проверял авторизацию и не фильтровал по сети — отдавал продажи ВСЕХ сетей за день (имена, точки, все метрики) кому угодно с любым или вообще без `X-Telegram-Id`. Добавлены `requireActive` + фильтр по сети точки (с self-inclusion, как в `/schedules` — своя продажа видна при подмене в чужой сети). `POST /sales` и `PUT /sales/:id/zero` тоже не проверяли, что точка принадлежит сети менеджера — manager одной сети мог вписать или обнулить продажу на точке вообще любой другой сети; теперь `assertStoreInOrg()`, кроме записи за самого себя (подмена остаётся легитимной). Заодно закрыты 4 места во фронтенде, где `org_id` не передавался при просмотре чужой сети admin'ом: карточка сотрудника, месячный график, форма добавления продажи, бейджи «продажи сегодня» в списке команды — везде тихо подставлялась своя сеть admin'а вместо той, что он смотрит |
| **16.0.0** | Пикер сети при регистрации гостя (`GET /access/orgs`, публичный) — заявка на доступ теперь несёт `org_id` и уходит только manager/supervisor/senior выбранной сети + admin (страховка на случай новой сети без активного управляющего), а не всем сетям сразу. `GET /access/employees-directory` тоже получил `?org_id=` — список «я из списка» раньше показывал сотрудников вообще всех сетей, гость сети B мог заклеймить сотрудника сети A. Важный смежный фикс: одобрение заявки (`POST /access/requests/:id/approve`) раньше создавало сотрудника в сети ОДОБРЯЮЩЕГО менеджера, а не в сети заявки — с admin, теперь получающим cc по заявкам любой сети, это стало реальным риском молча пересоздать сотрудника не в той сети. Деактивирована тестовая `test_network`, которая иначе попала бы в новый публичный пикер. Визуально: новый экран загрузки приложения (splash, `#appSplash`) вместо пустоты до первого ответа `/access/status`; экраны регистрации/ожидания/отказа доведены до токенов дизайн-системы (`.gate-icon`, `.btn-ghost` вместо инлайн-стилей); первое сообщение бота `/start` в приватном чате — с кнопкой «Открыть T2 Sales» (`web_app`-кнопка работает только в приватных чатах, не в групповых — учтено) |
| **16.0.1** | Хотфикс: admin не мог сохранить план точки чужой сети — 403 «Точка не принадлежит вашей сети», хотя сама точка корректно показывалась в списке (уже отфильтрованном переключателем сети). Бэкенд (`PUT /plans/stores/:id/month`) уже умел принимать `org_id` от admin, просто `saveStoreMonthPlan()` во фронтенде его не отправляла — тот же класс пропуска, что чинился весь этот сеанс в других местах |
| **16.1.0** | Новая сеть — без SQL. Admin заводит сеть прямо в приложении («Команда» → «Управление» → «Сети»): название, бренд, цвет, сектор (печатаешь новое имя — заводится тут же, отдельного экрана для секторов нет), chat_id и thread_id тем (узнаются командой `/chatid` боту). `upsertOrg()` раньше писал только 5 branding-полей — `sector_id/chat_id/sales_thread_id/reports_thread_id/is_active` существовали в схеме, но никогда не сохранялись через код; частичное сохранение (создать сеть раньше, чем узнают chat_id, дописать позже) — через `body[key] !== undefined` на новых полях и `COALESCE(...,organizations.x)` на branding-полях, чтобы пропущенное поле не затиралось молча. Форма новой точки получила часы работы/смены/время итога дня и переключатель «круглосуточно» — раньше этих полей не было в UI совсем, каждая точка тихо получала дефолт 10-21 (реальная круглосуточная точка требовала ручного SQL). Заодно `POST /stores` — тот же класс пропуска `org_id`, что чинился весь сеанс: admin не мог создать точку в чужой сети через переключатель |
| **16.2.0** | Кабинет супервайзера — полный рефактор оформления и функционала. Раньше supervisor делил интерфейс с manager: одна страница поверх обычных 5 вкладок, кнопка входа видна manager/admin/supervisor. Теперь — отдельный визуал: свой фиолетовый акцент (`.sv-*` классы в `styles.css` были захардкожены в основной синий, не через токены — перекрашены точечно), свои 4 вкладки (Обзор/Точки/Люди/Тренд, свой `#bottomNavSupervisor`) вместо обычных пяти. Реальная изоляция, не только визуальная: `GET /supervisor/dashboard` (`routes-supervisor.ts`) теперь только `supervisor | admin` — manager убран; `GET /supervisor/health` (виджет «Сеть за минуту» на главной manager/admin) не тронут, это отдельная фича на том же движке. Заодно закрыт смысловой разлад: обычные вкладки Команда/График/Касса резолвили supervisor в его СОБСТВЕННУЮ сеть (`resolveViewOrgId`), а не в весь сектор — то есть кабинет честно показывал весь сектор, а остальные вкладки — только одну случайную сеть; теперь supervisor вообще не монтирует обычные вкладки, только свой сектор. Точки и топ сотрудников в кабинете подписаны названием сети (`org_name`, join на `organizations` в `buildSupervisorDashboard()`) — раньше в кросс-сетевом списке было не видно, откуда какая запись. Admin заходит через кнопку «Кабинет супервайзера» (теперь `canAdmin()`, не `canViewAnalytics()`) с кнопкой «‹ Назад» |
| **16.2.1** | Хотфикс, найден по живой жалобе сразу после 16.2.0: у admin в кабинете супервайзера показывалась только его собственная сеть (РТТ Бижонов), точки РТТ Гуреева не отображались вовсе. `resolveSupervisorStores()` для `role='admin'` резолвит в одну сеть (`orgId`) — верно для `/supervisor/health` (виджет на ГЛАВНОЙ admin — его своя сеть), но не для самого кабинета: там кабинет обещает «весь сектор», а у admin персонального сектора нет, значит логичный эквивалент — вообще все сети. `GET /supervisor/dashboard` для admin теперь `scope: null` напрямую, минуя `resolveSupervisorStores()` |
| **16.3.0** | Кабинет супервайзера — выполнение и прогноз месячного плана. Вкладка «Точки» получила разворачиваемый список «Ещё метрики» — раньше показывались только SIM/MNP/ПА, теперь все 15. Вкладка «Тренд» получила: выполнение месячного плана по сектору целиком (сумма планов и факта всех точек по каждой метрике) и по каждой точке отдельно, плюс прогноз на конец месяца — та же модель, что уже была в `services/forecast.ts` (простое экспоненциальное сглаживание + сезонная поправка на день недели), но батчем одним запросом на весь сектор вместо N+1 по точке, и расширена с 4 метрик (SIM/MNP/ПА/Комбо) до всех 15. Найдены и починены при верификации перед деплоем два реальных бага модели на денежных метриках (Телефоны, Аксессуары): (1) `GROUP BY sale_date` молча пропускал дни без продаж — сглаженный уровень никогда не видел настоящий ноль и не спускался вниз; (2) холодный старт с фолбэком `baseline=1` был откалиброван под мелкие штучные метрики (SIM 0-10) — на суммах в тысячи рублей единственный ненулевой день после серии нулей делил сам на себя на «1» и улетал за 1000% плана. Оба фикса: заполнение пропущенных дней явными нулями + масштабирование холодного старта под реальное среднее метрики вместо универсальной константы |
| **16.4.0** | Новая быстрая вкладка на главной — «Сеть за месяц»: план/факт/% по всем 15 метрикам сразу, сумма по всей команде сети, строками-барами. Данные уже считались (`getMonthSummaryTable()` → `GET /plans/employees/month`, поле `totals.plan`/`totals.pct`), просто не были нигде показаны — существующая карточка «Итого сеть» на «Планы и факт за месяц» использовала только `totals.fact`, без плана и без цвета по проценту выполнения; заодно и её починили |
| **16.5.0** | «Сеть за месяц» получила разбор по сотрудникам — под общим итогом сети тот же барный визуал по каждому сотруднику (план/факт/% по всем метрикам, смены/остаток), реюз `svBarRowHTML`/`svExtraToggleHTML` из кабинета супервайзера. Отдельно — везде, где раньше выбор точки (продажа, график смен, касса, отчёты, тепловая карта) собирался из намеренно кросс-сетевого `GET /stores`, теперь используется новый `GET /org/stores` — точки только своей сети (с тем же admin-переключателем сети, что и в «Команде»); `GET /stores` остаётся кросс-сетевым для мест, которым это реально нужно (аналитика), просто больше не используется как источник для пикеров |
| **17.0.0** | Эпик «Надёжность перед деньгами»: CI на каждый push (`.github/workflows/ci.yml`) — Postgres-контейнер, проверка типов, smoke-тест фронтенда, 36 тестов на слой авторизации/изоляции сети (`vitest`, `backend/tests/`). Весь сеанс регулярно всплывал один и тот же класс багов — эндпоинт забывал отфильтровать данные по сети (сотрудники в 15.11.0, статистика/планы в 15.12.0, продажи без авторизации вообще в 15.16.1, пикеры точек в 16.5.0) — каждый раз находили вручную; теперь это проверяется автоматически. Тестовая БД — свежий schema-only снапшот прод-схемы (`sql/ci-schema.sql`, без данных и паролей, CI поднимает его в одноразовом контейнере — никакого доступа к проду из CI не требуется). Для `app.inject()` без реального порта вынесена `buildApp()` (`backend/src/app.ts`) из `index.ts` — тот теперь только `buildApp()` + `listen()` + бот/крон. Попутно, пока строили тесты именно на этот класс багов, нашли и закрыли реальную дыру: `PUT /cash` вообще не проверял, что точка принадлежит сети сотрудника (в отличие от `/schedules`/`/sales`, где эта проверка уже была) |
| **17.0.1** | Хотфикс первого прогона CI: Postgres-сервис в workflow был версии 16, а `sql/ci-schema.sql` снят `pg_dump` версии 18 (та же версия, что и на проде) — часть схемы не грузилась (`psql` падал с exit 3). Подняли сервис до `postgres:18`, чтобы версия совпадала с прод-БД |
| **17.1.0** | Вторая волна тестов изоляции — статистика/дэшборд (`/stats/daily`, `/dashboard`), живая карта (`/network/live`), планы (`/plans/employees/month`, `/plans/stores/daily`, `/plans/stores/:id/month`), промокоды (`/promos`), объявления и каналы (`/announcements`, `/channels`), заявки на доступ (`/access/requests`) — ещё 26 тестов на те самые места, где раньше находили реальные дыры (15.12.0, 15.15.0, 15.16.0). Итого 62 теста на слой авторизации/изоляции сети |
| **17.2.0** | Аудит всех роутов (`grep` на отсутствие `org_id`/`resolveViewOrgId`/`assertStoreInOrg`) нашёл 4 реальные дыры, которые предыдущие волны эпика 17.0 не задели: `GET /bfq`, `/bfq/:employeeId` были вообще без авторизации — кто угодно без токена мог узнать BFQ любого сотрудника любой сети по id; `GET /sales/history`, `/sales/audit`, `/export/sales.csv`, `/export/bfq.csv`, `/export/schedules.csv` отдавали данные сразу всех сетей любому manager; `/sales/quick` и `/sync/batch` (офлайн-очередь) — параллельные пути внесения продажи в обход основного `POST /sales` — не проверяли ни своего сотрудника, ни свою точку; `GET /reports/day/:storeId` отдавал превью отчёта любой точки по id. Все четыре закрыты тем же паттерном (`resolveViewOrgId`/`assertStoreInOrg` + новый `assertEmployeeInOrg`) и покрыты 22 регрессионными тестами — итого 84 теста изоляции. Заодно докручен admin-переключатель сети на новых проверках (BFQ-карточка в «Команде», CSV-экспорты, превью отчёта) — тот же класс пропуска `org_id`, что чинился весь сеанс |
| **17.3.0** | Ещё 2 дыры того же класса: what-if симуляция переноса смен (`/schedule/what-if`, `/schedule/what-if/apply`) тянула точки ВСЕХ сетей без фильтра — `/apply` реально пишет в `schedules`, то есть можно было переставить чужого сотрудника на точку любой другой сети; теперь сценарий строится только по своей сети. `POST /alerts/:id/ack` гасил алерт по id без проверки сети — manager другой сети мог тихо снять чужой критический алерт. Плюс регрессионные тесты на уже закрытые в 15.15.0 места (`/forecast`, `/heatmap`, `/cohorts/newbies`, `/staffing-hints`, `/export/bi/daily`) — итого 96 тестов изоляции. Заодно переименован «Эпик 17.0» в истории версий 15.8.0-15.16.0 (старое кодовое имя эпика мультитенанта этого же сеанса) — не путать с реальной версией 17.x, это другой, более поздний эпик |
| **17.4.0** | Последний пункт «Дорожной карты»: рендер отчётов (`resvg`, SVG→PNG) вынесен в `worker_threads` (`src/workers/svg-render.worker.ts` + пул из 2 воркеров, `services/svg-render-pool.ts`) — замерено, один рендер занимает 350-400мс синхронной работы, раньше на это время блокировался вообще весь сервер (cron шлёт микро/итоговые отчёты по каждой точке несколько раз в день). Проверено на реальных данных через `buildApp()` + `app.inject('/health')`, поллинг каждые 15мс во время реального рендера — event loop не блокируется, ответ стабильно <1мс. Плюс включён `checkSuites` на deployment trigger Railway — красный CI теперь блокирует деплой |
| **17.5.0** | Система миграций (`src/db/migrate.ts`) вместо ad hoc SQL руками на проде. Пронумерованные файлы в `sql/migrations/` + трекинг в `schema_migrations` — применяются сами: на проде автоматически при старте сервера (до открытия порта), в CI на каждый push, локально `npm run migrate`. `0001_baseline.sql` — снапшот текущей схемы, на самом проде не исполнялся (таблица заранее засеяна записью об этом файле), только на чистой БД (CI/локально). Заодно удалён `sql/ci-schema.sql` — его содержимое теперь и есть `0001_baseline.sql`, поддерживать два файла с одной и той же схемой смысла не было |
| **17.5.1** | Хотфикс: 17.5.0 крашлупился на реальном деплое (`restartPolicyType: ON_FAILURE`, Railway ретраил старт раз в секунду) — миграции лежали в `sql/migrations/` на корне репозитория, а Root Directory сервиса на Railway = `backend`, всё, что снаружи, в контейнер не попадает вообще. Перенесены в `backend/migrations/`. Заодно найден и починен смежный баг, который вскрылся при отладке: `set_config('search_path','',false)` внутри `0001_baseline.sql` (стандартный pg_dump) не транзакционный — переживает даже `ROLLBACK` — и «протекал» на весь пул соединений: следующий случайный запрос, который доставал ту же переиспользуемую коннекцию из пула, падал с «relation X does not exist» на ровном месте. `RESET search_path` теперь явно вызывается перед возвратом клиента в пул. Старый (17.4.0) деплой всё это время продолжал обслуживать реальный трафик — простоя не было |
| **17.6.0** | Алерт в рабочий чат при падении миграции или старта сервера (`alertAndExit()` в `index.ts`, переиспользует уже существовавший, но нигде не подключённый `notifyAdmin()` из `bot/index.ts`) — раньше единственным сигналом были Railway logs, которые никто не смотрит проактивно, именно так сегодня и нашли баг 17.5.0. Таймаут 5с на отправку, чтобы недоступность Telegram не подвесила сам выход из процесса |
| **17.7.0** | Пользователь принёс security-чеклист от стороннего ИИ — большая часть уже была закрыта этим сеансом или неприменима к масштабу проекта, но сплошной проход по всем `PATCH/PUT/DELETE` подтвердил самое главное подозрение и нашёл реальный захват аккаунта: `POST /me/bind` брал telegram_id из тела запроса, а не из подтверждённой Telegram initData — любой мог отвязать чужой telegram_id (в т.ч. admin) и привязать свой, без единой авторизации. Плюс ещё 8 эндпоинтов без проверки сети (`PATCH/DELETE /employees/:id` и `/stores/:id`, `PATCH /employees/:id/role`, `DELETE /schedules`, `GET/PUT /plans/employees/:id/month`, approve/reject заявок на доступ), `PUT /supervisor/:id/sector` сужен до admin (межсетевые полномочия), удалены 2 неиспользуемых уязвимых дубликата эндпоинтов, `employees.telegram_id` получил `UNIQUE` (закрывает race condition в bind/approve — были не в транзакции). 121 тест изоляции |
| **17.8.0** | Оставшиеся 2 пункта из внешнего чеклиста. CORS (`origin: true` → `origin: false`) — фронтенд всегда бьёт на API своим же origin (`window.location.origin`), легитимного кросс-origin браузерного вызова нет; проверено вживую через Playwright (свой origin работает, чужой блокируется). `EXPLAIN (ANALYZE, BUFFERS)` на проде по самым частым запросам подтвердил: `stores`/`employees` без индекса на `org_id` вообще — каждый org-scoped запрос сеанса Seq Scan; сейчас незаметно (7×7 строк), но первое, что упрётся при росте, особенно в N+1-циклах (`getLiveNetworkMap`, `calculateAllBFQ` — по 4 запроса на точку/сотрудника). Добавлены индексы (`stores.org_id`, `employees.org_id`, `sales`/`schedules(store_id, дата)`); сами N+1-циклы намеренно не трогали — преждевременная оптимизация при текущих объёмах (`supervisor-analytics.ts` уже показывает, как это делать правильно — батчем на весь сектор, 16.3.0) |
| **17.9.0** | Начало аудита корректности бизнес-операций (не security-изоляция, а «может ли система прийти в состояние, невозможное в реальной работе»). Первая находка: три места записи продажи (форма, быстрый текстовый ввод, офлайн-очередь) были тремя разошедшимися кусками SQL — только форма писала пол через `GREATEST(0, ...)`, только форма и очередь писали в `sales_audit`/heatmap, быстрый ввод не уведомлял чат вообще. Сведены в один `applySaleUpsert()` (`services/sales-write.ts`). Ни одна точка входа не защищала от повторной отправки — двойной тап или сетевой ретрай после «потерянного» ответа удваивал сумму (запись аддитивная); теперь форма и быстрый ввод тоже принимают `client_id` (тот паттерн, что раньше был только у офлайн-очереди), кнопка дизейблится на тап, а очередь переиспользует `client_id` формы вместо генерации своего — иначе дедуп ломался ровно в сценарии «запрос дошёл, ответ клиент не увидел». 7 новых тестов, итого 128 |
| **17.10.0** | `/shifts/open`/`/shifts/close` ничем не были ограничены по числу вызовов в день — каждый close начислял полную награду (XP + бейджи + streak_days) заново, можно было бесконечно фармить прогресс одним спамом кнопки без единой реальной продажи. Награда теперь не более раза за календарный день; сама смена по-прежнему закрывается нормально каждый раз. Заодно найден смежный скрытый баг: `toDateISO()` читал pg `date`-колонку через `getUTC*()`, хотя node-postgres парсит её в полночь ПО ЛОКАЛЬНОМУ времени процесса — на проде (контейнер в UTC) не проявлялось, но сдвигало дату на день назад при любом локальном TZ восточнее UTC (в т.ч. Europe/Moscow — таймзона самого проекта). Заменено на локальные геттеры. 9 новых тестов, итого 136 |
| **17.11.0** | Настоящие гонки (не последовательный спам, а параллельные запросы) в открытии/закрытии смены. `/shifts/close`: два одновременных запроса на закрытие одной сессии оба награждали XP — гейт из 17.10.0 не ловил гонку с самим собой. Переход в `closed` теперь атомарный compare-and-swap; проигравший получает уже закрытую сессию без повторной награды. `/shifts/open`: два одновременных запроса могли создать два «открытых» сеанса для одного сотрудника — добавлен partial unique index, проигравший получает сессию победителя. 2 новых теста с реальными параллельными запросами, итого 138 |
| **17.12.0** | `store_plans` (дневной снапшот плана точки — источник данных для BFQ, live-карты, дашборда, отчётов, supervisor-аналитики) материализуется только кроном в 6:00 МСК. Правка плана точки (`PUT /plans/stores/:id/month`) снапшот не трогала — новые цифры были видны сразу только в `GET /plans/stores/daily` (единственный живой расчёт), всё остальное показывало бы вчерашний план вплоть до следующего утра. Теперь снапшот на сегодня/завтра пересчитывается сразу после сохранения плана. 1 новый тест, итого 139 |
| **17.13.0** | Самая серьёзная находка за весь заход: BFQ (Качество/Прибыль сотрудника) считал план из `store_plans WHERE plan_date IS NULL` — строки, создающейся ОДИН РАЗ нулями при создании точки и не обновляющейся больше нигде в коде. Реальный, редактируемый план сотрудника (`employee_month_plans`) в расчёт BFQ не попадал никогда — сколько бы менеджер ни менял план через `PUT /plans/employees/:id/month`, BFQ считал против фантомного нулевого плана, что делало коэффициенты качества/прибыли оторванными от реальных целей практически у всех сотрудников. Теперь BFQ берёт план из того же живого источника, что и остальные /plans-эндпоинты — правка видна сразу. Ничего не нужно исправлять в старых данных: BFQ нигде не сохраняется, считается заново на каждый запрос. 1 новый тест, итого 140 |
| **17.14.0** | Две дыры целостности при увольнении сотрудника. `POST /me/bind` не проверял `is_active` целевой карточки и сам же реактивировал её на любой bind — employee_id маленький и последовательный, легко угадать/запомнить: кто угодно руками мог привязать свой Telegram к карточке уволенного и унаследовать всю его историю продаж/BFQ/XP. Теперь бинд на деактивированную карточку — 409, реактивация только осознанным действием менеджера. `DELETE /employees/:id` (soft-delete) не чистило будущие смены — уволенный продолжал бы висеть в завтрашнем графике и учитываться в покрытии точки; теперь будущие смены удаляются при увольнении, прошлые (реальная история) остаются нетронутыми. 3 новых теста, итого 142 |
| **17.15.0** | Симметрично 17.14.0: `DELETE /stores/:id` (закрытие точки) тоже не чистило будущие смены на ней — сотрудники продолжали бы висеть в графике закрытой точки. Прошлые смены (история) не трогаем, будущие удаляются при закрытии. `PATCH`/`DELETE /stores/:id` подтверждены как чистый soft-delete без каскада — исторические продажи/смены на удалённой точке в безопасности по построению. 1 новый тест, итого 143 |
| **17.16.0** | Micro/итоговые фото-отчёты в чат и напоминания «завтра смена» не были защищены от повторной отправки — deploy-окно Railway (старый контейнер жив, пока новый не пройдёт healthcheck) или ручная кнопка «отправить сейчас», совпавшая по минуте с автоматическим тиком, могли задвоить фото в чате. Новая `cron_send_log` — claim по ключу точка+дата+час перед каждой автоматической отправкой, тот же паттерн, что `offline_sync_log`/`client_id` (17.9.0); ручная кнопка намеренно не участвует в claim — это осознанное действие менеджера. 4 новых теста, итого 147 |
| **17.17.0** | Последний пункт аудита корректности: денежная точность. Проверено (не проигнорировано) — все денежные/дробные колонки на `numeric`, ни одной на `real`/`double precision`; единственные float-колонки во всей схеме — GPS-координаты, где это уместно. Суммирование всегда на стороне SQL — классический `0.1 + 0.2 !== 0.3` не воспроизводится в принципе. Фикса не потребовалось, архитектура уже была безопасна — 2 новых теста фиксируют это как регресс-гвард (типы колонок + end-to-end: 10.1+20.2+5.05 = ровно 35.35). Итого 149 — этим закрывается весь список бизнес-корректности перед 18.0 Core Freeze |
| **18.0.0** | **Core Freeze.** Эпоха 18 открывается фиксацией ядра (не новой фичей): auth/Telegram, multi-tenant, RBAC, employees, stores, shifts, sales, plans, cash, BFQ, offline sync, cron, audit — всё под 149 регресс-тестами, после эпика 17.0 «Надёжность перед деньгами» (17.9.0–17.17.0). После 18.0 ядро меняется только контролируемо, с regression tests — дальше roadmap идёт в продуктовые эпики |
| **18.1.0** | **Command Center.** Первый продуктовый эпик: единый экран manager/supervisor/admin вместо трёх разрозненных мест (виджет на главной, live-карта, кабинет супервайзера) — что происходит / где проблема / что делать. Новый `GET /command-center` не дублирует расчёты, собирает существующие `buildSupervisorDashboard()` и `smart_alerts` (кассовые разрывы/простой часа — работали, но фронт их нигде не показывал) в один ответ. Новое: `findUnderperformingEmployees()` — персональная просадка (0 продаж при работающих коллегах), не только по точке. Действия ведут на существующие экраны, ничего не дублируем; conversion и задачи из проблемы — намеренно вне скоупа (нет данных о трафике, задач ещё нет — это 18.2). 6 новых тестов, итого 155 |
| **18.2.0** | Новый блок «Сводный график команды» на вкладке График (manager/senior/admin) — весь месяц одной таблицей с указанием точки на каждый рабочий день (важно при подменах — видно, кто где именно в этот день). Метрика НВ добавлена в «Блок GI» на вкладке План. Pull-to-refresh больше не срабатывает случайно на обычном скролле к началу страницы — жест теперь отслеживается целиком, а не только по началу/концу касания, плюс видимый индикатор и cooldown. Анонс релиза в чат мог продублироваться при деплое (реально случилось с 18.1.0) — тот же claim-паттерн, что уже чинил дубли cron-отчётов (17.16.0), теперь и здесь. 4 новых теста, итого 159 |
| **18.3.0** | Личный помесячный график сотрудника (под сводной таблицей) шёл просто по порядку 1..30/31 без выравнивания по дням недели — непонятно было, где выходные. Теперь настоящая календарная сетка: понедельник первой колонкой, дни соседних месяцев — серые, полупрозрачные, некликабельные, день 1 в своей настоящей колонке, сверху подпись дней недели |
| **18.4.0** | **Tasks / Action System.** Замыкает цикл данные → alert → action → задача → результат, который Command Center (18.1) начал, но не заканчивал — его кнопки действий теперь умеют создавать задачу с предзаполненным контекстом, а не только открывать существующие экраны. Менеджер создаёт и назначает задачу — сотрудник получает Telegram-сообщение и видит её на «Моём дне», берёт в работу/отмечает выполненной, менеджер получает сообщение обратно, когда задача закрыта. Новая страница «Задачи» — список по сети с фильтрами и тредом комментариев (он же история статусов). Архитектура зеркалит уже отработанный `support_tickets` + `support_messages`. 15 новых тестов, итого 174 |
| **18.5.0** | **Store Intelligence.** Профиль точки впервые в проекте — план/факт/прогноз, тренд, задачи и алерты на одном экране, открывается из live-карты или Command Center. Store Health Score — объяснимая композиция (план/темп дня/штат/касса), без выдуманных Revenue/Conversion/Avg check — данных о трафике и деньгах в системе нет, разбивка компонент готова принять пятое измерение, когда источник трафика появится. Попутно найден и исправлен реальный баг: тренд по точке/сети был ВСЕГДА пустым с момента появления этого расчёта (дата из Postgres приводилась к строке через `String(dateObject)`, получая нечитаемое "Tue Aug 11" вместо ISO-формата) — молча ломало график тренда и в кабинете супервайзера тоже. 6 новых тестов, итого 180 |
| **18.6.0** | **Alerts 2.0.** Полный жизненный цикл алерта вместо open→acked — добавлены in_progress/dismissed, новая страница «Алерты» с фильтрами и деталями, кнопка «Взять в работу» прямо в Command Center. Задача, созданная из алерта, теперь сама закрывает его при выполнении. Попутно найден и исправлен третий за вечер баг того же класса (после 17.16.0 и 18.2.0): `insertAlertOnce()` была неатомарной SELECT+INSERT-проверкой — при деплое оба перекрывшихся контейнера могли создать дубль и оба уведомить admin, вероятный источник жалобы «менеджер получает 15 одинаковых alerts» из роадмапа. Атомарный `INSERT ... ON CONFLICT DO NOTHING` по partial unique индексу. 8 новых тестов, итого 188 |
| **18.7.0** | **Shift 2.0.** Смена — объект с памятью вместо пары timestamp'ов. При открытии сотрудник сразу видит план на сегодня, свои открытые задачи и заметку-передачу от предыдущей смены на этой точке, если она была оставлена. Пока смена открыта, карточка на «Моём дне» показывает живой прогресс план/факт — раньше факт дня считался только один раз, в момент закрытия. При закрытии можно оставить заметку для следующего, кто откроет смену на этой точке. Расчёт план/факт дня вынесен в переиспользуемый `services/shift-pace.ts` и используется одной формулой в open/current/close. 4 новых теста, итого 192 |
| **18.8.0** | **Employee 2.0.** Профиль сотрудника впервые в проекте — BFQ, геймификация, история смен и объяснимый Employee Health Score (план/идеальные смены/явка/стабильность) на одном экране, вместо прежней карточки с одним лишь планом/фактом на сегодня. Прямой аналог Store Health Score (18.5), только для сотрудника — никакого нового расчётного движка, только сборка уже существующих BFQ/геймификации/истории смен в одном месте. Открывается кнопкой «Профиль →» в существующей карточке сотрудника или из Command Center. 5 новых тестов, итого 197 |
| **18.9.0** | **Reports.** Новая страница «Отчёты» собирает разбросанную по интерфейсу отчётность (SVG-картинка точки, CSV-экспорт) в одном месте, плюс то, чего не было вообще — недельная/месячная сводка по сети (план, темп, лучшие/отстающие точки), переиспользующая тот же `buildSupervisorDashboard()`, что Command Center и Store Profile. Автоматически по понедельникам и 1-го числа в 09:00 МСК, плюс ручная кнопка. Рассылка защищена тем же atomic-claim паттерном, что уже трижды закрывал дубли в этой эпохе (17.16.0/18.2.0/18.6.0). 6 новых тестов, итого 202 |
| **18.10.0** | **UX/polish.** Профиль точки и профиль сотрудника были тупиковыми экранами — открывались с конкретным id (карточка/live-карта/Command Center), после чего вернуться было некуда, кроме нижней навигации, которая не помнит источник. Кнопка «‹ Назад» теперь ведёт туда, откуда реально открыли. Кнопки смены статуса задачи/алерта и отправки сетевой сводки блокируются на время запроса — тот же паттерн, что у формы добавления продажи, защита от двойного тапа. Только фронтовые изменения |
| **18.11.0** | Анонс новой версии теперь уходит только в выделенный Telegram-канал вместо общего рабочего чата сети — решение владельца продукта. Продажи/алерты/отчёты не тронуты, поменялся только адресат анонсов: один явный `RELEASE_CHANNEL_ID` вместо перебора `organizations.chat_id` с фолбэком на глобальный `CHAT_ID` |
| **19.1.0** | **Forecast v2 — эпоха 19 (Intelligence).** Попутно найден и исправлен реальный баг: страница «Прогноз» точки использовала более старую, менее защищённую версию модели прогноза, чем месячный прогноз кабинета супервайзера (не заполняла пропущенные дни нулями, не клэмпила выбросы на денежных метриках) — обе версии сведены в одну переиспользуемую модель, баг закрыт сразу везде. Страница «Прогноз» получила визуал формы недели и короткое AI-объяснение прогноза (Groq) — сначала расчёт, потом объяснение поверх него, не наоборот. 7 новых тестов, итого 209 |
| **19.2.0** | **Anomaly Detection.** Новый тип алерта `anomaly_vs_forecast` — единственный триггер smart_alerts, сравнивающий вчерашний день со статистически типичным для этой точки в этот день недели (z-score против forecast-модели 19.1), а не с планом или фиксированным порогом. Ловит обе стороны — и провал, и необычный всплеск (раньше ни один триггер не отмечал хорошие дни). Ноль новой инфраструктуры — переиспользует весь workflow Alerts 2.0 как есть, просто новый alert_type на уже тикающем таймере. 8 новых тестов, итого 217 |
| **19.3.0** | Расчёт комбо: сумма в формуле 1900 → 1950. Новый калькулятор «Школа» (цена − 70% + 30% от цены + 3600 + 3490). Убрана кнопка «Записать дневные планы в БД» — обе ситуации, ради которых её нажимали, давно автоматизированы (ежедневный крон 6:00 МСК + мгновенный пересчёт при правке плана точки), кнопка была мёртвым кодом |
| **19.4.0** | **Главная и Профиль — первый батч UX-правок.** «Мой день» показывает все метрики с ненулевым дневным планом (не жёстко зашитую пятёрку). «Мой день» и «Сеть сегодня» объединены в один слот со свайпом между ними (переиспользуемый жест, пригодится для свайпа между вкладками позже). Над «Мой день» — код и адрес точки (или «Выходной»). В Профиле убраны дублирующие блоки «Факт / план дня» и «Мои продажи сегодня», кольцо «Сегодня» скрыто на выходной, «Прогресс за месяц» в карточном стиле, добавлена «История твоих продаж». Из «Быстрых действий» убраны «Добавить продажу» и «Кто на смене сегодня». Вкладки «Главное»/«Мой» переименованы в «Главная»/«Профиль» |
| **19.5.0** | Правки по отзывам на 19.4.0. Свайп-карточка «Мой день»/«Сеть сегодня» больше не растягивается под высоту длинной панели — заодно нашёлся и убран настоящий баг с лишним `overflow:hidden` на треке, ломавший отрисовку только что появившейся панели. «Прогресс за месяц» в Профиле переведён на формат «Блок GI/Товарка/Ростелеком/Кредиты» с барами факт/план — как на карточке точки в «План», а не числовая сетка. Шапка приложения больше не перекрывается плавающими системными кнопками Telegram на части клиентов — учтён `contentSafeAreaInset` |
| **19.6.0** | **Навигация и жесты.** Карточки точек в «План» больше не разворачивают первую по умолчанию — все свёрнуты. Лёгкий свайп между вкладками нижней навигации (с анимацией перехода, без живого перетаскивания страницы) — с защитой от срабатывания поверх модалок/обучения и внутри горизонтально прокручиваемых блоков. Модалка добавления продажи закрывается свайпом вниз за ручку/заголовок; заодно закрыт побочный баг с pull-to-refresh, который мог сработать в фоне при свайпе по открытой модалке |
| **19.7.0** | **Кастомные названия точек, аватарки, визуал графика и касса.** Руководители/старшие продавцы могут задать точке кастомное название — подменяет обычное имя везде в мини-аппе (не в Telegram-сообщениях). Кастомная аватарка загружается из Профиля и видна в шапке и в Команде — раньше Команда показывала только буквы-инициалы. Сводный график сети перекрашен в тот же цветной язык, что и календарная сетка на той же странице. Вкладка «Касса»: «Внести кассу» наверху, только 2 последних дня видны сразу, сама вкладка переехала в «Быстрые действия» первым пунктом. 8 новых тестов, итого 225 |
| **19.8.0** | **Google Sans, иконки вместо эмодзи, Android-hardening.** Шрифт приложения — Google Sans (легально лицензированный OFL-дистрибутив). Весь эмодзи-набор (119 вхождений, 62 уникальных) заменён на line-иконки Lucide — маскот «Арбузыч» и типографские стрелки в тексте оставлены как есть, цветные точки-статусы переведены на CSS-кружки. Android-hardening без привязки к конкретным багам: viewport-fit=cover, overscroll-behavior против конфликта с pull-to-refresh, 100dvh точнее 100vh |
| **19.9.0** | Подпись к картинке анонса версии в Telegram-канале теперь несёт заголовок и все пункты обновления целиком, а не короткую строку — не нужно отдельно открывать картинку, чтобы прочитать, что изменилось. Длинные релизы аккуратно обрезаются по границе слова под лимит Telegram в 1024 символа |
| **19.10.0** | Новое оформление карточки анонса — цветной градиентный блок-заголовок с крупным номером версии, скруглённая карточка, список изменений снизу, шрифт Google Sans. Все прошлые анонсы перегенерированы и переотправлены в канал в новом виде |
| **19.11.0** | Аудит контроля доступа: усилена авторизация на ряде эндпоинтов (identity — только из подтверждённой Telegram-подписи, запись/чтение — строго в рамках своей сети), аккуратные ответы об ошибке вместо падения сервера на некорректных входных данных. 26 новых регрессионных тестов (`tests/adversarial/`), итого 255 |
| **19.12.0** | Убраны свайп между вкладками нижней навигации и pull-to-refresh (обновление вытягиванием страницы сверху) — по прямому запросу владельца продукта. Обновить страницу по-прежнему можно кнопкой ↻ в шапке |
| **19.13.0** | Код точки и адрес в шапке приложения (рядом с датой). Живой индикатор «Смена открыта/закрыта» и бейдж «N дн. до выходного» на карточке приветствия. «Промокоды РТК» продублированы широкой кнопкой в блоке «Калькуляторы». Убран дублирующий пункт «План дня по точкам» из Быстрых действий, «Сеть за месяц» переименована в «Динамика выполнения по сотрудникам» |
| **19.14.0** | **Rate limiting, security headers, прод-гварды.** `@fastify/rate-limit` на всё API (общий потолок + жёсткие лимиты на публичную аватарку/отчёты/заявку на доступ), `@fastify/helmet` (CSP и другие заголовки, подобрано под встраивание в Telegram и под onclick/style-атрибутную вёрстку фронтенда). Сервер отказывается стартовать в проде без `BOT_TOKEN` или с `ALLOW_INSECURE_AUTH=true`. Срок жизни Telegram-сессии — 1 час вместо суток, при истечении понятное сообщение вместо голого 401. Загрузка аватарки проверяется по magic bytes реального файла, а не по заголовку от клиента; SVG больше не принимается. `@fastify/static` обновлён до версии, закрывающей несколько известных уязвимостей обхода пути |
| **19.15.0** | **Единый обработчик ошибок, аудит в CI, контекстные логи.** Глобальный Fastify `setErrorHandler` мапит известные коды ошибок Postgres (дубликат → 409, некорректный формат → 400 и т.п.) в стабильный JSON вместо голого 500 с сырым текстом драйвера; 16 роутов, отвечавших клиенту тем же сырым текстом вручную, переведены на общий безопасный хелпер. CI падает при известной уязвимости высокой критичности в зависимостях (`npm audit --audit-level=high`). Логи сервера несут `employee_id`/`org_id` на каждой строке запроса |
| **19.16.0** | `stores.id` получил настоящий `PRIMARY KEY` — найдено живой проверкой нового error handler (19.15.0): дубликат id точки тихо создавал вторую строку вместо ошибки. В проде дублей не было, миграция накатилась без изменения данных |
| **19.17.0** | **Tenant-check как декораторы роутов.** 18 write/read-роутов переведены с ручного вызова `assertStoreInOrg`/`assertEmployeeInOrg` внутри тела обработчика на `requireStoreInOrg()`/`requireEmployeeInOrg()` — preHandler-декораторы в опциях роута, которые нельзя случайно забыть в новом коде (именно так были упущены дыры 19.11.0). Поведение не изменилось: 255/255 тестов прошли без расхождений. Роуты, где store/employee id узнаётся только после fetch внутри самого обработчика, или где self-write/manager-for-other разруливается по-разному — оставлены с ручной проверкой намеренно (декоратор технически не может знать эту логику заранее) |
| **19.18.0** | **Типизированная валидация — первый заход.** TypeBox-схемы (`schema.body` на уровне роута, Fastify/ajv валидирует до входа в обработчик) на самых денежных/расписательных write-эндпоинтах: `/sales`, `/sales/:id/zero`, `/cash`, `/schedules`, `/schedules/bulk`, `/shifts/open`, `/shifts/close`, `/sales/parse`, `/sales/quick`, `/sync/batch`. Некорректный запрос отвечает понятной ошибкой (`validation_failed` + список того, что именно не сошлось) сразу, а не падает по пути к базе. Остальные ~25 write-роутов — следующими заходами |
| **19.19.0** | TypeBox-схемы на роутах доступа/ролей и CRUD сотрудников/точек (`/access/request`, approve/reject, назначение роли и сектора, `/employees`, `/stores`, `/me/bind`). Попутно найден и исправлен реальный регресс, который сам процесс подключения схем чуть не внёс: `ajv` (встроен в Fastify, `coerceTypes: true`) по умолчанию тихо превращает `null` в `0` для числовых полей — открытие/закрытие смены без геолокации едва не начало писать координаты `0,0` вместо честного «геолокации нет». Пойман до продакшена живой проверкой, не постфактум |
| **19.20.0** | TypeBox-схемы на задачах (создание/комментарии/статус/переназначение), тикетах поддержки, промокодах РТК, объявлениях и каналах сети — на этот раз все места, где фронтенд намеренно шлёт `null` (сброс дедлайна задачи), проверены заранее по урокам 19.19.0 |
| **19.21.0** | **Переход на TypeBox завершён.** Последний заход — алерты, what-if сценарии, кастомные метрики, месячные планы, ручная отправка отчётов, сеть/бренд (админ), обучение; попутно найдены и закрыты два роута (BFQ: VMR+штраф, анкета), пропущенные более ранним заходом. `request.body as any` в write-роутах закрыт по всему API |
| **19.22.0** | **Data Access Layer — старт.** Первый repository (`src/repositories/stores.ts`) — доступ к таблице `stores` только через него, `orgId` первым обязательным параметром каждой функции (не «не забыть проверить», а физически невозможно вызвать без сети). Роуты точек выделены в `routes-stores.ts`, CI проверяет, что этот файл не откатился на прямой SQL. Пилот на одной сущности — Employees/Sales/Schedules следующими заходами |
| **19.23.0** | **Audit Trail.** Новая таблица `audit_log` — единая лента чувствительных действий (смена роли, деактивация сотрудника, правка продажи, изменение плана, экспорт CSV), экран «История действий» для admin. Первое появление транзакций (`withTransaction()`, `db/index.ts`) в прикладном коде — смена роли и правка продажи теперь либо коммитятся целиком вместе с записью в историю, либо откатываются целиком |
| **19.24.0** | **Concurrency & Workflow Integrity.** Перед тем как писать код, проверено, что уже реально защищено (смена — partial unique index, закрытие смены — CAS, `/sync/batch`/`/sales/quick` — UNIQUE на `client_id`) — всё оказалось надёжным с прошлых версий, закреплено новыми adversarial-тестами, которые реально стреляют параллельными запросами. Найдена и закрыта одна настоящая гонка: `materializeStoreDailyPlans()` (крон 6:00 + ручное сохранение плана точки) могла оставить задвоенные строки — теперь `UNIQUE(store_id, plan_date)` + per-store upsert вместо delete-then-insert |
| **19.25.0** | **Supervisor Scope Cache.** Кабинет супервайзера и Command Center больше не пересчитывают заново JOIN `supervisor_sectors → organizations → stores` на каждую загрузку — держится в памяти сервера 5 минут (`services/scope-cache.ts`). Redis сознательно не взят — прод на 1 реплике Railway (бот на long-polling физически не может жить на двух), in-memory даёт ту же корректность без новой инфраструктуры. Автосброс при смене сектора супервайзера (точечно) и при переносе сети между секторами (весь кэш). `GET /admin/cache-stats` — hit/miss метрики. Этим закрыт весь 5-пунктовый roadmap владельца продукта перед 20.0.0 |
| **20.0.0** | **Frontend Foundation — старт эпохи 20.** Первый Vite-билд: типизированный API-клиент (`frontend/src/api-client.ts`, TypeScript ES-модуль, собирается в один classic-script бандл) для выбора точек (`/org/stores`) и каталога метрик (`/metrics`) — общий контракт типов (`backend/src/shared/api-types.ts`) переиспользуется и роутами, и фронтендом, без дублирования. `01-core.js` делегирует оба вызова новому клиенту, поведение снаружи не изменилось. Фундамент для frontend-тестов (vitest + jsdom, `frontend/tests/`). Пилот на двух эндпоинтах — остальные 19 файлов `frontend/js/` переезжают на TypeScript следующими версиями той же схемой, что уже работала для Data Access Layer и TypeBox |
| **20.0.1** | Хотфикс сразу вслед за 20.0.0 — прод-деплой упал на билде: Railway/Nixpacks собирает контейнер на Node.js 18.20.5, а Vite 8 требует `^20.19.0 \|\| >=22.12.0` (внутри зависит от `util.styleText`, которого в Node 18 нет). Версия Node для деплоя нигде не была закреплена. Добавлено `"engines": {"node": "22.x"}` в `backend/package.json` — тот же Node 22, что уже использует CI (`actions/setup-node`) |
| **20.0.2** | Из-за 20.0.1 не ушёл анонс 20.0.0 в канал — `announceReleaseIfNeeded()` сверял `CHANGELOG` со своим же `package.json.version`, а тот на первом успешном старте контейнера был уже `20.0.1` (хотфикс, сознательно без записи в `CHANGELOG`) — функция тихо выходила. Переписано на сверку с последней записью `CHANGELOG` напрямую, не с версией запущенного процесса — тот же класс защиты, что не даст молча потерять анонс, если версия с записью в `CHANGELOG` ещё раз упадёт на деплое |
| **20.1.0** | **Второй срез Frontend Foundation.** Промокоды РТК (`12-promos.js`) переехали на typed API-клиент — 5 функций (`getPromos`/`getPromoCard`/`createPromo`/`markPromoUsed`/`keepPromo`) в `frontend/src/api-client.ts`, общий контракт расширен в `backend/src/shared/api-types.ts`. Заодно клиент научился разбирать серверное `{error, message}` на не-ok ответе вместо голого статус-кода — нужно было, чтобы toast с ошибкой сохранения промокода показывал прежний осмысленный текст, не изменившееся поведение для двух прежних эндпоинтов (01-core.js их не читает). Живой прогон нашёл не связанную с этим заходом мелочь: `code_required` в `routes-promos.ts` недостижим — TypeBox-схема (`minLength: 1`) перехватывает пустой код раньше и всегда отвечает `validation_failed` |
| **20.2.0** | **Третий срез Frontend Foundation.** Касса (`GET /cash/table`, `PUT /cash`) и кастомные метрики плана (`POST /metrics`, `DELETE /metrics/:id`) — на typed-клиент. Найдено и исправлено реальное расхождение приоритета сообщения об ошибке: клиент с 20.1.0 бросал `error`-код первым, а `09-cash-metrics.js` исторически показывал в toast `message` первым (человекочитаемый текст, не машинный код) — `request()` переведён на `message`-first глобально; для промокодов это тоже улучшение, не регресс |
| **20.3.0** | **Четвёртый срез Frontend Foundation.** Ручная отправка недельной/месячной сводки по сети (`POST /reports/send-digest`, `19-reports.js`) — на typed-клиент, самый маленький срез на сегодня (один эндпоинт) |
| **20.4.0** | **Пятый срез Frontend Foundation.** Алерты (`GET /alerts`, `POST /alerts/:id/status`, `17-alerts.js`) — на typed-клиент: список, карточка алерта, смена статуса жизненного цикла |
| **20.5.0** | **Шестой срез Frontend Foundation.** Задачи (`GET /tasks`, `GET /tasks/:id`, `POST /tasks/:id/status`, `POST /tasks/:id/comments`, `15-tasks.js`) — на typed-клиент: список, карточка, смена статуса, комментарии |
| **20.6.0** | **Седьмой срез Frontend Foundation.** Command Center (`GET /command-center`, `GET /employees`, `POST /tasks`, `14-command-center.js`) — на typed-клиент. `GET /command-center` собирается из нетипизированной внутренней функции (`buildSupervisorDashboard`) — контракт описывает то, что реально читает фронтенд, не весь внутренний ответ; аннотация на `problems` поймала настоящую type-narrowing ошибку (строка вместо литерального union) прямо на этапе типизации |
| **20.7.0** | **Frontend Foundation завершена — все оставшиеся 15 файлов одним заходом.** По прямому запросу владельца продукта («доделай всё разом») — вместо очередного узкого среза сразу все 13 оставшихся `frontend/js/*.js` плюс два файла (`16-store-profile.js`, `18-employee-profile.js`), чьи бэкенд-типы и клиентские функции уже были готовы, но не подключены. ~60 новых функций в `frontend/src/api-client.ts`, общий контракт (`shared/api-types.ts`) расширен по многоуровневой схеме — точные типы для CRUD-форм, «что реально читает фронтенд»-типы с `[key: string]: unknown` для больших нетипизированных дашбордов (`buildSupervisorDashboard`, `getMonthSummaryTable`, `calculateAllBFQ`, `simulateScheduleMoves`, `getLiveNetworkMap`). Два новых хелпера в клиенте: `requestUpload` (multipart, аватар) и `requestBlob` (CSV-экспорт, не JSON). При сверке поведения до/после на каждом вызове найдено и исправлено несколько мест, где миграция тихо огрубляла текст ошибки в toast (падал общий «Ошибка» вместо прежнего специфичного сообщения) — исправлено на месте, не как отдельный пункт. Найден и **не тронут** мёртвый роут: кнопка «Скопировать неделю» в расписании (`06c-support-tickets.js`) шлёт `POST /schedules/copy-week`, которого нет ни в одном `routes-*.ts` — always-404 с самого начала; оставлено как есть с комментарием в коде, заводить новый роут — отдельное продуктовое решение, не механическая миграция. 51 тест в `frontend/tests/api-client.test.ts` (было 17) — репрезентативный набор на все различные URL/метод-паттерны, не по тесту на каждую из ~60 функций. Из 118 исходных `fetch()`-вызовов эпохи 20 остались ровно 2 сознательно нетронутых: `bootApp()` в `08-access-supervisor.js` (инспектирует `res.status === 404` напрямую, не ложится в общий клиент) и упомянутая мёртвая кнопка |
| **20.8.0** | **Full Data Access Layer — весь backend на репозиториях.** Первый пункт нового roadmap владельца продукта (20.8-20.20, Core hardening) — «главный технический релиз» по его же оценке. Весь прямой SQL, ещё остававшийся вне routes-stores.ts/employees/sales/schedules/cash/tasks/shifts (уже перенесённых ранее), перенесён в `src/repositories/*`: алерты (`services/alerts.ts`, `routes-live-alerts.ts`, `routes-command-center.ts`), планы вместе с гоночно-защищённой материализацией (`services/plans.ts`, см. 19.24.0), кабинет супервайзера (`supervisor-analytics.ts`), живая карта сети (`live-map.ts`), инсайты смены (`insights.ts`), `routes-me.ts` (identity/bind), прогноз (`forecast.ts`), генерация картинок отчётов (`report-image.ts`), статистика/дашборд/BI-экспорт, профили точки/сотрудника, объявления/каналы, поддержка (тикеты), BFQ, геймификация (XP/бейджи), кастомные метрики, промокоды, what-if симуляция, темп смены, сетевые сводки, каталог метрик, точный heatmap, автоанонс релиза, AI-аудит, оба cron-модуля (отчёты в чат + алерты 14:00/16:00) — 18 новых файлов-репозиториев, ~45 изменённых route/service/cron-файлов. Правило `route → service → repository → PostgreSQL` теперь без исключений по всему backend, `npm run check:no-direct-sql` растянут на 54 файла. Полным прогоном тестов ещё до пуша поймана и исправлена одна настоящая регрессия: у нового `repositories/alerts.ts::insertOnce()` `alert_date` стал обязательным параметром и потерял старый дефолт «сегодня» — вернули, тест на дедупликацию алертов зафиксировал это на будущее. Внешнее поведение не изменилось ни для одного эндпоинта — рефактор архитектуры, не новая функциональность; проверено полным прогоном (277 тестов, зелёные) и живыми проверками на dev-сервере по каждому переносимому куску |
| **20.9.0** | **Authentication Boundary — Telegram изолирован в adapter.** Второй пункт roadmap'а 20.8-20.20; владелец продукта сам сузил исходный пункт «Identity Abstraction» до именно этой границы (без схемы БД, без `identities`-таблицы — до появления второго реального provider). Новый `src/auth/`: `identity.ts` (provider-agnostic `Identity` — `{provider, providerId}`), `providers/telegram.ts` (вся Telegram-специфика — initData/HMAC/заголовки, раньше жившая прямо в `middleware-auth.ts`), `principal.ts` (`Identity → Principal/AuthUser`, диспетчеризация по `provider`). `middleware-auth.ts` стал тонкой Fastify-обвязкой поверх этой границы и ре-экспортирует все прежние имена (`Role`, `AuthUser`, `ROLE_LEVEL`, `canAssignRole`, `loadUser`) — ни один из ~30 роут-файлов не пришлось трогать. Один баг пойман ещё до пуша: первая версия положила `identity` прямо в `Principal` — и оно тут же протекло в тело ответа `GET /access/status` (роут отдаёт `request.user` как есть); убрали `identity` из `Principal`-типа, оставили параметром только у `loadUser()` — API отдаёт байт-в-байт то же, что и раньше. Новые тесты (`tests/unit/auth-boundary.test.ts`) фиксируют саму границу: авторизационные примитивы физически не принимают `Identity` в сигнатуре, `loadUser()` с гипотетическим будущим provider отдаёт безопасный guest, не пытаясь трактовать его как Telegram |
| **20.10.0** | **Audit & Observability 2.0.** Третий пункт roadmap'а 20.8-20.20. `audit_log` пополнился forensic-полями `actor_role` (снимок роли актора НА МОМЕНТ действия, не текущая — роль могут сменить позже) и `target_org_id` (сеть цели действия, структурно отдельно от сети актора — сегодня всегда совпадают, для будущих кросс-org сценариев уже разделено). Новый `src/utils/job-logger.ts` — структурные (pino JSON, тот же формат, что HTTP-логи Fastify) логи фоновых задач: старт/длительность/успех-неудача на каждое реальное cron-действие (материализация планов, микро/итог-картинки по каждой точке отдельно, напоминания о смене, недельная/месячная сводка, умные алерты). Побочный эффект той же правки: раньше ошибка отправки картинки одной точки могла оборвать цикл по остальным точкам в этом тике — теперь каждая точка обёрнута отдельно. Найден и подключён мёртвый код: `src/cron/alerts.ts::startAlertCron()` (контроль «0 продаж на смене» 14:00, «отставание точек» 16:00) был полностью написан, но никогда не вызывался из `index.ts` — обе фичи не срабатывали в проде ни разу; подключено по решению владельца продукта |
| **20.11.0** | **Repository Restructuring — backend layered structure + docs split.** Владелец продукта попросил перестроить структуру репозитория вне очереди, до Concurrency & Reliability. `backend/src/` (103 файла) переехал из плоской структуры в layered: `api/routes/` (сгруппированы по домену — `me/`, `org/`, `analytics/`, `ops/`, `profiles/`, плюс плоские CRUD-домены), `core/<domain>/` (бывший `services/`), `data/repositories/`+`data/db/` (бывший `repositories/`+`db/`, чистый rename), `platform/notifications/`, `integrations/telegram/`+`integrations/ai/` (бывший `bot/`+часть `services/`). Механический перенос: физическое перемещение + кодмод на все относительные импорты (307 специфаеров в 61 файле бэкенда + 50 в 41 тестовом файле), без единого изменения бизнес-логики — весь риск в 4 legacy grab-bag файлах (`routes-v8.ts`, `routes-v14.ts`, `routes-live-alerts.ts`, `routes-core.ts`), каждый годами сваливал несколько несвязанных доменов в одну кучу; разложены по настоящим границам (`org/access.ts` — новый, `org/branding.ts`+`analytics/heatmap.ts`, `analytics/live.ts`+`ops/alerts.ts`+`analytics/what-if.ts`, слияние `PATCH /employees/:id/role` в `org/employees.ts` и `GET /me/access` в `me/index.ts`, `GET /plans` в `api/routes/plans.ts`). `app.ts` стал тоньше — регистрация 31 route-модуля выделена в `api/routes/index.ts`. `npm run check:no-direct-sql` — 56 путей обновлены. `docs/` вынесен из 1143-строчного README: `ARCHITECTURE.md` (диаграмма + полное дерево), `API.md` (таблица эндпоинтов), `DEVELOPMENT.md` (запуск/тесты/конвенции), `SECURITY.md` (весь бывший §24), `docs/ADR/` — 6 ретроспективных architecture decision records (repository pattern, in-memory scope cache, CAS-конкурентность, TypeBox, Authentication Boundary, iife-бандл вместо полной Vite-миграции), `docs/archive/` (устаревший `INTEGRATION-V13.md`). README держит короткие ссылки на прежних номерах секций — ничего не переименовано, все 6 существовавших `§N`-ссылок в коде остались валидны. Frontend — начат настоящий feature-sliced переезд отдельным, более поздним заходом (не смешан с этой версией, см. `docs/ADR/006`). Внешнее поведение API не изменилось ни для одного эндпоинта — проверено полным прогоном (285 тестов), CI-чеком, live E2E по каждому расколотому/слитому файлу |
| **20.11.1** | Хотфикс сразу вслед за 20.11.0 — кодмод переписал только статические `import`/`from`-специфаеры, но не заметил runtime-строки путей (`new Worker(...)`, `path.join(__dirname, ...)`) в 3 файлах, переехавших на уровень глубже. Реально сломан был только `core/reports/svg-pool.ts` (`new Worker()` без фолбэка на `process.cwd()` — «Cannot find module» в проде сразу после деплоя, ломало рендер PNG-отчётов, не только announce-путь, на котором ошибка сначала всплыла в логах); `data/db/migrate.ts` и `core/reports/image.ts` не были реально сломаны (их первый кандидат — `process.cwd()`-путь — уже резолвился верно), но их мёртвый `__dirname`-фолбэк тоже поправлен. Проверено на реальном скомпилированном `dist/` и живым E2E через настоящий worker-поток |
| **20.12.0** | **Frontend rewrite — первый реальный срез.** `frontend/src/app/router.ts` — типизированный реестр страниц вместо роста if-цепочки в `02-nav-utils.js`; сознательно НЕ URL/hash-based (Telegram Mini App без серверных роутов, свой `start_param` вместо путей). `frontend/src/app/state.ts` — типизированный доступ к сессии поверх легаси-глобала `me`, не новое хранилище (тот же источник правды, что уже читает весь легаси-код — без риска разъехаться). `frontend/src/features/send-network-digest/` — первая кнопка на `addEventListener` вместо `onclick=`. `frontend/src/pages/reports/` заменяет `frontend/js/19-reports.js` файл-в-файл (тот же `window.loadReportsPage`, легаси `switchPage()`/`loadPage()` не тронуты — выбран как пилот: самая маленькая легаси-страница, 71 строка, без собственного состояния). Каждая мигрированная страница — свой отдельный iife-бандл (Rollup не даёт несколько точек входа в одной iife/umd-сборке) — второй `vite build` (`vite.pages.config.ts`, `emptyOutDir: false`) добавлен в `build:frontend`. Реального браузера в среде выполнения не было — основная поведенческая проверка на этот раз jsdom-тест с реальным симулированным кликом (`frontend/tests/reports-page.test.ts`), плюс живая проверка на dev-сервере, что бандл действительно отдаётся и подключён |
| **20.13.0** | **Security hardening — единое правило «продажи за другого», закрыты 2 XSS-дыры.** По внешнему security-аудиту (v20.11.1) — проверен по каждому пункту на реальном коде, не принят на веру: часть находок подтвердилась, часть оказалась уже известными осознанными компромиссами (публичные аватары под rate-limit), одна («self-check» в `/sync/batch`) не подтвердилась — механизм и так был корректен. Подтверждённая несогласованность: `POST /sales` уже запрещал `senior` вносить продажу за ДРУГОГО сотрудника (только `manager`/`admin`), а параллельные пути того же действия — `POST /sales/quick` и `POST /sync/batch` — разрешали через общий `isManager()` (который включает `senior`); один и тот же вопрос имел два разных ответа в зависимости от точки входа. Новая `src/auth/guards.ts::canWriteSalesForOthers()` — единая точка правды, применена во всех трёх местах. Закрыты 2 подтверждённых XSS-пробела без экранирования (`07-add-sale.js` — имя сотрудника в модалке правки продажи, `11-v13.js` — текст и пункты AI-инсайта смены), оба теперь через `esc()`. Внешнее поведение не изменилось ни для кого, кроме `senior` в этих двух конкретных путях — проверено полным прогоном (291 тест, было 285) и живым E2E по всем трём путям записи продажи за другого |
| **20.14.0** | **Жесты по apple-design skill — перехватываемые свайпы, без CSS-transition.** Аудит по только что установленному skill `emilkowalski/skills` (`apple-design`, WWDC «Designing Fluid Interfaces») нашёл реальный баг в свайп-закрытии модалки продажи (`07-add-sale.js`) и свайпе между панелями «Мой день»/«Сеть сегодня» (`02-nav-utils.js`): решение закрыть/переключить было чисто по дистанции, а сеттл ехал на CSS `transition` — повторный захват элемента, пока он ещё едет обратно, давал видимый скачок (новый drag стартовал от предполагаемой конечной точки, а не от текущего визуального положения). Новая `createSpring()` (`01-core.js`) — рукописная пружина на `requestAnimationFrame` без внешней библиотеки (~25 строк), с `damping`/`response` как в самом skill: `1.0` — критическое затухание без bounce (дефолт), `~0.86` — лёгкий bounce, но только когда жест уже нёс скорость; в любой момент можно `stop()` и продолжить от текущего значения — это и даёт перехватываемость. Решение «закрыть/переключить» теперь учитывает скорость жеста, не только дистанцию — быстрый флик на 40px закрывает модалку, хотя порог по дистанции остался 80px; живой прогон headless Chromium (Playwright) на локальном демо-окружении подтвердил и это, и что медленный под-пороговый драг по-прежнему корректно возвращается на место. В процессе того же прогона найден и закрыт ещё один баг: скорость жеста считалась без защиты от почти нулевого интервала между точками касания — это давало огромную мнимую скорость и пружину, улетающую далеко за экран; `gestureVelocity()` теперь игнорирует интервалы короче 5мс и клампит результат до 4000px/s. Заодно `prefers-reduced-motion` (`styles.css`) перестал быть «убить всё подряд» — короткие opacity/color-переходы (тост, подсветка строки) остаются мягкими 200ms-переходами вместо мгновенного рывка, зацикленные/смещающие анимации по-прежнему убираются полностью |
| **20.15.0** | **Concurrency & Reliability, часть 1 — idempotency-ключи на критичных операциях.** Первый пункт нового roadmap (20.15-20.17). Аудит всей write-поверхности API (кроме уже защищённых `POST /sales`/`POST /sync/batch`) нашёл настоящую гонку: `POST /access/requests/:id/approve` читал `status='pending'` до мутации, а сама mark-запись это не перепроверяла — два параллельных approve на одну заявку оба проходили ранний чек и оба создавали сотрудника + слали "✅ Доступ открыт" в Telegram. `markApproved()`/`markRejected()` стали CAS (`UPDATE ... WHERE status='pending'`), выполняются первым делом внутри `withTransaction()` — если создание/апрув сотрудника упадёт следом, вся транзакция откатится вместе с claim'ом, заявка вернётся в pending, а не застрянет в approved без реально созданного сотрудника. Живой прогон (`Promise.all()` на реальном тесте, не гипотетически) поймал и более тонкий баг в первой версии фикса: отдельный ранний "уже не pending" 404-чек до транзакции читал status не-CAS'ом, и второй из двух параллельных approve иногда успевал долететь до него уже после коммита первого — получал голый 404 вместо опрятного `deduped:true`; чек убран, единственный источник "уже обработано" теперь сам CAS. `POST /tasks` — раньше вообще без защиты (голый INSERT + уведомление в Telegram), тот же профиль риска, что был у продаж до эпохи 19: переиспользован уже существующий `claimIdempotencyKey()`/`offline_sync_log.client_id` (та же инфраструктура, что у `/sales`, домен-агностична, изменений схемы БД не потребовалось), фронтенд (`14-command-center.js`) генерирует `client_id` один раз на открытие формы, как и `submitSale()`. Остальная write-поверхность — уже естественно идемпотентна по конструкции (абсолютная перезапись, upsert на реальных UNIQUE, CAS вроде `PUT /promos/:id/use`); несколько находок с меньшим риском (`POST /bfq/questionnaire`, `POST /promos`, `POST /employees`) осознанно отложены до сверки с продуктом по ожидаемой семантике. Полный прогон (294 теста, было 291) |
| **20.16.0** | **Adversarial race-condition suite — стенд на N конкурентных запросов, 2 закрытых пробела.** Второй пункт roadmap (20.15-20.17). Новый `tests/helpers/concurrency.ts` — раньше каждый race-тест (`shift-open-race`, `sales-double-submit-race`, `plans-materialize-race`, `access-approve-reject`) заново писал свой `Promise.all([...])` вручную на 2 запроса; `fireConcurrent()`/`countStatus()` переиспользуемы и на N — с двумя иногда реальная гонка не воспроизводится стабильно, пять параллельных попыток надёжнее вскрывают неатомарное read-then-write окно, если оно есть. `POST /me/bind` — код сам документировал комментарием «узкое окно гонки» между SELECT-чеком карточки и UPDATE-привязкой, полагаясь на `employees.telegram_id` UNIQUE (миграция 0002) как страховку, но это утверждение ни разу не проверялось настоящим конкурентным запросом; новый тест — 5 параллельных bind одним `telegram_id` на 5 разных карточек — подтверждено, ровно один побеждает (200), четыре получают чистый `409 already_bound` (не голую 500-ку от constraint), в БД ровно одна карточка с этим `telegram_id`. `PUT /promos/:id/use` — в 20.15.0 классифицирован как «уже естественно идемпотентен» по одному только чтению кода (CAS `UPDATE ... WHERE is_used=false`), тоже без реальной проверки; новый тест — 5 параллельных попыток забрать один промокод — подтверждено, ровно один получает `{ok:true}`, четыре — 404, `is_used` выставляется ровно один раз. Остальные UNIQUE-упорядоченные write-пути (`schedules`, `store_cash`, `bfq_manual`) — абсолютная перезапись по семантике «последняя запись побеждает», не «первый забирает» — гонка там не создаёт риска задвоения, отдельного теста не потребовала. 296 тестов (было 294) |
| **20.17.0** | **Recovery — graceful shutdown (закрывает Concurrency & Reliability, 20.15-20.17).** Третий и последний пункт roadmap. Из трёх заявленных тем реально нужна была только одна: бэкапы — платформенная настройка Railway, не код репозитория; откат миграций — уже задокументирован как политика «только вперёд» (`docs/RUNBOOK.md`, 20.15.0), сознательное архитектурное решение, не пробел. Graceful shutdown — единственное, что реально отсутствовало: до этой версии `SIGTERM` (Railway шлёт старому контейнеру на каждом деплое, когда новый уже прошёл healthcheck) убивал процесс дефолтным поведением Node — мгновенно, без единого шанса доотдать уже начатые HTTP-ответы. `src/index.ts`: на `SIGTERM`/`SIGINT` сначала перестаём принимать новую работу (снимаем крон-таймеры — `startReportCron`/`startDigestCron`/`startAlertCron` теперь возвращают handle вместо того, чтобы его терять, — `app.close()` дожидается уже начатых HTTP-ответов, `bot.stop()` только если polling реально шёл), потом закрываем `pg.Pool` — порядок важен, иначе ещё летящий запрос лишится соединения с БД посреди обработки. Жёсткий таймаут 8с — Railway не документирует публично точный grace period до SIGKILL. Живая проверка обычным `kill`/`taskkill` между процессами уперлась в платформенное ограничение Windows-песочницы этой сессии (Node на Windows не доставляет `SIGTERM`/`SIGINT` в `process.on()` при межпроцессной отправке — ограничение самого Node, не бага в коде; прод — Linux/Railway, там штатно по POSIX); проверено вместо этого `process.emit('SIGTERM')` внутри того же процесса — обходит платформенную прослойку, проверяет саму логику: реальный запуск, реальный `app.close()`/`pool.end()`/`bot.isRunning()`-гейт, порт освобождается чисто, процесс выходит без зависания. Закрывает эпоху Concurrency & Reliability целиком, ровно по плану, без версий сверху |
| **20.18.0** | **Explain — почему точка недоработала (первый шаг Intelligence Layer).** Platform Layer (20.19-20.22) не начат — по сверке с владельцем продукта за ним не стоит ни одной реальной жалобы или запроса, только гипотеза без триггера, а Mini App работает стабильно и спрос растёт; отложен в backlog без даты вместо постройки на голом предположении. Вместо него — первый кусок Intelligence Layer / Operations Copilot, раньше запланированного отдельной эпохой 21, но продолженного в нумерации эпохи 20 по решению владельца продукта. Explain — первый шаг конвейера Explain → Predict → Recommend → Act → Learn: до этой версии `anomaly_vs_forecast` (19.2.0) сообщал только ЧТО точка провалилась (z-score против прогноза), не ПОЧЕМУ. Три детерминированных фактора, без LLM и без нового алерта — считаются только для просевших дней и дописываются в уже существующий алерт: `understaffing` (укомплектованность по графику вчера против типичной для этого дня недели по этой точке), `shift_gap` (по графику N человек, смену реально открыли меньше), `network_wide` (в тот же день просела заметная доля сети — вероятно, не локальная причина). Новые repo-функции тем же батч-по-точкам паттерном, что уже был у `sales.ts` (`findHistoricalTotals`/`findTotalsForDate`): `schedules.ts::findHeadcountHistory`/`findHeadcountForDate`, `shifts.ts::findSessionCountForDate` — z каждой точки за вчера теперь считается в двух проходах (первый даёт z всех точек разом, нужен для `network_wide`; второй создаёт алерты только для превысивших порог). `payload.possible_causes` хранит структуру для будущей аналитики, но фронтенд (`17-alerts.js`) рендерит только `body` — причины дублируются туда читаемым текстом. Сознательно не в эту версию: BFQ-корреляция и активность промо — более слабый/косвенный сигнал, добавятся отдельно, если первые три фактора окажутся полезными на практике. 301 тест (было 296) — 3 юнит-теста на чистую функцию решения `network_wide` (без БД) + 2 через реальный Postgres (недоукомплектованный график/разрыв явки с настоящими `schedules`/`shift_sessions`) |
| **20.18.1** | **Хотфикс — реальный race-баг в `POST /me/bind`, пойман CI на пуше 20.18.0, не связан с Explain.** `tests/isolation/me-bind.test.ts`'s adversarial-тест (5 параллельных bind одним `telegram_id` на 5 разных карточек, из 20.16.0) поймал в CI: `countStatus(results, [200])` вернул 2 вместо 1. Причина — `clearTelegramId()`+`bindTelegram()` шли двумя отдельными автокоммитящимися запросами вместо одной транзакции; `employees.telegram_id UNIQUE` (0002) защищает от одновременной записи в ОДНУ и ту же строку, но не от следующего сценария: запрос A успешно биндит карточку и уже отдал клиенту 200, затем запрос B (для той же гонки, другая целевая карточка) выполняет свой `clearTelegramId()` — тот безусловно снимает `telegram_id` с ЛЮБОЙ карточки, где он сейчас стоит, включая ту, что только что выиграла A, — и тут же ставит его на свою карточку. Итог: более одного 200 за прогон, хотя финальное состояние БД в итоге сходится к одному владельцу. Новая `employees.ts::claimTelegramId()` — то же самое одним атомарным `WITH ... UPDATE` вместо двух раздельных запросов, окно между «снять» и «поставить» больше не существует. Не удалось надёжно воспроизвести локально (гонка редкая, зависит от точного тайминга) — проверено обратным экспериментом: 15 прогонов race-теста на копии CI-окружения (тот же `postgres:18`, чистая БД) со старым кодом — 0 из 15 упало, со фиксом — тоже 0 из 15 (ожидаемо, гонка вероятностная, не детерминированная; сама логическая дыра в коде подтверждена чтением, не только эмпирикой). CI зелёный, деплой 20.18.0+20.18.1 одним пушем |
| **20.19.0** | **Predict — прогноз конца дня по темпу, второй шаг Intelligence Layer.** Перед кодом найден реальный пробел: у `store_hour_profile` было ДВЕ параллельные реализации пересчёта — живой роут `POST /admin/rebuild-hour-profiles` звал фейковую (`insights.ts` — захардкоженная кривая 10-21ч с ручными пиками обед/вечер, полностью игнорирует реальные данные), а настоящая (`heatmap.ts` — честная агрегация `sales_events`, которые пишутся на каждый `POST /sales` с v14) была вообще ничем не вызвана. Тот же паттерн у `/heatmap/:storeId` — фронтенд им не пользуется (`getHeatmapPrecise` зовёт уже честный `/heatmap/precise/:storeId`), мёртвый роут с фейковой `salesHeatmap()` удалён, не оставлен рядом с рабочим. Переключено на реальную реализацию + автоматический ежедневный пересчёт (`cron/reports.ts`, 05:00 МСК, было — только вручную кнопкой). Сам Predict — та же математика, что уже была в `expectedPct` (`buildShiftInsight`, доля типичного дня по `store_hour_profile`, пройденная к этому часу), но доведённая до прямого прогноза: `projectEndOfDay()` (чистая функция, без похода в БД) экстраполирует факт-сейчас на весь день, переиспользуется на двух уровнях без дублирования — сотрудник (`GET /me/insight`, новые поля `plan_total`/`projected_total`/`on_track`, отрендерено в live-смене) и точка (новый `smart_alerts`-триггер `plan_miss_projected`, warn, окно 15%-85% пройденного дня — не сигналим ни на шуме единичной продажи, ни слишком поздно, когда действовать уже некогда). `MIN_FRACTION_DONE=0.15`/`ON_TRACK_TOLERANCE=0.9` — договорные константы, не статистически выведенные. Живая проверка: реальный профиль из 14 дней `sales_events` с неравномерной формой (пик 10-12ч) дал корректную проекцию (2 факта к середине дня против типичных 64% → прогноз ~3 из плана 18, `on_track:false`), совпало с ручным расчётом. `plan_miss_projected` не покрыт integration-тестом через `runSmartAlertsTick()` — та же граница, что у остальных трёх триггеров этого файла (ни один не тестируется на этом уровне из-за завязки на реальный час МСК); сама математика — 7 юнит-тестов отдельно. 308 тестов (было 301) |
| **20.20.0** | **Recommend — подсказка «что сделать» в задаче из алерта, третий шаг Intelligence Layer.** Act уже существовал (18.4, создание задачи прямо из проблемы Command Center) — Recommend только подставляет ЧТО написать в задачу вместо пустого поля. Детерминированное правило cause→действие (`core/alerts/recommend.ts::suggestAction`), без LLM — та же дисциплина, что у Explain/Predict; менеджер всё равно видит подсказку в поле «Что сделать» перед созданием, редактирует или игнорирует, ничего не выполняется само. `understaffing` → «проверить график на ближайшие дни, рассмотреть усиление»; `shift_gap` → «связаться со сменой, уточнить причину»; `plan_miss_projected` → «скорректировать оставшуюся часть дня» с реальными числами прогноза/плана из payload. `network_wide` — сознательно БЕЗ совета: весь смысл этой причины «вероятно не локальная», предлагать точке действие было бы неверным сигналом; алерты до Explain/Predict (`low_mnp_ratio`/`no_sales_hour`/`cash_gap`) — тоже без подсказки, не выдумываем совет для типов вне подтверждённого скоупа. Единственная точка подключения — `GET /command-center`, action `type=create_task`: `message` теперь `suggestAction(alert.alert_type, alert.payload) ?? alert.title` (прежнее поведение как фолбэк); `GET /alerts` (отдельная страница) не тронут — там пока нет кнопки создания задачи. 317 тестов (было 308) — 7 юнит-тестов на `suggestAction` (все причины + приоритет `understaffing` над `shift_gap` при обеих сразу + осознанное отсутствие совета у `network_wide`/старых типов) и 2 через реальный Postgres + HTTP |
| **20.21.0** | **Дилер — владелец сектора, новый уровень иерархии.** Не из конвейера Intelligence Layer — отдельный запрос по иерархии сети: над Сектором не было записи о том, кто им реально владеет. Новая цепочка: Дилер (ООО/ИП) → Сектор → Сеть (`organizations`) → Точка → Сотрудник. Только ownership-запись для отчётности/договоров — без своего входа, роли или дашборда, тем же лёгким приёмом, что уже был у самого сектора (заводится по имени из формы, не отдельный CRUD-экран). `dealers (id, name UNIQUE)` + `sectors.dealer_id` — один дилер может владеть несколькими секторами разом (FK не уникален), у сектора — ровно один дилер или ни одного. Заводится в той же форме редактирования сети (`13-v14.js`), где уже было поле «Сектор» — новое поле «Дилер», upsert по имени (повторный ввод того же названия не плодит дубликат); привязывается к СЕКТОРУ этой сети, не к самой сети — если несколько сетей в одном секторе, у них общий дилер. Пустой ввод не трогает уже назначенного дилера — тот же приём, что у остальных опциональных полей этой формы (`chat_id` и т.п.), уже принятое ограничение, не новое. 322 теста (было 317) — 5 новых через реальный Postgres + HTTP; миграция 0015 и полный прогон дополнительно проверены на чистой БД с нуля, той же дисциплиной, что после 20.18.1 |
| **20.22.0** | **Каскадный переключатель Дилер → Сектор → Сеть у admin.** По запросу владельца продукта сразу вслед за 20.21.0 — раньше у admin был один плоский `<select>` всех сетей, с ростом числа дилеров/секторов в нём тяжело искать нужную. Три select — Дилер → Сектор → Сеть, каждый сужает следующий, выбор всё равно сходится к ОДНОЙ сети (тот же `adminViewOrgId`, что раньше) — никакого нового агрегированного вида по дилеру/сектору целиком, это осталось за рамками, как и решили в 20.21.0. Чисто фронтенд (`06-team-bfq.js`) — `GET /orgs` уже отдаёт `dealer_name`/`sector_id` на каждой сети с прошлой версии, бэкенд не тронут; группировка dealer→sector→orgs строится на лету из уже загруженного списка. Сети без назначенного дилера — бакет «Без дилера». Живой браузер с настоящей Telegram WebApp identity в этой среде недоступен — вместо кликового E2E тот же jsdom-приём, что уже был у пилотной страницы (`reports-page.test.ts`), но впервые применён к classic-script напрямую: реальный файл грузится indirect eval'ом в global scope (та же семантика, что `<script>` без `type="module"` в `index.html`), не переписанная копия логики. 5 новых фронтенд-тестов (`frontend/tests`, 55 → 60) — бэкенд не тронут, остался на 322. *[Правка задним числом: версия ошибочно указывала "327 тестов (было 322)" в бэкенд-счётчике — на деле выросли фронтенд-тесты, не бэкенд.]* |
| **20.23.0** | **Learn — сработала ли рекомендация, закрывает конвейер Intelligence Layer.** Explain → Predict → Recommend → Act уже были — Learn единственный шаг, отвечающий не "что произошло/предсказано/сделать", а "помогло ли на самом деле". Детерминированно, без LLM. Два независимых измерения: `plan_miss_projected` (Predict) — однодневное, реальный итог дня против `projectedTotal`, зафиксированного В МОМЕНТ алерта (тот самый пессимистичный read, что видел менеджер) → `recovered`/`still_missed`; `anomaly_vs_forecast` (Explain), только "тихие" дни (`payload.z < 0`) — рецидив: была ли у ТОЙ ЖЕ точки ещё одна просадка в следующие 7 дней → `recovered`/`recurred`, оцениваются только алерты старше окна. Оба фиксируют `had_task` — была ли задача не просто создана, а ДОВЕДЕНА до конца (`status='done'`), отдельно отвечая на вопрос "помогает ли вообще Act", не только "стало ли лучше само по себе". Тот же дневной тик, что Explain (`core/alerts/service.ts`, hour===11), сразу после `checkAnomalyVsForecast()`. Новый `GET /alerts/effectiveness` (admin-only, как `/orgs`) — агрегированная сводка across org'ов; видна в «Отчётах» новой секцией "Эффективность рекомендаций" (admin-only). Пойман и исправлен реальный баг ещё до пуша: `payload`-независимый `id` из репозитория — bigint, pg возвращает строкой, а `Set` с id из отдельного запроса — числами; `had_task` всегда врал в `false` из-за `number !== string`, пока id не нормализован сразу после чтения — тот же класс бага, что уже ловили в `newbieCohorts` (19.x), там на датах. Задним числом поймана и поправлена неточность в changelog 20.22.0 (см. выше). 328 тестов (было 322) — 6 новых через реальный Postgres, плюс 2 новых фронтенд-теста (`frontend/tests`, 60 → 62) |
| **20.24.0** | **Frontend rewrite продолжен — второй мигрированный экран (промокоды РТК).** Возврат к frontend-переезду после закрытия Intelligence Layer (Explain → Learn, 20.18-20.23) — план владельца продукта: сначала цикл, потом фронт. Второй мигрированный файл после пилота (`19-reports.js`, 20.12.0): `frontend/js/12-promos.js` → `frontend/src/features/promos/`, файл-в-файл, то же самое поведение. Выбран по той же логике, что и пилот: из 4 самых маленьких оставшихся легаси-файлов (`18-employee-profile.js`, `12-promos.js`, `17-alerts.js`, `16-store-profile.js`) — ни один другой `frontend/js/*.js` не зовёт функции `promos.js` напрямую, нет локального state, страница даже не подключена к `switchPage()`/`loadPage()` nav-диспетчеру — самостоятельная модалка на двух статичных `onclick="openPromos()"` в `index.html`. Не `router.ts` "страница" — `app/router.ts` специально под `page-<name>` DOM/`switchPage()`, сюда не попадает (тот же `#modalTitle`/`#modalBody`/`#overlay`, что открывает `07-add-sale.js`). Форма как у `features/send-network-digest`, только крупнее: все 7 функций подвешены на `window.*`, потому что легаси-HTML зовёт их обратно через `onclick="..."` строки в innerHTML. `legacy-globals.d.ts` пополнен (`adminViewOrgId`, `orgQueryParam()`, `openModal()`). Второй per-page vite-конфиг (`vite.promos.config.ts`, тот же паттерн, что `vite.pages.config.ts`) — iife-формат Rollup требует ровно один global namespace на сборку, у каждой мигрированной страницы свой independent `vite build`, склеенные в `build:frontend`. Живой браузер с настоящей Telegram WebApp identity недоступен в этой среде — тот же jsdom-приём, что у `reports-page.test.ts`. 12 новых тестов (`frontend/tests`, 62 → 74) |
| **20.25.0** | **Frontend rewrite продолжен — третий мигрированный экран (алерты).** Третий мигрированный файл после пилота: `frontend/js/17-alerts.js` → `frontend/src/pages/alerts/`, файл-в-файл. В отличие от `promos.js` (самостоятельная модалка) — это настоящая `router.ts` "страница" той же формы, что `reports.js` (20.12.0): `page-alerts` DOM, `window.loadAlertsPage` бриджится в `registerPage`/`renderPage`, `02-nav-utils.js` вызывает её неизменно. `setAlertsFilter`/`openAlertDetail`/`changeAlertStatus` подвешены на `window.*` — легаси-HTML зовёт их обратно через `onclick="..."` строки; `renderAlertDetail` — НЕ на `window.*`, её никто не зовёт через onclick, только изнутри модуля, осталась приватным хелпером. `legacy-globals.d.ts` пополнен (`page`, `esc()`, `openTaskDetail()`). Четвёртый per-page vite-конфиг (`vite.alerts.config.ts`, тот же паттерн, что у reports/promos). 13 новых тестов (`frontend/tests`, 74 → 87) — фильтр-табы, рендер списка, critical/warn иконки, ошибка API не роняет страницу, деталь алерта (в т.ч. синтетическая заглушка для алерта, ушедшего из текущего фильтра), кнопки статусов кроме текущего, кнопка задачи только при `task_id`, смена статуса (тост + перерисовка + условная перезагрузка списка только если `page==='alerts'`), disabled-гейт от двойного клика, весь `window.*`-мост |
| **20.26.0** | **Frontend rewrite продолжен — четвёртый мигрированный экран (профиль сотрудника).** `frontend/js/18-employee-profile.js` → `frontend/src/pages/employee-profile/`, файл-в-файл. Настоящая `router.ts`-страница, но легаси nav-диспетчер (`02-nav-utils.js`) зовёт её нестандартным именем — напрямую `renderEmployeeProfile()`, без `load`-префикса — мост здесь назван под это конкретное имя, а не под общую конвенцию, ради которой пришлось бы лезть в `02-nav-utils.js`. `openEmployeeProfile(employeeId)` — точка входа, которую зовут ДРУГИЕ легаси-файлы (`06-team-bfq.js`, `14-command-center.js`) — на `window.*`, эти файлы не тронуты и не знают, что реализация переехала; она только ставит state и зовёт `switchPage()`, не рендерит напрямую — та же защита от рекурсии через `switchPage()`, что была в оригинале. `bfq`/`gamification`/`shifts.recent` из `EmployeeProfileResponse` нарочно слабо типизированы (`Record<string, unknown>`/`unknown[]`) — тот же осознанный компромисс для больших ad-hoc дашбордов, что и раньше (см. §21, 20.7.0). `legacy-globals.d.ts` пополнен (`commandCenterTone()`). Пятый per-page vite-конфиг (`vite.employee-profile.config.ts`). 6 новых тестов (`frontend/tests`, 87 → 93) |
| **20.27.0** | **Frontend rewrite продолжен — пятый мигрированный экран (профиль точки), найден и исправлен реальный баг.** Пятый и последний из четырёх изначально самых маленьких кандидатов: `frontend/js/16-store-profile.js` → `frontend/src/pages/store-profile/`, файл-в-файл. Тот же нестандартный мост, что у `employee-profile` (20.26.0). Пойман и исправлен реальный бэкенд-баг при типизации: `buildSupervisorDashboard()` уже считал `display_name` точки, но `GET /stores/:id/profile` строил свой `store`-объект вручную и забывал его пробросить — ответ всегда отдавал `undefined`, даже если кастомное название уже стояло, и подсказка «текущее название» в `prompt()` редактирования была ВСЕГДА пустой. Typed `StoreProfileResponse` не дал молча дописать поле во frontend-модуль, не заметив, что бэкенд его не отдаёт — исправлено в самом роуте (`src/api/routes/profiles/store.ts`) + новый regression-тест на бэкенде, не обход на фронте. `window.__storeProfileDisplayName` (легаси-костыль передачи state между `renderStoreProfile()` и `editStoreDisplayName()`) заменён на приватную module-scoped переменную — подтверждено grep'ом, что снаружи файла это поле никогда не читалось. `legacy-globals.d.ts` пополнен (`progressHTML()`, `metricLabel()`). Шестой per-page vite-конфиг. 13 новых тестов — 12 фронтенд (`frontend/tests`, 93 → 105) + 1 бэкенд (было 328, стало 329) |
| **20.28.0** | **Frontend rewrite продолжен — шестой мигрированный экран (задачи).** `frontend/js/15-tasks.js` → `frontend/src/pages/tasks/`, файл-в-файл. Той же формы, что `alerts.ts` (20.25.0) — почти зеркальная структура (фильтр-табы, список, деталь-модалка, смена статуса), плюс тред комментариев. `openTaskDetail` — первый случай зависимости МЕЖДУ мигрированными модулями: `pages/alerts/` и `pages/store-profile/` уже ссылались на неё через ambient-декларацию в `legacy-globals.d.ts`; теперь `pages/tasks/` даёт РЕАЛЬНУЮ реализацию под тем же именем на `window.*` — оба модуля продолжают работать без единой правки. Устаревшая ambient-декларация `function openTaskDetail(...)` удалена из `legacy-globals.d.ts` — она была текстом внутри `onclick="..."` строк в шаблонах alerts/store-profile, не настоящей TS-ссылкой, `tsc` прошёл чисто и без неё. Ни одной новой ambient-зависимости — все нужные глобалы уже были объявлены предыдущими миграциями. Седьмой per-page vite-конфиг (`vite.tasks.config.ts`). 15 новых тестов (`frontend/tests`, 105 → 120) |
| **20.29.0** | **Frontend rewrite — батч из 13 файлов одним заходом, весь `frontend/js/` мигрирован кроме двух фундаментальных.** После шести файлов по одному (20.24.0-20.28.0) — явная просьба владельца продукта закрыть оставшееся одним коммитом. Все 13 оставшихся экранов, кроме `01-core.js`/`02-nav-utils.js` (общий фундамент, на котором ambient-декларациями держится вся уже мигрированная кодовая база — перенос означал бы разворачивать направление зависимости для всего сразу, отдельный более осторожный заход): `09-cash-metrics.js` → `pages/cash-metrics/`, `14-command-center.js` → `pages/command-center/`, `06c-support-tickets.js` → `pages/support/`, `07-add-sale.js` → `features/add-sale/`, `06-team-bfq.js` → `pages/team/`, `03-home.js` → `pages/home/`, `06b-plans-bfq.js` → `pages/plans-bfq/`, `04-schedule.js` → `pages/schedule/`, `05-my-plan.js` → `pages/my-plan/`, `11-v13.js` → `pages/shift/`, `13-v14.js` → `pages/network-admin/`, `10-tutorial.js` → `features/tutorial/`, `08-access-supervisor.js` → `pages/access-supervisor/` — файл-в-файл, тот же метод, что и первые шесть. Часть ambient-глобалов расширена или переведена в писабельный `let`, потому что их реальный владелец сам переехал в typed-мир и теперь пишет в них по-настоящему: `me` (виджет «Мой день»/`bindMe()`, тип расширен полями `bound`/`id`/`telegram_id`), `adminViewOrgId` (`switchAdminOrg()`), `historyEmployeeFilter` (остался на `window.*` — пишется из ещё-легаси `onclick="historyEmployeeFilter=...` в 05-my-plan.js и в самом index.html). `openModal()`/`closeModal()`/`toggleMonthExtra()`/`svBarRowHTML()`/`svExtraToggleHTML()` получили РЕАЛЬНУЮ реализацию под именами, на которые уже ссылались более ранние миграции — та же ситуация, что `openTaskDetail` в 20.28.0. Найден и сознательно НЕ исправлен один визуальный огрех оригинала (обучение задаёт `task.textContent` строке с сырым `<svg>` вместо `innerHTML` — тег остался бы виден как текст): батч механический, не время чинить то, что не всплыло за месяцы прод-эксплуатации. 13 новых vite-конфигов, `index.html` перевешан на 13 бандлов (`pages/access-supervisor` — на своём `bootApp()` при загрузке модуля — поставлен последним в порядке подключения, безопаснее исходного). 13 старых `frontend/js/*.js` удалены. `tests/team-org-switcher.test.ts` (eval'ил `06-team-bfq.js` напрямую) переписан на импорт настоящего `src/pages/team/`. 185 новых фронтенд-тестов (`frontend/tests`, 120 → 305), бэкенд не тронут (329) |
| **20.30.0** | **Frontend rewrite закрыт полностью — `01-core.js`/`02-nav-utils.js`, последние два файла.** Все 21 экран `frontend/js/` теперь typed TypeScript; `01-core.js` → `frontend/src/app/core.ts` (сессия/роли/каталог метрик/gesture-физика), `02-nav-utils.js` → `frontend/src/app/nav.ts` (toast/тема/`switchPage()`+`loadPage()`-диспетчер/swipe-жест). Технически другой класс миграции: эти два файла — единственный источник ~9 разделяемых МУТИРУЕМЫХ переменных (`me`, `stores`, `employees`, `saleSelection`, `scheduleMonth`, `planMonth`, `adminViewOrgId`, `METRICS`, `page`), которые полтора десятка уже смигрированных модулей читают/пишут ГОЛЫМ идентификатором (`me = x`), унаследовано от classic-script эры, где top-level `let` одного `<script>` виден любому следующему. Решение — core.ts/nav.ts устанавливают всё как настоящие `window.*` свойства (никогда как локальный `let`, иначе он затенил бы глобал в пределах собственного iife-бандла); работает благодаря ECMA-262 Global Environment Record (неквалифицированный идентификатор резолвится и через Declarative Record, и через Object Record единым lookup) — голое присвоение из другого бандла работает даже в строгом режиме, поскольку он запрещает только СОЗДАНИЕ нового implicit-глобала, не запись в уже существующий. Гипотеза проверена трижды: синтетический Node-репро, полный `smoke-frontend.mjs` на реальном порядке подключения (23 файла), и 32 новых юнит-теста. Ни один из уже смигрированных 19 файлов не тронут. `frontend/js/` как директория больше не существует. 32 новых фронтенд-теста (`frontend/tests`, 305 → 337), бэкенд не тронут (329) |
| **20.32.0** | **Production Observability — `/healthz` + `/readyz` с настоящей readiness-семантикой, Prometheus-совместимый `/metrics`.** Владелец продукта предложил "20.31 Repository Consolidation" разминкой перед этим — проверка показала, что backend уже полностью на layered-структуре с 20.11.0, Full DAL закрыт с 20.8.0, `check:no-direct-sql` уже в CI, пункт вычеркнут без единой строчки кода. `/healthz` — жив ли процесс, без внешних зависимостей. `/readyz` — bootstrap-флаг (`platform/observability/readiness.ts`) + живой `SELECT 1`, намеренно БЕЗ bot polling в критерии готовности (интеграция, не условие способности Core API отвечать на HTTP). `/integrations/health` — конфигурация Telegram/AI (только env-переменные, без живых запросов). `/metrics` — Prometheus exposition через `prom-client` (официальный `@prometheus-io/client` pre-1.0, выбран более проверенный вариант) + `collectDefaultMetrics()`. Четыре группы: HTTP (route — ПАТТЕРН через `request.routeOptions.url`, не резолвнутый URL — иначе каждый id даёт свою time-серию навсегда; 404 → `not_found`, не сырой путь сканера), DB (агрегатно, без лейбла по домену — отложено до v2), Jobs (`runJob()` инструментирован рядом с существующим pino-логом), AI (`callGroq()` получил `operation`, тот же словарь, что `ai_audit.kind`). Структурные логи уже были готовы — Fastify's pino + `authPlugin`'s child-логгер с 20.9.0. 12 новых тестов (бэкенд, 329 → 341) + живой smoke реального процесса (не только `app.inject()`) — curl по всем пяти эндпоинтам, включая проверку, что сканер на случайный путь коллапсирует в `not_found` |
| **20.33.0** | **Domain Integrity — org-scoping инвариант закреплён в PostgreSQL, не только в TypeScript.** Владелец продукта поставил вопрос иначе, чем обычный security-аудит: какие бизнес-инварианты (Employee/Store принадлежат Organization, целостность владения Дилера, граф Сектор→Дилер) существуют только в TypeScript и ни разу не закреплены в БД. Аудит по всем 15 миграциям: из 7 `org_id`-колонок в схеме только 3 (`access_requests`/`regions`/`rtk_promocodes`) имели FK с baseline, ещё 4 (`employees`/`stores`/`announcements`/`channels`) — обычные text-колонки без FK, защищённые только приложением (`tenant.ts::assertStoreInOrg`/`assertEmployeeInOrg`); `channels.store_id` — та же история. Перед `ALTER TABLE` — read-only проверка прод-БД (Railway-прокси, только `SELECT`): 0 строк-сирот по всем пяти колонкам, `org_id` нигде не `NULL`. `0016_org_scoping_fk.sql` — 5 новых FK: `employees.org_id`/`stores.org_id`/`announcements.org_id`/`channels.org_id` → `organizations(id)`, `channels.store_id` → `stores(id)`. `NULL` по-прежнему разрешён (FK не трогает `NULL`, `COALESCE(org_id, 'default')` остаётся рабочей конвенцией, `'default'` — настоящая строка `organizations`). Дилер→Сектор (`sectors.dealer_id`, 0015) и Сеть→Сектор (`organizations.sector_id`, baseline) уже были закрыты FK раньше — граф плоский, циклов структурно не бывает, "Sector hierarchy must not create invalid graph" из спеки уже была выполнена без единой строчки новой миграции. 6 новых тестов (бэкенд, `tests/isolation/org-scoping-fk.test.ts`, 341 → 347 — исправлено задним числом с ошибочного "347 → 353" в этой же строке; реальный счёт был не пересчитан после написания текста) — по одному INSERT-отказу на каждую FK-колонку плюс два позитивных кейса (`NULL` org_id и валидный org_id по-прежнему проходят). Прогнано на одноразовом локальном Postgres (`initdb`+`pg_ctl` на отдельном порту) — полная миграция `0001`→`0016` с нуля накатывается чисто |
| **20.34.0** | **Product Analytics — сколько рекомендаций открывают, какие ложные, где задача реально меняет исход.** Explain/Predict/Recommend/Learn (20.18-20.23) уже отвечали "сработала ли рекомендация" (recovered/still_missed/recurred, с задачей/без); следующий вопрос владельца продукта — насколько это вообще помогает людям. Аудит перед кодом (Explore-агент): "action rate" (рекомендация → задача) уже полностью закрыт через `tasks.alert_id`+`had_task` в `/alerts/effectiveness` — искать было нечего; "opened rate" не существовал вообще, ни одной колонки времени просмотра; "false-positive rate" и "AI outcome attribution" физически вычислимы из уже накапливаемых данных (`payload.outcome`/`had_task`), просто не посчитаны и не показаны. `smart_alerts.first_opened_at` (`0017_alert_engagement.sql`) — один `timestamptz`, не join-таблица по образцу `announcement_reads`: там нужен список КТО прочитал (экран "кто не в курсе" для обязательных объявлений), здесь нужна только агрегатная доля "хоть кто-то открыл" — join-таблица была бы абстракцией под то, чего не просят. `POST /alerts/:id/read` (тот же уровень строгости, что `announcements/:id/read` — `requireManager`, без body) вызывается fire-and-forget из `pages/alerts/index.ts::openAlertDetail`, не блокирует рендер и не роняет его при сетевой ошибке. `GET /alerts/effectiveness` расширен пятью полями на каждый тип алерта: `open_rate`/`dismissed_rate` (из `first_opened_at`/`status='dismissed'`), `false_positive_rate` (recovered БЕЗ задачи ÷ все оценённые — ближайший доступный proxy "само прошло, вмешательство не требовалось"), `recovery_rate_with_task`/`recovery_rate_without_task` — числовые ставки поверх уже существовавших сырых счётчиков, реальный ответ на "где AI меняет результат", а не два сырых bucket'а, которые приходилось сравнивать в уме. Экран "Эффективность рекомендаций" получил строку "Открыли/Отклонили/Похоже на ложную тревогу" и явную дельту "Задача меняет исход: +N п.п." (null-рейты рендерятся как «—», не `NaN%`/`undefined`). "Какие типы прогнозов точнее" — не отдельная MAPE-метрика, а прямое сравнение `false_positive_rate` между двумя типами (ниже — точнее); лишняя абстракция не добавлена ради вопроса, на который уже есть прямой ответ. 4 новых бэкенд-теста (`tests/isolation/alert-engagement.test.ts`, 347 → 351) + 1 фронтенд-тест (337 → 338) — прогон существующего набора сразу поймал недостающий `markAlertRead`-стаб в моке `window.apiClient`, чинить пришлось до отчёта "готово", не после |
| **20.35.0** | **Не-Telegram вход (телефон + пароль) — backend-механизм.** Три сигнала подряд от владельца продукта: новый дилер просит подключить целый сектор, стажёр вообще не пользуется Telegram, а в стране бизнеса Telegram работает только через VPN — риск доступности продукта для ЛЮБОГО сотрудника, не "неудобно с ноутбука" из старой формулировки Web/Desktop. ADR-005 (20.9.0) специально оставил `Identity{provider, providerId} → Principal` как шов для будущего второго provider'а, сознательно отложив полную identity-модель до момента, когда он станет конкретным — этот момент настал. `auth/providers/phone.ts` — второй provider, зеркало `providers/telegram.ts`, но identity из cookie-сессии, не подписанного initData; `guards.ts` не изменился НИ В ОДНОЙ сигнатуре гварда — только `resolveUser()` пробует Telegram первым, phone-сессию вторым. Уточнено с владельцем в диалоге: Telegram остаётся полностью нетронутым (чисто аддитивно); идентификатор — телефон+пароль, не email (нет провайдера почты); регистрация — открытая самостоятельная, тот же flow "заявка → админ одобряет", что уже есть для Telegram; сброс пароля — через админа, не self-service SMS (нет SMS-провайдера, решение осознанное — тот же токен-примитив легко станет SMS-flow позже). Схема — `phone`/`password_hash` на `employees` рядом с `telegram_id`, не отдельная `identities`-таблица (ADR-005 отложил полную модель именно до этого момента, но одного provider'а недостаточно для порога полной схемы); `access_requests.telegram_id` стал nullable + `provider`/`phone`/`password_hash` + CHECK на форму строки. `employee_sessions`/`employee_password_resets` — непрозрачные токены (`crypto.randomBytes`, в БД только `sha256`), не JWT — сессий в проекте и так не было, Telegram identity и так перепроверяется заново на каждый запрос; instant revoke при деактивации ближе к этой философии, чем self-contained токен с denylist. Пароль — `crypto.scrypt` (встроенный модуль, не bcrypt/argon2). Новых обязательных секретов не потребовалось. `POST /auth/register` (rate-limit 5/мин) → `access_requests` с `provider=phone` → существующий `/access/requests/:id/approve` (небольшая ветка по provider) → `POST /auth/login` (rate-limit 10/мин, одинаковый 401 на "нет такого телефона" и "неверный пароль") → httpOnly cookie. `POST /auth/admin/reset-password/:id` (`requireManager`+org-scope) → одноразовая ссылка → `POST /auth/reset/:token`. Найдена и закрыта ДО пуша реальная утечка: `/auth/register` возвращал клиенту `access_requests` целиком (`RETURNING *`), включая `password_hash` — хешированный, но не должен покидать сервер без нужды; `GET /access/requests` чинился тем же способом (явный список колонок вместо `SELECT ar.*`). `@fastify/cookie` — единственная новая зависимость. Живой smoke на реальном процессе — register → approve через Telegram-админа → login → `GET /me` через cookie → тот же `GET /me` через Telegram-заголовок ТОГО ЖЕ админа параллельно, оба provider'а отвечают верно на одном инстансе; полный цикл admin-reset→consume тоже прогнан живьём. 11 новых тестов (`tests/isolation/phone-auth.test.ts`, 351 → 362). Фронтенд (экран логина, wiring, admin UI) — ещё не начат, отдельной версией |
| **20.36.0** | **Привязать телефон и пароль из своего профиля — для тех, кто уже работает через Telegram.** Другой сценарий, чем открытая регистрация 20.35.0: не "у меня нет аккаунта", а "я уже авторизован через Telegram, хочу ещё и телефон+пароль на случай, если Telegram недоступен (VPN, разряженный телефон и т.п.)". `POST /me/link-phone` (rate-limit 5/мин) — без approve через `access_requests`, identity уже подтверждена тем же запросом: тот же принцип, что `POST /me/bind` (19.11.0) — целевой `employee_id` для UPDATE всегда берётся из `request.user` (подтверждённый Telegram identity), никогда из тела запроса, иначе один сотрудник мог бы привязать телефон к чужой карточке по угаданному id. `hashPassword()` (тот же `crypto.scrypt`, что 20.35.0) → `employeesRepo.setPhoneAndPassword()`. UNIQUE на `employees.phone` (0018) ловит конфликт и превращает `23505` в понятный `409`, не сырую 500-ку. `GET /me` расширен полем `phone` — фронт теперь знает, привязан ли уже второй канал. Личный кабинет ("Мой план", `pages/my-plan`) получил новую секцию "Вход с компьютера" — по дизайну ничего нового не изобреталось: тот же `.section`/`.row`/`.row-icon`/`.row-chevron` паттерн, что уже использует `reports.ts` ("Отчёт по точке"); кнопка "Привязать телефон и пароль" открывает модалку (три поля, тот же `openModal()`/`#modalBody`-приём, что `openAddEmployee` в `team.ts`), либо — если уже привязан — инертная строка "Подключено: +7…". Живой smoke на реальном процессе: Telegram-авторизованный сотрудник линкует телефон → `GET /me` тем же Telegram-запросом сразу видит новый `phone` → логин через `POST /auth/login` по только что привязанному телефону работает, Telegram-канал у того же сотрудника не тронут. 11 новых тестов (4 бэкенд, `tests/isolation/me-link-phone.test.ts`, 362 → 366; 7 фронтенд — рендер обоих состояний секции, модалка, три ветки валидации, успех, ошибка API, 338 → 345) |
| **20.37.0** | **Экран входа без Telegram — теперь реально можно работать с компьютера.** Последний пробел из 20.35/20.36: backend-механизм и самопривязка из профиля уже работали, но зайти "с холодного старта" (нет Telegram, нет аккаунта вообще) было буквально негде — `bootApp()` снаружи Telegram содержал безусловный debug-пропуск "для отладки, но в проде лучше gate" ещё с 20.9.0, входящий просто попадал в пустую оболочку с `me=null`. Экран логина — НЕ новая `.page`, а третье состояние уже существующего `accessGate`-оверлея (тот же, что "Ожидайте подтверждения"/"В доступе отказано" для Telegram-заявок): до входа бессмысленно показывать header/bottom-nav, которые `.page`-система предполагает как данность. `showLoginGate('login'\|'register'\|'reset')` переиспользует `.gate-card`/`.field`/`.btn-main` пиксель-в-пиксель и даже сам org/claim-пикер (`loadGateOrgs`/`onGateClaimChange`) — тот оказался не Telegram-специфичным, просто раньше использовался только оттуда. `bootApp()` снаружи Telegram теперь: (1) `?reset=<token>` в URL — сразу экран смены пароля, минуя логин, сама ссылка уже секрет; (2) иначе пробует `GET /me` — валидная cookie-сессия (после `/auth/login` или `/me/link-phone` раньше) пускает как раньше; (3) и только если её нет — показывает логин вместо молчаливого входа вслепую. `submitPhoneLogin`/`submitPhoneRegister`/`submitPasswordReset` — тонкие обёртки над уже готовыми с 20.35.0 `/auth/login`, `/auth/register`, `/auth/reset/:token`, здесь не появилось ни одного нового backend-роута. Заодно найдена и починена мелкая, но реальная проблема: очередь заявок для admin (`GET /access/requests` рендер) показывала "TG null" для заявок `provider=phone` — теперь "Тел. +7…". Личный кабинет ("Мой план") получил кнопку "Выйти" — `POST /auth/logout` уже существовал с 20.35.0, но не был подключён нигде во фронтенде; показывается только вне Telegram. Живой smoke полного холодного старта на реальном процессе — реальная сборка (`npm run build:frontend`), подтверждено, что отданный браузеру бандл реально содержит новые функции (не только исходники): `GET /me` без единой идентичности → `bound:false` → register → approve тем же Telegram-admin flow, что и раньше → login → `GET /me` с новой cookie → `bound:true`, ровно тот путь, который проходит настоящий браузер. 15 новых тестов, все фронтенд (тесты для login-гейта живут в `access-supervisor-page.test.ts` — тот же файл, что уже владеет `accessGate`; backend не тронут ни строкой, 366 без изменений) — оба ветвления `bootApp()` (валидная сессия / её нет), `?reset=` маршрут, все три submit-функции (валидация + успех + ошибка сервера), фикс "TG null" → "Тел.", кнопка "Выйти", полный `window.*`-мост (345 → 360) |
| **20.38.0** | **Десктоп-раскладка — один брейкпоинт правит всё приложение разом.** Вход без Telegram (20.37.0) открыл сайт для настоящего браузера, но вся вёрстка написана mobile-first под Telegram Mini App и ни разу не адаптирована под широкий экран — во всём `styles.css` (2091 строка) существовал ровно один `@media` (`prefers-reduced-motion`), никаких брейкпоинтов по ширине; `.sheet` не имел `max-width` вообще — на мониторе растягивался на всю ширину. Не редизайн под "десктоп-плотность" (переосмысление контента каждого экрана по отдельности — другой масштаб работы), а тот же приём, что Telegram Web/WhatsApp Web/Twitter: то же мобильное приложение как единая раскладка, но центрированная колонка-панель (800px) на широком экране вместо растянутой на весь монитор. CSS не бандлится Vite и не разбит по страницам — один блок в конце `styles.css` правит всё приложение разом, ни одной страницы отдельно трогать не пришлось. `@media (min-width: 860px)` — единственный брейкпоинт, ниже не меняется ни пиксель, реальный Telegram Mini App (всегда уже порога) ничем не рискует. `.app-header`/`.sheet`/`.bottom-nav` получают `max-width:800px`, `.sheet` округляет все четыре угла и получает тень ("плавающая панель" на нейтральном фоне `body` вместо страницы во всю ширину), `.fab` пересчитывает `right` через `calc(50vw - 400px + …)` — от края фрейма, не от физического края окна. `.lk-actions-grid` ("Мой план", 8 кнопок) — с жёстких 2 колонок на 4. `.sum-sch-table` (сводный график, до 31 дня) честно не перестаёт скроллиться горизонтально даже на 800px — реальная широкая таблица, не искусственное ограничение; шрифт 11px→12px и колонка с именем 140px→160px читаемее, `overflow-x:auto` остаётся, нормальный паттерн и для настоящих десктопных таблиц с диапазоном дат. Модалка (`.overlay`/`.sheet-modal`) — на десктопе bottom-sheet не единственно верный паттерн, чисто мобильная условность; на 860px+ обычный центрированный диалог с фейдом. Логин-экран (`.gate-shell`, уже `max-width:420px` с 20.37.0) переделки не потребовал — пустое пространство вокруг получило мягкий radial-gradient фон теми же токенами, что уже использует `.gate-card .bind-glow` внутри карточки. Отдельный блок `@media (min-width:860px) and (hover:hover)` — не просто по ширине, `(hover:hover)` исключает touch-ноутбуки/планшеты в альбомной ориентации, которым залипающий hover только мешает; во всём файле раньше не было ни одного `:hover`, только `:active` (тактильная обратная связь для тача) — добавлены `.row`/`.nav-item`/`.lk-action`/`.fab`. Найдена и обойдена реальная specificity-ловушка: `showAccessGate()`/`showLoginGate()` при каждом показе выставляют `background` инлайн через `gate.style.cssText` — обычный CSS-класс/id это не перебивает, фон логин-экрана на десктопе пришлось поставить через `!important` с явным комментарием почему, а не правкой JS (план был "ровно один файл `styles.css`"). Верификация: чисто CSS, без единой строчки JS — `tsc`/vitest не покрывают визуальный результат (jsdom не считает layout/paint), автоматических тестов на это писать нечего; `npm run smoke:frontend` прогнан, живой сервер подтвердил, что новый блок реально попал в файл, отданный по HTTP. Честно: правки НЕ проверены визуально реальным браузером/скриншотом — инструмент недоступен в текущем окружении, то же ограничение, что уже отмечалось при миграции `reports.js` в 20.12.0 |
| **20.39.0** | **Desktop Shell — не центрированное мобильное приложение, а настоящая раскладка сайдбар+топбар+рабочая область.** Развёрнутая критика 20.38 от владельца продукта справедлива: центрирование в 800px-панель — мобильное приложение в рамке, не десктоп-интерфейс. После рассмотрения плана владелец дал 10 конкретных технических уточнений и утвердил их все, включая обязательное условие закрытия версии — реальную браузерную проверку, а не только `tsc`/vitest. Архитектура: один application state + один `switchPage()`/router + два presentation shell, не два разных приложения — `.desktop-shell` оборачивает новый `<aside class="sidebar">` + `.shell-main` (который сам оборачивает уже существующие `.app-header`+`.sheet`), `.bottom-nav`-элементы остаются сиблингами, просто скрываются на десктопе. `.nav-item`-синхронизация переиспользована буквально: `switchPage()` уже делал `document.querySelectorAll('.nav-item')` без scope (обслуживал `#bottomNavMain`+`#bottomNavSupervisor` и раньше) — третий контейнер (сайдбар) с тем же `class="nav-item" data-page="..." onclick="switchPage(...)"` контрактом подхватился без единой правки JS. Секции сайдбара гейтятся целиком (не по отдельному пункту), тем же приёмом, что уже гейтит `btnAccessRequests`/`btnSupervisor`/`btnMgrTutorial` в `bootApp()`: Обзор (всегда), Аналитика (`canViewAnalytics()`), Управление (`canManage()`), Супервайзер (`isSupervisor()\|\|canAdmin()`), Администрирование (`canAdmin()`) — пустой заголовок секции без пунктов не показывается. 3 брейкпоинта вместо одного из 20.38: `<860px` мобильный не меняется ни пикселем, `≥860px` Desktop Shell (CSS grid, sticky-сайдбар на всю высоту с независимым скроллом через `overflow-y:auto`, обычный document-flow скролл страницы — не отдельный scroll-контейнер), `≥1200px` более широкая рабочая колонка (800px→1120px, `--sidebar-width` 248px→264px через CSS-переменную, готово для будущего collapse без хардкода по всему файлу; четвёртый брейкпоинт 1600px+ из исходного предложения владельца сознательно не введён — раскладывать шире пока нечего). Swipe→grid на Главной ("Мой день"/"Сеть сегодня"): `.swipe-track` получает `transform`/`transition`/`width` через `!important` — `track.style.transform` пишется инлайн через JS, обычный каскад не перебивает; по требованию владельца добавлен ещё и ранний `return` в самом начале `touchstart`-обработчика `initSwipePanels()` по `window.innerWidth>=860`, чтобы жест не считал лишнего под уже статичной сеткой, а не полагался только на CSS-маскировку результата. `.fab` — `calc()`-от-центра-фрейма из 20.38 отменён (после сайдбара центр viewport ≠ центр рабочей области, сохранять ту же математику — усложнение без пользы) в пользу простого `right:32px;bottom:32px`. Модалка — новый tier-паттерн через CSS-переменную вместо дублирования правил: `.sheet-modal` шириной `min(calc(100vw-32px), var(--modal-max-width,520px))`, классы `.modal-sm`/`.modal-md`/`.modal-lg` переопределяют только переменную; `.modal-md` применён на `openAddStore()` (`team.ts`) как демонстрация паттерна, полный ретрофит всех модалок — намеренно не в этой версии. Реальная браузерная проверка (закрыт пробел, честно отмеченный недоступным в 20.38.0 и 20.12.0) — `playwright-core`+кэшированный Chromium оказались уже доступны в этом окружении. Матрица скриншотов 390/768/859/860/1024/1200/1440/1920px против живого dev-сервера (`npm run build:frontend`+`tsx src/index.ts`, реальная сборка): сайдбар/топбар появляются ровно на 860px и не раньше, мобильная раскладка на 859px не изменилась, grid вместо swipe на десктопе, FAB не наезжает на контент на 1920px, sticky-сайдбар подтверждён скроллом реального длинного списка "Инструменты" на Главной (сайдбар остаётся на месте `top:0`, контент уходит под него), regression-проверка `.nav-item`-синхронизации (клик по пункту сайдбара корректно подсвечивает тот же пункт во всех трёх nav-контейнерах, без залипшего `.active` на скрытых мобильных барах). Живая проверка поймала реальный регресс ДО релиза, не только подтвердила ожидаемое: `enterSupervisorShell()`/`exitSupervisorShell()` ставят инлайн `style.display` на `#bottomNavMain`/`#bottomNavSupervisor` независимо от ширины экрана (роль `supervisor`, либо admin входит в "кабинет супервайзера") — инлайн-стиль перебивал `.bottom-nav{display:none}` на десктопе без `!important`, то есть нижняя панель могла всплыть поверх Desktop Shell; закрыто тем же приёмом, что уже применён к `.swipe-track`. Явно не в этой версии (по roadmap владельца, 10 пунктов "не расширяй scope"): переделка контента страниц (Home dashboard-grid, таблица в Команде и т.п.) — 20.40; command palette/keyboard shortcuts/поиск — 20.41; плотные десктоп-таблицы и 1600px-tier — 20.42; пункт сайдбара для `network-admin` — неточная догадка без детализации, которая приходит вместе с 20.40. 1 существующий фронтенд-тест расширен (не добавлен) — стаб ролей в `access-supervisor-page.test.ts` получил `canViewAnalytics()`, которого не было в списке моков до нового вызова в `bootApp()` (360 без изменений в количестве), бэкенд не тронут ни строкой (366 без изменений) |
| **20.39.1** | **Хотфикс по подробному ревью владельца продукта сразу после 20.39.0.** Реальная находка — тот же класс бага, что `.bottom-nav`, во второй раз: `resize`-обработчик `initSwipePanels()` (`nav.ts`) на каждый resize вызывал `settle(current, false)`, которая безусловно выставляет `track.style.height` под высоту ТОЛЬКО текущей панели (нужно на mobile — «Мой день»/«Сеть сегодня» разной высоты по дням) — на desktop, где обе панели видны одновременно как grid, тот же inline `height` от последнего мобильного состояния мог обрезать более высокую вторую колонку при переходе mobile→desktop без перезагрузки страницы (реальный сценарий — ресайз окна через границу 860px). Резолвер теперь на `window.innerWidth>=860` сбрасывает `track.style.height`/`transform` в `''` вместо вызова `settle()` — воспроизведено и проверено живьём (Playwright): inline `height:122px`/`transform` от мобильного состояния после ресайза до 1200px корректно очищаются, computed `display:grid` подтверждён. Заодно: `.sheet` получил `padding-bottom:96px` на десктопе — `body.padding-bottom` обнулялся под скрытый `.bottom-nav` ещё в 20.39.0, но `.fab` остался fixed-элементом без своего зазора, последняя строка длинной страницы могла уехать под круглую кнопку (проверено скроллом до самого низа списка "Инструменты" на Главной, FAB больше не перекрывает "О приложении"). Убран дублирующий `.sidebar .nav-item:hover` (полностью покрывался уже существующим `.nav-item:hover`, cosmetic). Активный пункт сайдбара получил тонкий индикатор слева (`::before`, `var(--primary)`) поверх уже существующей заливки `.primary-soft`. Отдельно рассмотрено и намеренно НЕ тронуто по итогам того же ревью: `.lk-actions-grid`/`.sum-sch-table`/`.sum-sch-name` — не новая адаптация 20.39, а буквально перенесённые без изменений правила из уже задеплоенного 20.38 (просто попали в новый блок при замене структуры); откатывать их сейчас значило бы намеренно регрессировать уже живое в проде поведение ради формальной чистоты границы версий — оставлено как есть, явно задокументировано прямо в CSS-файле. Диапазон 860-1024px (беспокойство по ширине сайдбара) проверен живыми скриншотами на 860/900/960/1024 — shell сам по себе не ломается ни на одной из этих ширин; единственная видимая теснота — переполнение 6-элементного stat-grid в карточке "Сеть сегодня" на Главной, но это присутствует ОДИНАКОВО на всех проверенных ширинах вплоть до 1920px, то есть дело в контенте страницы (20.40 Page Adaptation), не в ширине сайдбара — sidebar оставлен как есть (248px/264px), брейкпоинт не поднят. 360 фронтенд-тестов без изменений, `tsc --noEmit`/`smoke:frontend` зелёные — без анонса в Telegram-чат (хотфикс/рефакторинг, не эпик) |
| **20.40.0** | **Design System + Home Dashboard — первая настоящая страница поверх Desktop Shell.** Владелец продукта прислал развёрнутый бриф на полную переделку desktop UX всего приложения (~30 страниц), явно потребовав аудит ДО кода и запретив начинать массовый рефакторинг без review. Процесс: сначала `docs/DESKTOP-UX-AUDIT.md` — полная инвентаризация каждой страницы (кто видит, какие данные, текущий UI-паттерн, покрытие desktop-CSS); ключевой находкой стало то, что почти каждая страница видна ВСЕМ ролям, а гейтится обычно не доступ к странице, а запись/редактирование внутри неё (`canManage()` прячет кнопки, не саму страницу) — это меняет то, как проектировать IA (нельзя просто гейтить целые секции сайдбара по роли, как это сделал 20.39 для малого набора пунктов). Затем `docs/DESKTOP-DESIGN.md` — предложенная IA, design-система, responsive-стратегия, staged roadmap по страницам. Owner review плана (через `EnterPlanMode`) дал 10+ конкретных технических уточнений, все приняты до кода. Согласованный скоуп реализации — ровно design-система + Home, ничего больше: `docs/DESKTOP-UX-AUDIT.md`+`docs/DESKTOP-DESIGN.md` → CSS-примитивы (`.page-header`/`.page-actions`/`.workspace-grid`/`.metric-grid` — все `auto-fit` по образцу уже responsive `.hm-grid`, не захардкоженные колонки как у `.stats-row`; `.row--dense` — модификатор существующего `.row`, не второй параллельный list-компонент "на вырост"; `.data-table`/`.detail-panel`/`.desktop-toolbar` сознательно не заведены — ни одной странице в этой версии не нужны) → разметка Home → JS-рендер → guard'ы `initSwipePanels()` в последнюю очередь. Team/Schedule/Command Center/Supervisor/Admin — staged пункты 20.41-20.44 в самом roadmap-документе, код этих страниц не тронут ни строкой. Три мелких, но реальных бага, найденных по пути (тот же класс проблем, что уже несколько раз всплывал в 20.35-20.39: остатки Telegram-only допущений и рассинхрон mobile/desktop меток), отдельным абзацем от новой архитектуры: (1) сайдбар называл `data-page="my"` "Мой план", хотя тот же самый пункт на мобильном всегда назывался "Профиль" — переименовано; (2) "Команда" была гейтед `canManage()` в сайдбаре, хотя `team.ts` гейтит только внутреннюю `.managerTools`-панель, не саму страницу, а на мобильном пункт постоянный в bottom-nav — обычный сотрудник терял вход в "Команда" именно на десктопе, перенесено в "Обзор" (всегда видимая секция); (3) `loadMyDay()` безусловно делал ранний return по `!tgUser()?.id` — стало неверным ещё с 20.35 (не-Telegram вход): `GET /me/day` и так уже гейтится по identity независимо от провайдера и graceful возвращает `{bound:false, message:'Привяжите аккаунт во вкладке Профиль'}` (сам текст сообщения уже предполагал имя "Профиль" — ещё одно подтверждение находки №1), старый гейт молча ломал "Мой день" для ЛЮБОГО desktop/phone-login сотрудника. Архитектурное решение для Home, исправляющее сам источник обоих багов 20.39.0/20.39.1: десктоп-дашборд — НЕ CSS-grid поверх мобильного swipe-DOM (`.swipe-track{display:grid!important}` трижды подряд конфликтовал с `initSwipePanels()`), а отдельная разметка (`.desktop-dashboard`, `#page-home`), populated расширением тех же `loadHome()`/`loadMyDay()` — данные получены один раз (`getMyDay`/`getStatsDaily`/`getSupervisorHealth`), два тонких render-таргета (`setBothHTML()`) их потребляют, ни одного нового API-вызова, backend не тронут. Порог дашборда — отдельный от порога shell: `≥1200px`, не `≥860px` (владелец лично поправил первую версию плана после review) — в диапазоне 860-1199 виден shell (сайдбар+топбар), но контент Главной остаётся мобильным swipe-каталогом без изменений: насильно втиснутый в узкий ноутбучный экран двухколоночный дашборд читался бы хуже мобильного layout внутри просто более широкой колонки. Swipe должен не исполняться вообще, когда активен дашборд, а не просто быть визуально перебит CSS — раздельного DOM недостаточно, `settle()` продолжал бы писать инлайн `height`/`transform` в скрытый track при каждом resize/асинхронной подгрузке (тот же класс бага, что уже дважды находили). Guard'ы 20.39.0/20.39.1 (порог 860px) **заменены** (не оставлены рядом вторым слоем) одним guard'ом на `SWIPE_DESKTOP_BREAKPOINT=1200` внутри `settle()` — идемпотентная проверка перед записью (реально ноль обращений к `style` после первого сброса, не просто "пишем пустую строку каждый раз"), тот же порог в `touchstart`. Проверено живым Playwright: на 1199 drag/dots работают как раньше, на 1200 — нет; `.swipe-track` не получает ни одной инлайн-записи через init+resize+асинхронный рефреш подряд. `.swipe-track{display:grid!important}` в CSS полностью убран — ниже 1200 выглядит как мобильный, выше скрыт целиком, никогда не нужно одновременно. Топбар получил breadcrumb (`#topbarBreadcrumb`, текст — из уже существующей подписи активного `.sidebar .nav-item` в `switchPage()`, не новый источник данных) — и сразу же живым скриншотом на 1200px пойман реальный дубль: мобильная секция `#commandCenterSection` ("Сеть за минуту") не была скрыта на десктопе и рендерила ту же health-score карточку, что и новая `.desktop-dashboard`'s панель аналитики, дважды на одной странице; закрыто тем же приёмом, что уже был у `.bottom-nav` в 20.39.0 (`!important` поверх инлайн-`display`, который `loadCommandCenter()` ставит независимо на обе секции). Изначальный план предполагал 3-колоночную сетку "Топ за 7 дней"/"Аналитика" на `≥1600px` через `:has(.insights-panel)` — при живой проверке выяснилось, что эта секция структурно вмещает максимум 2 панели вообще, ни для одной роли нет третьего контент-блока, поэтому `repeat(3,1fr)` создавал пустую 3-ю колонку ВСЕГДА, когда аналитика показана — ровно то, чего пункт должен был избежать, просто для другой роли; убран, не заменён — на ultrawide `auto-fit` из design-системы просто даёт панелям больше ширины честно, без фиктивного места (класс `.insights-panel` остаётся в JS на будущее, если появится настоящий 3-й блок). Отдельно обнаружен и обойдён rate-limiter собственного дев-сервера (300 запросов/60с, `x-ratelimit-*` заголовки) — Playwright-матрица из 10 ширин исчерпывала лимит на девятом-десятом полном перезаходе страницы (каждый — HTML+CSS+23 JS-бандла); не баг продукта, особенность верификации в этом окружении, скриншоты досняты после сброса окна. Верификация — полная матрица 390/768/859/860/1024/1199/1200/1440/1600/1920px живым Playwright: мобильный Home на 859 пиксель-в-пиксель прежний; 860-1199 показывает shell поверх немодифицированного мобильного контента, не "полу-дашборд"; ровно один "Главная" и один refresh на десктопе; сотрудник без `canViewAnalytics()` не видит пустой колонки на 1920px (панель аналитики просто отсутствует в DOM, сосед занимает всю ширину через `auto-fit`). 360 → 363 фронтенд-теста (`home-page.test.ts` расширен: `setBothHTML()`-зеркалирование в оба render-таргета, `.insights-panel` toggle на успех/ошибку/без-прав, идемпотентное клонирование `#homeToolsSection` → `#homeDesktopTools` без дубля `id`), `tsc --noEmit` (backend+frontend)/`smoke:frontend`/`check:no-direct-sql` зелёные |
| **20.40.1** | **Хотфикс по живым прод-данным сразу после 20.40.0.** Владелец продукта прислал скриншот реального desktop-дашборда — на настоящих данных (точка "Мегалит", полный набор из 10 отслеживаемых метрик: SIM/MNP/ПА/Комбо/Тел/Доп/Страх/Wink/ШПД/ФО) карточка "Мой день" оказалась заметно выше своей пары "Сеть сегодня" в том же `.workspace-grid`, а CSS Grid по умолчанию (`align-items:stretch`) растягивал короткую карточку под высоту длинной — большая пустая белая область снизу "Сеть сегодня" (и аналогично у пары "Топ за 7 дней"/"Сеть за минуту"). Синтетические Playwright-фикстуры из 20.40.0 использовали всего 6 метрик и не воспроизводили эту разницу высот — баг проявился только на реальном объёме данных одной конкретной точки, не на тестовых заглушках. Один атрибут — `.workspace-grid { align-items: start }` — обе панели теперь по высоте собственного контента, ровный верх, разный (честный) низ, вместо разной высоты потенциально с большой пустой областью. Проверено живым Playwright с реплицированным сценарием (10 прогресс-баров в "Мой день" против короткой "Сеть сегодня") — пустая область исчезла на скриншоте. Чисто CSS, JS/разметка не тронуты, 363 фронтенд-теста без изменений, `tsc --noEmit`/`smoke:frontend` зелёные — без анонса в Telegram-чат (хотфикс, не эпик) |
| **20.40.2** | **Хотфикс по живому прод-скриншоту с ролью admin — реальный access-баг, найденный за пределами заявленного 20.40 CSS-only скоупа.** Владелец продукта прислал скриншот реального desktop-дашборда, залогинившись как admin — сайдбар показывал только секцию "Обзор", ни "Аналитика", ни "Управление", ни "Супервайзер", ни "Администрирование" не появлялись вообще. Расследование нашло причину не в CSS и не в 20.40: `gateSidebarSection()`-вызовы (введены в 20.39.0) жили только внутри Telegram-ветки `bootApp()` (`access-supervisor/index.ts`) — не-Telegram bound-ветка (desktop-браузер/`POST /auth/login` через телефон, механизм с 20.35+) делала `return` через `enterHomeOrSupervisorShell()` РАНЬШЕ этого кода; то же верно для 404-fallback-ветки (`/access/status` не найден) и для catch-ветки при сетевой ошибке — итого 3 из 4 путей выхода из `bootApp()` никогда не гейтили секции сайдбара вообще, независимо от реальной роли пользователя. Тот же класс бага, что уже чинили в `loadMyDay()` в 20.40.0 (Telegram-only допущение, протухшее с приходом multi-provider auth в 20.35, просто не пойманное раньше, потому что вся верификация 20.39/20.40 до этого момента байпасила `bootApp()` синтетической подстановкой `window.me`, а не гоняла реальный boot flow). Исправлено: 4 повторяющихся блока (`btnAccessRequests`/`btnSupervisor`/`btnMgrTutorial` + 4 `gateSidebarSection()`) вынесены в одну `applyRoleGatedNav()`, вызывается из всех 4 веток `bootApp()` — не-Telegram/404/success/catch. Проверено не синтетической подменой `window.me`, а полным живым прогоном `bootApp()` с мокнутым `fetch()` (`/me`, `/access/status`, `/me/day`, `/supervisor/health`) — ровно тот код, что реально исполняется в браузере пользователя; 2 новых теста в `access-supervisor-page.test.ts` (не-Telegram admin — все 4 секции открыты; не-Telegram employee — все 4 остаются скрыты), 363 → 365. Заодно, в том же живом скриншоте, ещё два реальных находки по layout: (1) даже с `align-items:start` (20.40.1) под короткой панелью в паре ("Сеть сегодня"/"Топ за 7 дней") оставалась пустая область — align-items управляет положением ВНУТРИ grid-строки, а не высотой самой строки, которая остаётся высотой самой высокой ячейки; заменено на `.workspace-col` — независимые вертикальные flex-колонки вместо парных grid-строк, каждая складывает свои панели по своей собственной высоте, без общей высоты с соседней колонкой вообще (настоящая masonry-независимость, не имитация выравниванием); (2) `.sheet{max-width:1120px}` (откалиброван в 20.39 под однострочный mobile-style контент остальных страниц, "нормально, если хочешь сохранить читаемость") на реальном многоколоночном workspace-grid-контенте читался как большая пустая область справа, не запас — `.sheet:has(#page-home.active){max-width:1440px}` на ≥1200px и `1600px` на ≥1600px, гейтится `:has()` только на активную страницу Home, остальные страницы (вне scope 20.40) не тронуты, тот же `.sheet{1120px}` у них как был. Проверено живым Playwright на 1200/1440/1920 (полный boot flow с мокнутым fetch, не синтетический байпас) и мобильным 859 (без изменений). `tsc --noEmit` (backend+frontend)/`smoke:frontend` зелёные — без анонса в Telegram-чат (хотфикс, не эпик, хоть и существенный по содержанию) |
| **20.40.3** | **Хотфикс — визуальный дубль трёх секций Home на десктопе.** Владелец продукта: "почему блок Инструменты дублируется? убери это. и блок с калькуляторами помести под сеть за минуту". Причина: `.desktop-dashboard` (20.40.0) рендерит "Топ за 7 дней" в свой собственный `#homeTopDesktop` и клонирует "Инструменты" в `#homeDesktopTools` — но ОРИГИНАЛЬНЫЕ мобильные секции `#homeTop`/`#homeToolsSection` никогда не скрывались на ≥1200px (в отличие от `#homeTodaySwipe`/`#commandCenterSection`, которые скрыли ещё в 20.40.0) — оставались видимыми НИЖЕ дашборда, тот же контент дважды на одной странице. "Калькуляторы" (Комбо/Школа/Промокоды) той же проблемы не имели (в дашборде не рендерились вообще), но владелец явно попросил их туда добавить. Исправлено: `#homeCalcSection`/`#homeTopSection`/`#homeToolsSection` получили `id` и скрываются на ≥1200px тем же приёмом (`display:none!important`), что уже применён к `#commandCenterSection`. "Калькуляторы" — 3 статичные кнопки без данных, продублированы вручную (не JS-клон, как `#homeDesktopTools` — там 16 строк SVG, здесь 3 кнопки дешевле скопировать, чем городить клонирование) в правую `.workspace-col`, сразу под "Сеть за минуту". "Быстрые действия" НЕ трогали — её контент нигде в дашборде не дублируется, скрывать нечего, продолжает жить как единственная mobile-only секция ниже дашборда на десктопе (осознанно, не баг). Проверено полным живым `bootApp()`-прогоном (мокнутый `fetch`, не синтетический байпас) на 1600px: ни одного визуально задвоенного `.section-title`, `#homeCalcSection`/`#homeTopSection`/`#homeToolsSection` подтверждены `display:none` вычисленным стилем, "Быстрые действия" на своём месте единственным экземпляром. 365 фронтенд-тестов без изменений (чисто HTML/CSS, ни одного JS-таргета не менялось), `tsc --noEmit`/`smoke:frontend` зелёные |
| **20.40.4** | **Хотфикс — "Быстрые действия" поднята под "Калькуляторы".** Владелец продукта: "быстрые действия под калькуляторы подними и пусть они находятся только там". Тот же приём, что уже применён к "Калькуляторам" в 20.40.3: 4 строки-кнопки (Касса/Планы и факт за месяц/Динамика выполнения/Поддержка) статично продублированы в правую `.workspace-col` дашборда, сразу под "Калькуляторы" — итоговый порядок правой колонки: Сеть сегодня → Сеть за минуту → Калькуляторы → Быстрые действия. Мобильная секция получила `id="homeQuickSection"` и добавлена в тот же список `display:none!important` на ≥1200px, что уже скрывает `#homeCalcSection`/`#homeTopSection`/`#homeToolsSection` — "находятся только там" в буквальном прочтении: один видимый экземпляр на десктопе, ровно в дашборде. Проверено живым `bootApp()`-прогоном (мокнутый `fetch`) на 1600px — секция видна один раз, на нужном месте; мобильная версия по-прежнему `display:none` вычисленным стилем. Чисто HTML/CSS, 365 фронтенд-тестов без изменений, `tsc --noEmit`/`smoke:frontend` зелёные |
| **20.41.0** | **«Динамика выполнения» — та же сводная таблица, теперь и по точкам, не только по сотрудникам.** Владелец продукта: переименовать "Динамика выполнения по сотрудникам" в просто "Динамика выполнения" везде, и добавить под сотрудниками такую же разбивку по точкам. Расследование перед кодом (не сразу код) нашло, что бэкенд уже был наполовину готов: `getStoreMonthFacts()`/`getStoreMonthPlan()` (`core/plans/service.ts`) существовали давно — для карточки точки и редактирования её плана — не хватало только агрегата по всей сети сразу, ровно как `getMonthSummaryTable()` уже делает для сотрудников. Новая `getStoreMonthSummaryTable()` — буквально та же функция, что `getMonthSummaryTable()`, просто `storesRepo.listActiveBasic(orgId)` вместо `employeesRepo.listBasicByOrg()` и уже существующие `getStoreMonthFacts`/`getStoreMonthPlan` вместо employee-эквивалентов; ни одной новой SQL-функции писать не пришлось, только композиция уже существующих. `GET /plans/stores/month` — тот же гейт `requireActive` ("видно всей команде"), что у `/plans/employees/month`. `totals` здесь — сумма ПЛАНОВ ТОЧЕК, не планов сотрудников: это разные, независимо задаваемые цели, числа могут не совпадать между двумя таблицами — это ожидаемо, не баг сверки, задокументировано прямо в комментарии к функции. Фронтенд (`plans-bfq/index.ts`): `loadNetMonth()` получает оба среза параллельно (`Promise.all`), общий `renderNetMonthSection()` (вынесен из повторявшегося кода — раньше рендер сотрудников был инлайн в `loadNetMonth()`, теперь параметризованная функция с `nameOf`/`subOf`/`factOf`/`planOf` коллбэками) рендерит "По сотрудникам" и "По точкам" тем же барным стилем `sv-store`/`sv-bars`, что уже использует кабинет супервайзера — не новый визуальный язык. Раздельные id-префиксы у toggle "Ещё метрики" (`nme-`/`nms-`) — иначе `svExtraToggleHTML()` (глобальный toggle по id) перепутал бы секции при одинаковом индексе строки. Пустая секция (сеть без точек или без сотрудников) не рендерится вообще — не пустой заголовок без единой карточки под ним, тот же принцип, что уже применён к сайдбар-секциям в 20.39. Переименование "Динамика выполнения по сотрудникам" → "Динамика выполнения" — в обоих местах, где строка жила (мобильная секция "Быстрые действия" и её десктопная копия в `.desktop-dashboard`, добавленная в 20.40.4); подпись "все метрики · вся команда" тоже обновлена на "все метрики · сотрудники и точки" — старая формулировка подразумевала только людей. Историю (`CHANGELOG.md`/`changelog.ts`) не переписывали задним числом — прежние записи корректно описывают состояние на момент своего выпуска. Обнаружен и обойдён попутно: локальный тестовый Postgres (`.env.test.local`) держал данные с предыдущего прогона, нарушавшие FK-constraint из `0016_org_scoping_fk.sql` при повторном накате миграций — пересоздан с нуля (`DROP SCHEMA public CASCADE`), все 18 миграций накатились чисто; 4 теста в `org-scoping-fk.test.ts` (не связаны с этой версией) падают из-за RU-локали этого конкретного Postgres (сообщение об ошибке FK на русском, регулярка теста ждёт английское "foreign key") — не тронуто, вне scope этой правки. 3 новых backend-теста (`tests/isolation/plans.test.ts`) — та же org-изоляция, что уже проверена для `/plans/employees/month` (чужая сеть не видит точки, своя видна целиком с totals, без токена — 401), 1 расширенный + 1 новый фронтенд-тест (обе секции рендерятся с раздельными id-префиксами; пустой список точек не оставляет пустого заголовка "По точкам"). Живая проверка (Playwright, stub на `apiClient.getPlansStoresMonth`/`getPlansEmployeesMonth` — реальная авторизация недоступна в этом окружении, тот же класс ограничения, что уже отмечался всю сессию) подтвердила рендер на реальной структуре страницы с реалистичными метриками (`GET /metrics` — настоящий каталог из 15 метрик, не тестовая заглушка). 365 → 366 фронтенд-тестов (1 расширен на месте, не в счёт прироста), +3 backend isolation-теста, `tsc --noEmit` (backend+frontend), `smoke:frontend`, `check:no-direct-sql` зелёные |
| **20.42.0** | **Команда — первая настоящая `.data-table`.** Staged roadmap из 20.40 (`docs/DESKTOP-DESIGN.md`, Part G): единственная страница, которой реально нужен новый примитив без прецедента в проекте — сортируемая таблица. `.workspace-grid`/`.metric-grid`/`.row--dense` были заведены в 20.40 без потребителя, `.data-table` — намеренно нет ("нет ни одной страницы, которой он нужен прямо сейчас"). До этой версии desktop показывал ровно тот же список `<button class="row">`, что мобильный экран, просто внутри более широкого `.sheet`. Новый `#teamTableWrap` (`index.html`) рядом с существующим `#teamList` — `<table class="data-table">`, оба питаются одним и тем же ответом `loadTeam()` (`getEmployees()`+`getSales()`, без нового запроса и без своей фильтрации по `is_active` — состав строго тот же, что в мобильном списке). `#teamList` скрывается на ≥860px тем же base-rule-outside-media приёмом, что `.desktop-dashboard` в 20.40, — обычный shell-порог, не отдельный 1200px Home: в отличие от Home здесь нет swipe-карусели, конфликтовать не с чем. Сортировка (ФИО/Роль/SIM/Комбо/Тел) — клиентская, поверх уже загруженных данных, без повторного запроса; ФИО/Роль через `localeCompare('ru')`, SIM/Комбо/Тел — числовым сравнением, не строковым (иначе "9" встала бы раньше "10"). Заголовки получили настоящий `aria-sort` ("ascending"/"descending"/"none"), не только визуальный chevron. Уточнено с владельцем продукта перед кодом (не после): клик по строке у тех, кому доступна аналитика (`canViewAnalytics()`), сразу открывает страницу `employee-profile` (`openEmployeeProfile`) вместо модалки — раньше туда попадали только через кнопку "Профиль →" внутри той же модалки; остальным (`employee` без `canViewAnalytics()`) — прежняя модалка `openEmployeeCard`, без изменений. Клик где угодно внутри ячейки действий (`td.actions` — role-чипы/"Удалить") не переходит по строке — `closest('td.actions')` на весь блок, а не точечный `stopPropagation()` на каждой кнопке. 8 новых фронтенд-тестов (`tests/team-page.test.ts`): состав таблицы совпадает с `#teamList`, пустой список показывает то же сообщение, числовая сортировка по SIM с разворотом направления по повторному клику и корректным `aria-sort`, ru-сортировка по имени, клик по строке уводит на профиль или открывает модалку в зависимости от роли, клик внутри `td.actions` не переходит по строке, действия скрыты при `!canManage()`. Живая Playwright-проверка (мок `fetch` для `/me`/`/access/status`/`/employees`/`/sales` + реальный `bootApp()`, тот же приём, что вскрыл sidebar-баг в 20.40.2 — первая попытка с неполным `/me`-ответом без поля `bound:true` показала login-gate поверх корректно отрендеренных данных, поймано и исправлено до скриншотов) на 859/860/1440: мобильный список пиксель-в-пиксель как раньше, таблица со sticky-заголовком и рабочей сортировкой на десктопе, оба сценария клика по строке подтверждены визуально и через `window.page`/`openEmployeeProfile`. 366 → 374 фронтенд-теста, `tsc --noEmit`, `vitest run` (оба конфига), `smoke:frontend`, `check:no-direct-sql` зелёные; backend в этой версии не тронут ни строкой |
| **20.42.1** | **Хотфикс — CI поймал то, что локальная проверка пропустила.** `npx tsc --noEmit -p frontend/tsconfig.json` на 20.42.0 реально был прогнан через `| head -100` — вывод получился пустым не потому, что ошибок не было, а потому что предыдущий (успешный) прогон случился ДО того, как в `tests/team-page.test.ts` дописались новые тесты; после них typecheck ни разу не перезапускался живьём до пуша, только `vitest run` (esbuild-транспиляция, не настоящий `tsc`, типовые ошибки не ловит). CI: `error TS2488: Type 'NodeListOf<Element>' must have a '[Symbol.iterator]()' method` — `[...document.querySelectorAll(...)]` в трёх новых тестах требует `DOM.Iterable` в `lib`, которого нет в `frontend/tsconfig.json` (`lib: ["ES2022","DOM"]`, без `.Iterable`) — трогать `lib` ради трёх строк в одном тестовом файле не стали (blast radius), вместо этого `Array.from(document.querySelectorAll(...))` — работает с `NodeListOf` без `Symbol.iterator`, ровно то же поведение. Все шаги ритуала перепроверены с реальным exit-код (не через `| head`, который скрывает нетривиальные ошибки): `tsc --noEmit` (backend+frontend) — 0, `vitest run --config frontend/vitest.config.ts` — 374/374, `smoke:frontend`/`check:no-direct-sql`/`build:frontend` — зелёные |
| **20.43.0** | **Бот подчищает за собой сообщения в группах/каналах через 2+ дня.** Владелец продукта сам выдал боту admin-права и доступ к сообщениям в Telegram (вне этого репозитория) и попросил автоудаление. Уточнено в диалоге: везде, где бот пишет в группы/каналы (отчёты точек, алерты, дайджесты, продажи/смены) — но не в релиз-канал (анонсы версий с картинкой, `RELEASE_CHANNEL_ID`), тот остаётся архивом истории версий навсегда. Полностью новая возможность — исследование кодовой базы подтвердило: нигде не хранился `(chat_id, message_id, sent_at)` отправленных сообщений, `bot.api.deleteMessage` нигде не использовался. Новая таблица `bot_sent_messages` (`migrations/0019_bot_sent_messages.sql`) — `message_id integer`, не `bigint`: первая версия миграции использовала `bigint`, тесты упали с "expected `[222]` to include `222`" (`bigint` node-postgres отдаёт строкой, не числом, из-за потенциального переполнения `Number.MAX_SAFE_INTEGER`) — таблица пересоздана с `integer`, реальный диапазон Telegram `message_id` умещается с запасом, тест-исправление поймало это до пуша, не в CI. `notifyChat()`/`notifyChatPhoto()`/`notifyChatMediaGroup()` (`integrations/telegram/bot.ts`) — единственный узкий проход, через который идут все групповые отправки (отчёты/алерты/дайджесты/продажи/смены) — теперь захватывают `message_id` из ответа Telegram API (раньше результат отбрасывался) и пишут его в журнал через новый `trackGroupMessage(chatId, messageId)`. Личные сообщения сотрудникам (`notifyUser`) идут отдельным путём и не затронуты; `notifyAdmin()` технически вызывает `notifyChat(text, ADMIN_ID)` — фатальные алерты в личку админа тоже попадают под чистку через 2 дня, осознанное следствие общего решения "везде, где бот пишет в группы/каналы", а не отдельно разобранный случай — но проговорено прямо здесь, не решено молча. `trackGroupMessage()` читает `process.env.RELEASE_CHANNEL_ID` на каждый вызов, а не кэширует в модульную константу при импорте — первая версия так и делала, но тогда проверить исключение в тесте можно только пересоздав весь граф зависимостей `bot.ts` через `vi.resetModules()`, а это транзитивно пересоздаёт и пул Postgres (`data/db/index.ts` создаёт `new Pool()` на верхнем уровне модуля) — второй незакрываемый пул соединений в тестовом процессе; переделано на чтение env на каждый вызов ДО того, как это стало проблемой в тесте, не после. Новый `src/cron/message-cleanup.ts` — смоделирован буквально на `cron/digest.ts`/`cron/alerts.ts` (`cron.schedule('* * * * *', ...)` + ручной гейт по `nowTimeMoscow() === '03:00'`, без опции timezone у node-cron — сознательный паттерн проекта, тот же класс времени, что digest в 09:00). `sweepOldGroupMessages()` находит записи старше 2 дней (`listSentGroupMessagesOlderThan`), поштучно (`for`, не `Promise.all` — осторожность к Telegram rate-limit, та же, что неявно соблюдена во всех остальных местах, где бот шлёт много сообщений подряд) вызывает `bot.api.deleteMessage()`, и в ЛЮБОМ случае (успех или "уже удалено вручную/чат недоступен") убирает строку журнала — мёртвые записи не копятся, таблица самоограничена по размеру без отдельного retention-джоба. Зарегистрирован в `index.ts` рядом с `alertCronTask`, с `.stop()` в `gracefulShutdown()` — тот же паттерн, что уже есть для `digestCronTask`/`alertCronTask`. 6 новых backend-тестов (`tests/unit/message-cleanup.test.ts`, реальный Postgres, тот же стиль, что `tests/unit/cron-idempotency.test.ts`): запись/чтение-по-cutoff/удаление строки журнала по отдельности, `sweepOldGroupMessages()` целиком (старая запись 3 дня пропадает, свежая 1 час остаётся — глобальный sweep по всем чатам, не по одному), исключение `RELEASE_CHANNEL_ID`. `BOT_TOKEN` не задан ни в CI, ни в `.env.test` → `bot === null` → `bot.api.deleteMessage` физически не вызывается (тот же null-guard, что уже есть в каждой функции `bot.ts`) — тесты проверяют поведение таблицы, живой Telegram API не мокался и не вызывался. Живой прогон миграции 0019 и полный старт сервера (`npm run dev` на одноразовом локальном Postgres) подтвердили `🧹 Message cleanup cron started (2 дня, 03:00 МСК)` в логе. 365 → 371 backend-тестов, `tsc --noEmit`, `smoke:frontend`, `check:no-direct-sql` зелёные; frontend в этой версии не тронут ни строкой |
| **20.44.0** | **Schedule/Plans — Desktop Page Adaptation, следующий шаг после Team.** `docs/DESKTOP-DESIGN.md` (Part G, 20.40) назвал следующим шагом после Team "Schedule/Plans": `.data-table` на сводный график, monthplan-карточки → гибрид data-list/table. Разбор кода (не сразу код) показал, что это две разные по природе задачи: сводный график команды (`.sum-sch-table`, `schedule/index.ts::renderSummarySchedule()`) — матрица «сотрудник × день месяца» (2 строки на сотрудника: смена+точка, часы) с цветовым кодированием точки прямо в ячейке (инлайн `style` от JS) — колонки-дни физически не атрибуты для сортировки, сортировка здесь не имеет смысла; monthplan (`plans-bfq/index.ts::loadMonthPlans()`, `.mt-card`/`.mt-grid`) — список сотрудников с метриками, ровно тот архитектурный случай, что уже был у Команды до 20.42.0 (карточка на сотрудника, клик по шапке — редактирование). Подтверждено с владельцем продукта перед кодом: график — только визуальный рестайл под новый токен-язык `.data-table-wrap` (`.sum-sch-scroll{border-radius:var(--radius-lg)}` вместо захардкоженных `10px`), без сортировки и структурных изменений — цветовая подсветка точки в ячейке остаётся, это реальная информация (кто где вышел), не декор; monthplan получает настоящую `.data-table`. 15 метрик всего (6 основных + 9 «ещё») — подтверждено с владельцем: 6 сортируемых колонок постоянно + один общий тумблер над таблицей "Показать все метрики"/"Скрыть лишние метрики", добавляющий/убирающий 9 EXTRA-колонок сразу для ВСЕХ строк разом (не раскрытие по каждой строке отдельно, как на мобильных карточках через `toggleMonthExtra()`). Новый `#monthPlanTableWrap` (`index.html`) рядом с `#monthPlanList`, оба питаются одним и тем же ответом `getPlansEmployeesMonth()` — без нового запроса. `MAIN`/`EXTRA` (раньше жили только внутри `loadMonthPlans()`) подняты на уровень модуля — новая `renderMonthPlanTable()` их тоже использует, без повторного вычисления из `METRICS`. Сортировка клиентская: ФИО/Роль через `localeCompare('ru')`, Смены и каждая метрика — числовым сравнением по `fact`-значению. Тон-классы `good`/`warn`/`bad` на `<td>` — та же логика порогов (`pct≥100/50`), что уже была в `cell()` для карточек, просто без вложенного `.v` (в ячейке таблицы число — прямой текст). Клик по строке (`<tr data-clickable onclick="editEmployeeMonthPlan(...)">`) у `canManage()` — та же модалка редактирования плана, что сейчас на `mt-card-head` мобильных карточек; без `canManage()` строка не кликабельна вообще (нет атрибута, не просто визуально), как и сейчас. Строка "Итого сеть" — нередактируема, не участвует в сортировке, рендерится отдельно и всегда последней. Заодно поправлена мелкая утечка из 20.42.0: `.data-table tbody tr:hover{cursor:pointer}` показывал курсор-руку на КАЖДОЙ строке любой `.data-table`, даже некликабельной — для Team это было незаметно (там кликабельны все строки), но для monthplan подсвечивало бы некликабельные строки как интерактивные; заменено на `tr[data-clickable]{cursor:pointer}` (явный атрибут, не hover-предположение), Team-таблице проставлен `data-clickable` на каждую строку явно, чтобы её курсор не изменился. Живой скриншот поймал реальный баг ДО релиза, не после: `.btn-ghost` по умолчанию `width:100%` (мобильные CTA-кнопки) растянул тумблер "Показать все метрики" на всю ширину таблицы, выглядел как гигантская плашка — `.data-table-toolbar .btn-ghost{width:auto}` сделал его компактным по контенту. 6 новых фронтенд-тестов (`tests/plans-bfq-page.test.ts`, 374 → 380): состав таблицы совпадает с `#monthPlanList` плюс "Итого сеть", пустой список показывает то же сообщение с месяцем, числовая сортировка по метрике с разворотом направления и корректным `aria-sort`, тумблер добавляет/убирает 9 колонок разом (9 → 18 заголовков), строка кликабельна только при `canManage()` (проверено и на `employee`, и на `manager`), "Итого сеть" не кликабельна. Живая Playwright-проверка (мок `fetch` для `/me`/`/access/status`/`/plans/employees/month`/`/schedules/month` + реальный `bootApp()`) на 859/1440 по обеим страницам: мобильные виды (карточки monthplan, календарная сетка графика) пиксель-в-пиксель как раньше, sticky-заголовок и рабочая сортировка на десктопе, тумблер подтверждён по факту роста заголовков, клик по строке реально открыл модалку "План: Яна Смирнова" (после исправления async-race в самой проверке — первая попытка читала `modalShown` синхронно сразу после клика, до того как `await loadMetricsCatalog()`/`await getEmployeeMonthPlan()` внутри `editEmployeeMonthPlan()` успевали отработать). `tsc --noEmit` (frontend), `vitest run --config frontend/vitest.config.ts`, `smoke:frontend`, `check:no-direct-sql` зелёные; backend в этой версии не тронут ни строкой |
| **20.45.0** | **Command Center + Supervisor — `.workspace-grid` на списки-секции.** `docs/DESKTOP-DESIGN.md` (Part G, 20.40) назвал следующим шагом после Schedule/Plans: "`.workspace-grid` на `.sv-*`/`.cc-*` секции". В отличие от Team/monthplan, которым нужен был совсем новый примитив (`.data-table` с сортировкой), здесь задача заметно легче: Command Center (`#page-command-center`, `command-center/index.ts::loadCommandCenterPage()`) и все 4 вкладки супервайзера — «Обзор»/«Точки»/«Люди»/«Тренд» (`access-supervisor/index.ts`) — уже полностью карточно-списочные (`.sv-store`/`.sv-drop`/`.sv-rank`/`.progress-block`), на десктопе это был просто один длинный вертикальный список внутри `.sheet`, ни одной адаптацией не тронуто с 20.38 — «просто reflow»-страницы из исходного аудита 20.40. Никакого нового JS-функционала (сортировка/тумблеры/клики) не понадобилось — только раскладка уже существующих списков в адаптивную сетку вместо одной колонки. `.workspace-grid` (заведён в Home, 20.40 — `repeat(auto-fit,minmax(320px,1fr))`) определён ТОЛЬКО внутри `@media(min-width:860px)` — на мобильном класс ничего не меняет (не grid вне медиа-запроса), можно добавлять без риска для мобильного вида. Из 6 списков карточек 2 (`#svStoresBody` — «Точки», `#svPeopleBody` — «Люди») на 100% состоят из повторяющихся карточек (`box.innerHTML = (...).map(...).join('')`) — им grid-правила даны напрямую по id в CSS (`display:grid;grid-template-columns:repeat(auto-fit,minmax(320px,1fr))`), без единой правки JS/тестов. Остальные 4 списка — список только часть контейнера, рядом сосед по потоку (герой/заголовок/график): Command Center («Что происходит» — точки сети `.progress-block`, «Где проблема» — `.sv-drop`) — добавлен `class="workspace-grid"` на уже существующие обёрточные `<div style="padding...">`; «Обзор» (`renderSvOverview()`, «Просадки и риски») и «Тренд» (`renderSvTrend()`, «Прогноз по точкам» — хвост функции, после двух ОДИНОЧНЫХ `.sv-store`-карточек сектора целиком/прогноза сектора, которые гридить незачем — по одной карточке каждая) — точечно обёрнут JOIN-результат `.map(...).join('')` в новый `<div class="workspace-grid">...</div>`, вставленный только когда список непустой (пустой — прежний `.empty`-текст без обёртки, поведение не изменилось). `.sv-hero`/`.sv-metrics` (карточка-герой на «Обзоре», не список), `.sv-chart` (график на «Тренде», не карточки), внутренняя разметка `.sv-bars`/`.sv-extra`/«Ещё метрики» (уже сделанная в прошлых версиях компоновка СОДЕРЖИМОГО карточки) — не тронуты: доки прямо говорят "на секции", не "на содержимое карточек". Карточки несли собственные inline/класс-отступы (`margin-bottom` и т.п.) под блочную вёрстку — внутри грида это удвоило бы отступ поверх `gap`; добавлен сброс `margin:0!important` для карточек — прямых детей `.workspace-grid`/`#svStoresBody`/`#svPeopleBody`. Роли не тронуты — `canViewAnalytics()` для Command Center, `isSupervisor()||canAdmin()` для супервайзерских вкладок, те же гейты, что уже были. 3 новых/расширенных фронтенд-теста: `command-center-page.test.ts` — 2 assertion на существующий рендер-тест (`class="workspace-grid"` на обеих секциях); `access-supervisor-page.test.ts` — 2 assertion на существующий тест (drops/прогноз-по-точкам обёрнуты через regex, т.к. между открывающим тегом обёртки и первой карточкой есть перенос строки из шаблонного литерала) + 1 новый тест на пустые списки (без обёртки `.workspace-grid`, только `.empty`). Живая Playwright-проверка (мок `fetch` для `/me`/`/access/status`/`/command-center`/`/supervisor/dashboard` + реальный `bootApp()`/`enterSupervisorShell()`) на 859/1440 по всем 5 страницам: мобильный вид `getComputedStyle().display` — `block` (не изменился), десктопный — `grid`, карточки лежат сеткой без задвоенных отступов между `gap` и `margin` карточки, герой/график/заголовки-секции в грид не попали. `tsc --noEmit` (frontend), `vitest run --config frontend/vitest.config.ts` (380 → 381), `smoke:frontend`, `check:no-direct-sql` зелёные; backend в этой версии не тронут ни строкой |
| **20.46.0** | **Admin — «максимально функциональный».** Последний пункт staged roadmap из 20.40 (Part G: "`.data-table` на orgs/audit") — владелец продукта явно расширил объём: "учти что админ должен быть максимально функциональный". Аудит кода (не догадки, через два раунда research-агентов) нашёл три реальных пробела, каждый подтверждён с владельцем отдельным уточняющим вопросом перед кодом. **Баг**: повышение сотрудника до `supervisor` (Команда → role-чип → `setRole()`, `team/index.ts`) никогда не передавало `sector_id` — `PATCH /employees/:id/role` (`org/employees.ts:209-227`) уже принимал его опционально и молча не писал строку в `supervisor_sectors`, если он отсутствовал; человек оставался без сектора без единого сообщения об ошибке, а `PUT /supervisor/:id/sector` (эндпоинт-фикс) существовал, но фронтенд его никогда не вызывал. `setEmployeeRole()` (api-client.ts) получил опциональный 4-й параметр `sectorId`; `setRole()` теперь при `role==='supervisor'` открывает новый общий модал-пикер сектора (`openSectorPickerModal()`, `team/index.ts`, список секторов — из нового `GET /admin/dealers`, сгруппирован по дилеру через `<optgroup>`) — "Назначить и повысить" вызывает `setEmployeeRole(id, role, sectorId)` одним запросом, "Пропустить, назначу позже" сохраняет прежнее поведение осознанно (не потеряно молча — такой супервайзер попадёт в `unassigned_supervisors` нового экрана). **Новый экран «Дилеры/Секторы»** (`src/pages/dealers`, новый `vite.dealers.config.ts`/`dealers.bundle.js`, третий пункт в `sidebarSectionAdmin` и в мобильном "Управление" рядом с «Сети»/«История действий») — дилер/сектор раньше заводились только неявно свободным текстом на форме сети (`network-admin/index.ts::orgFormHTML()`, поля `#no_sector`/`#no_dealer`), не было GET-all эндпоинта вообще, не было переименования, не было способа посмотреть "какие сети/супервайзеры в этом секторе". Новый `GET /admin/dealers` (`src/api/routes/org/dealers.ts` → `core/orgs/dealers.ts::getDealersTree()`) — чистая композиция трёх уже существующих плоских выборок (`dealersRepo.listAllDealers()`, `dealersRepo.listAllSectorsWithDealer()`, уже существующий `orgsRepo.listAll()`) плюс новая `supervisorSectorsRepo.listAllWithSupervisorNames()` — строит дерево дилер→сектор→сети/супервайзеры в JS, ни одного нового прямого SQL сверх трёх простых SELECT/JOIN в репозиториях. Два хвоста в ответе — `unassigned_sectors` (секторы без `dealer_id`) и `unassigned_supervisors` (`role='supervisor'` без строки в `supervisor_sectors`) — именно то, что раньше терялось молча. `PATCH /admin/dealers/:id`/`PATCH /admin/sectors/:id` — первое в жизни переименование (`sectors.name` с момента создания через `organizations.ts::upsertSector()` всегда равнялось `id`, отдельной rename-функции не существовало). Экран рендерит НЕ `.data-table` (дерево — не однородный список одинаковых строк, тот же принцип выбора паттерна под форму данных, что уже применялся у Schedule/Plans в 20.44.0) — вложенные `.section`-карточки, переиспользующие уже стилизованные `.sv-store`/`.sv-store-head`/`.progress-block` классы (Supervisor/Command Center) и уже существующий `.workspace-grid` для раскладки секторов внутри дилера — новой CSS для этого экрана не потребовалось вообще, кроме нуля новых строк. **Аудит-фильтры**: `GET /audit` (`src/api/routes/audit.ts`) уже полностью поддерживал `action`/`target_type`/`from`/`to`/`limit`/`offset` на бэкенде (`auditRepo.list()` строит динамический WHERE) — фронтенд (`getAuditLog(headers, orgQuery)`) их никогда не вызывал, `#page-audit` не имел вообще никаких элементов управления. `getAuditLog()` получил опциональный `filters` объект, собирающий query string через `URLSearchParams`; попутно найден и исправлен реальный баг в СУЩЕСТВОВАВШЕМ коде при сборке URL — `request(\`/audit${orgQuery}\`, ...)` при непустом `orgQuery` (который всегда начинается с `&`, рассчитан на то, что перед ним уже стоит `?что-то=`) производил битую ссылку `/audit&org_id=...` БЕЗ `?` вообще; старый тест `api-client.test.ts` буквально называл эту битую ссылку "верный URL" и проверял её как корректную — переписан на проверку исправленного `/audit?org_id=...`. Новая панель фильтров (`.data-table-toolbar`, тот же примитив, что monthplan 20.44.0) — action/target_type `<select>` (опции из уже существующего `AUDIT_ACTION_LABEL` словаря + нового `AUDIT_TARGET_TYPE_LABEL`, статичный список из реальных `target_type`, найденных grep'ом по всем вызовам `recordAudit()`: employee/employee_plan/store_plan/sale/export — не выдуманный), 2 `<input type="date">`. Пагинация "Показать ещё" — backend не отдаёт total-count, кнопка скрывается по факту `returned.length < limit` (осознанное упрощение, не "страница N из M"). Новая `.data-table` (`#auditTableWrap`) с сортировкой по уже загруженной странице (не переспрашивает сервер) и клик по строке открывает модал с ПОЛНЫМ `before`/`after` (`JSON.stringify(...,null,2)` в `<pre>`), не инлайн-JSON в одну строку, как было в мобильном списке. Заодно починено: `GET /audit` игнорировало `?org_id=` СОВСЕМ (`resolveViewOrgId(request.user!, undefined)` — жёстко `undefined` вместо чтения из query) в отличие от остальных admin-роутов с org-переключателем — теперь `resolveViewOrgId(request.user!, q.org_id)`. Plus исходный пункт Part G: Сети (`#page-orgs`) → `.data-table` (`renderOrgsTable()`) тем же паттерном, что Team (20.42.0) — Название/Дилер/Сектор/Статус сортируемые, клик по строке — `openEditOrg()`, мобильный `#orgsList` не тронут. Попутно найдено тестом ДО пуша, не в CI: `dealers.id`/`employees.id` — оба `bigint` (`0001_baseline.sql`/`0015_dealers.sql`), node-postgres отдаёт такие колонки строкой, не числом — тот же класс проблемы, что уже ловили на `bot_sent_messages.message_id` (20.43.0); тест `body.dealers.find(d => d.id === dealerId)` молча возвращал `undefined` (строка `!==` число) — исправлено `id::int` в `listAllDealers()`/`listAllSectorsWithDealer()`/`listAllWithSupervisorNames()`, держит именно этот новый ответ честно типизированным, не пытаясь исправить куда более широкую существующую неоднородность типов `employees.id` по остальному backend (вне объёма этой правки). 12 новых backend isolation-тестов (`roles-sectors.test.ts` +2 — role change с/без `sector_id` реально пишет/не пишет `supervisor_sectors`, раньше не проверено вообще, только на словах в коде; `audit-log.test.ts` +5 — фильтры action/target_type/from-to реально сужают выборку, limit/offset пагинируют, `org_id`-override теперь работает для admin; новый `dealers-admin.test.ts` — 5 тестов на дерево/хвосты/admin-only-гейт/переименование) + 22 новых/расширенных фронтенд-теста (`api-client.test.ts` +6, `team-page.test.ts` +3 новых/1 исправлен, `network-admin-page.test.ts` +5, новый `dealers-page.test.ts` +7) — включая регрессию на саму находку прошлой версии: inline `onclick="..."` строки НЕ исполняются через синтетический `dispatchEvent(new Event('click'))` в jsdom (`ReferenceError: openEditOrg is not defined`) — клик по строке `.data-table` во всех новых тестах проверяется прямым вызовом функции, на которую ссылается атрибут, не синтетическим DOM-событием (тот же паттерн уже был у более ранних тестов Team в этой сессии, просто не был последовательно применён здесь с первого раза). Живая Playwright-проверка (мок `fetch` + реальный `bootApp()`) на 859/1440 по всем 3 страницам: Сети/Аудит — таблицы на десктопе/списки на мобильном не изменились, фильтры аудита реально фильтруют (`getAuditLog` вызван с ожидаемыми аргументами), "Показать ещё" догружает 51-ю строку и скрывает кнопку на неполной странице, diff-модал открывается по клику с полным JSON; Дилеры/Секторы — дерево, "не назначен", блок непривязанных супервайзеров, и на мобильном, и на десктопе рендерятся адаптивно без отдельной mobile-разметки (новая страница, нет legacy-вида для сохранения). `tsc --noEmit` (backend+frontend), `vitest run` — 371→383 backend (4 нерелевантных RU-locale теста в `org-scoping-fk.test.ts` по-прежнему падают, не тронуты, задокументировано с прошлых версий), 381→403 frontend, `smoke:frontend` (23→24 файла), `check:no-direct-sql` зелёные. Механическая зачистка оставшихся tool-страниц (bfq/cash/heatmap/forecast/etc.) на общие примитивы из Part C — явно вне объёма этой версии, отдельный шаг после, не забыто |
| **20.47.0** | **iPhone Web App/Safari — safe-area слой поверх Telegram Mini App.** Мобильный интерфейс изначально строился как Telegram Mini App; вне Telegram (Safari, установка на Home Screen как Web App) он не учитывал notch/Dynamic Island, Home Indicator и адресную строку Safari. Цель патча — не новый дизайн и не переписанный мобильный интерфейс, а корректный iOS-слой поверх существующего визуального языка и поведения. Единственный существующий механизм детекции Telegram (`window.tg.safeAreaInset` → инлайн-стиль `--tg-content-safe-top` на `<body>`, `core.ts`) расширен, а не продублирован — второй параллельный "это iPhone"-детект через user-agent не заводился. Новые переменные в `:root` (`styles.css`): `--app-safe-top: max(var(--tg-content-safe-top), env(safe-area-inset-top, 0px))`, аналогично `--app-safe-bottom/left/right` через `env()` — `max()`, а не сумма, гарантирует отсутствие двойных отступов внутри Telegram (там `env()` всегда `0`, работает только tg-канал) и корректный отступ в Safari/standalone (там `--tg-content-safe-top` всегда `0`, работает только `env()`). `--bottom-nav-clearance:92px` (прежняя жёсткая величина bottom-nav, сохранена как safe-area-zero базовая линия) + `--bottom-nav-safe-offset: calc(var(--bottom-nav-clearance) + var(--app-safe-bottom))` — единая семантическая переменная вместо разрозненных `calc()` у `body`/`.fab`/`.bottom-nav`/`.sheet-modal`/`.gate-shell`/tutorial-оверлеев; `.fab` отталкивается от той же переменной, что и bottom-nav (`bottom: calc(var(--bottom-nav-safe-offset) + 8px)`), не от отдельного магического числа для iPhone. `.app-header` перешёл с точечного `--tg-content-safe-top` на единый `--app-safe-top` (это и был центральный пробел — вне Telegram верхний отступ раньше не резолвился вообще); `.toast`, tutorial-оверлей (`#tutorialOverlay`/`#tutorialScreen`), `.gate-shell` — с точечных `env(safe-area-inset-*)` на единые `--app-safe-*`. `.sheet-modal` дополнительно получил `max-height:90dvh` (поверх уже существующего `90vh`-фолбэка), `-webkit-overflow-scrolling:touch` и `padding-bottom` с добавкой `--app-safe-bottom` — устраняет обрезание кнопки сабмита Home Indicator'ом. На устройствах без notch/Home Indicator (Telegram, Android, десктоп) все `--app-safe-*` резолвятся в `0px` — геометрия побайтово та же, что до патча, регрессии в Telegram нет структурно, не только по факту тестирования. Найден и учтён реальный пробел, не регрессия: landscape iPhone (реальные модели — до ~926px, например iPhone 14 Pro Max) reach-ит существующий breakpoint `@media(min-width:860px)` десктоп-shell — туда прежде не добавлялось НИКАКИХ safe-area отступов; теперь `.sidebar`/`.app-header`/`.fab`/`.sheet` внутри этого брейкпоинта тоже учитывают left/right/bottom safe-area. `-webkit-tap-highlight-color:transparent` точечно добавлен на `.fab`/`.mchip`/`.btn-main`/`.btn-ghost` (ранее был только на `.row`/`.nav-item`) — убирает серую вспышку iOS поверх кастомных `:active`-состояний; агрессивные глобальные `user-select:none`/`touch-action:none` НЕ добавлялись — не требовались, нативные `<button>` уже подавляют нежелательное выделение текста. PWA/Home Screen metadata (`index.html`, между `<title>` и Telegram SDK `<script>`): `apple-mobile-web-app-capable`, `mobile-web-app-capable`, `apple-mobile-web-app-status-bar-style=black-translucent` (статус-бар прозрачный/оверлей, не требует ручной синхронизации с переключателем светлой/тёмной темы), `apple-mobile-web-app-title`, `theme-color`; `viewport-fit=cover` уже был в существующем viewport-теге, не добавлялся повторно. Manifest/Service Worker/офлайн-кэш сознательно не добавлялись — фальшивая полноценная PWA-инфраструктура ради meta-тегов явно вне объёма этого патча. `nav.ts::applyTheme()` — новая ветка синхронизирует `<meta name="theme-color">` тем же цветом, что уже уходит в `tg.setBackgroundColor()` (не новый источник истины, тот же цвет по двум каналам — Safari/standalone читают meta-тег, Telegram его игнорирует и слушает свой canvas-API); `toggleTheme()` унаследовал автоматически, т.к. вызывает `applyTheme()` внутри. Проверено и сознательно оставлено БЕЗ изменений как уже корректное — по явному требованию не выдумывать проблемы: все `input`/`select` (`.field select`, `.field input`, `.qty-row input`, `.gate-card input/select`) уже ровно 16px (iOS autozoom при фокусе не воспроизводится, повторная правка не потребовалась); `body`/`.sheet` уже имели `min-height:100vh` с последующим `min-height:100dvh`-фолбэком (JS-полифилл `window.innerHeight` не добавлялся — dvh уже решает задачу конкретно для случая скрытия/появления адресной строки Safari, обоснованного случая для JS не нашлось); `overscroll-behavior-y:contain` уже стоял на `html, body`; `-webkit-overflow-scrolling:touch` уже был на двух горизонтальных списках (добавлен только на два новых вертикальных — `.sheet-modal`/`#tutorialScreen`); `-webkit-tap-highlight-color` уже была на `.row`/`.nav-item`; bottom-nav уже учитывал `env(safe-area-inset-bottom)` (не баг — просто сведено к общей переменной ради единой модели, не потому что было сломано). Новый тест в `app-nav.test.ts` — `applyTheme()` обновляет `<meta name="theme-color">` тем же цветом, что `tg.setBackgroundColor()`. Верификация: `tsc --noEmit` (backend+frontend) — 0 ошибок; `vitest run --config frontend/vitest.config.ts` — 403→404 (24 файла); `smoke:frontend` — 24 файла, OK; `check:no-direct-sql` — зелёный, backend не тронут ни строкой; `vitest run` (backend, локальный одноразовый Postgres) — 383 passed, те же 4 заведомо нерелевантных RU-locale теста в `org-scoping-fk.test.ts` по-прежнему падают (локальный Postgres настроен на `lc_messages=Russian_Russia.1251`, тест матчит `/foreign key/i` — задокументировано ещё в 20.46.0, никак не связано с этим патчем, CI использует англоязычный service-контейнер). Живой Playwright (мок `fetch` + реальный `bootApp()`) на 390/393/430/768/859/860/1200px подтвердил: bottom-nav-раскладка держится ровно до 859px, десктоп-shell включается ровно на 860px, sheet-модал с формой ("Новый сотрудник") не обрезает кнопку сабмита снизу, PWA meta-теги на месте, `theme-color` синхронизируется при переключении темы — но Chromium НЕ эмулирует реальные Safari safe-area insets один в один, поэтому Chromium-скриншот не доказывает корректность реального Dynamic Island/Home Indicator. Требуют ручной проверки на настоящем iPhone: standalone Home Screen Web App, Safari с показанной/скрытой панелью инструментов, светлая/тёмная тема статус-бара, bottom navigation и FAB живьём, модал с формой, фокус инпута и появление клавиатуры, длинный скролл страницы |
| **20.48.0** | **Web Security & Trust Layer, часть 1 — Authentication & Session Security.** Со standalone iPhone Web App/Safari-слоем (20.47.0) у приложения де-факто три канала входа — Telegram Mini App, телефон-браузер, standalone PWA — вместо одного доверенного Telegram-канала, на котором строилась исходная модель угроз. Владелец продукта предложил эпоху «Web Security & Trust Layer» из 6 частей, сам расставил приоритет (сначала Auth/Session Security) и дал три раунда детального архитектурного ревью этого плана перед стартом реализации — итоговый план явно зафиксировал 13 «непереговорных инвариантов». Аудит кода (не догадки) показал, что многое уже построено: провайдер-агностичная `Identity`/`Principal`-граница (ADR-005), реальная cookie-сессия для phone-входа (`employee_sessions`, `HttpOnly+Secure+SameSite=Lax`, только `sha256(token)` в БД), rate-limit на чувствительных роутах, уже throttled `last_seen_at` (раз в час, `touchSession()`), уже свежая генерация токена на каждый `/auth/login` (session fixation структурно невозможна — `expires_at` уже фиксируется один раз при создании сессии, абсолютный 30-дневный TTL уже есть, не sliding). Работа — закрыть подтверждённые пробелы и осознанно перейти порог, который ADR-005 дважды откладывал с обоснованием «один provider не оправдывает схему»: schema-level `identities`-таблица (`backend/migrations/0020_identities.sql`: `employee_id, provider, provider_key, created_at, updated_at`, `UNIQUE(provider,provider_key)` + `UNIQUE(employee_id,provider)`) — единственный источник правды для auth-резолва (`auth/principal.ts::loadUser()`), не декоративная таблица поверх `employees.telegram_id`/`phone`/`password_hash`, которые остаются нетронутыми для не-auth потребителей (бот-уведомления по `telegram_id`, отображение в Команде и т.д.). Разная семантика конфликта по provider, зафиксированная как инвариант владельцем продукта в процессе ревью: **Telegram** — ownership transfer (steal) разрешён и обязан сохранять уже протестированную продуктовую семантику self-bind (`data/repositories/identities.ts::transferIdentity()` — один атомарный `INSERT...ON CONFLICT (provider,provider_key) DO UPDATE SET employee_id=EXCLUDED.employee_id`, не `DELETE`+`INSERT` — race-safe при параллельных claim одного `provider_key`, Postgres сериализует через row-lock; `employees.ts::claimTelegramId()` — единственная точка изменения Telegram identity, employees-колонка и identities-строка обновляются внутри ОДНОЙ транзакции, никогда по отдельности). **Phone** — ownership transfer НЕ разрешён автоматически (`bindIdentityStrict()` — тот же освобождающий `DELETE` для собственного переподвязывания, но БЕЗ `ON CONFLICT`: конфликт с чужим номером — сырой `23505` → `409 phone_taken`, уже обрабатываемый существующим кодом в `me/index.ts`/`session.ts`) — телефон credential boundary, не recovery-механизм, как Telegram self-bind. При разборе найден и исправлен реальный, **предшествующий 20.48.0** баг (не внесён этой правкой): `claimTelegramId()`'s `WITH cleared AS (UPDATE employees SET telegram_id=NULL WHERE telegram_id=$1)` CTE не имел data-зависимости с главным `UPDATE` — PostgreSQL не гарантирует порядок выполнения независимых data-modifying CTE в одном `WITH`-запросе, поэтому при переносе Telegram с УЖЕ занятой карточки на другую main-UPDATE мог проверить `UNIQUE(telegram_id)` РАНЬШЕ, чем `cleared` освобождал строку прежнего владельца, и словить ложный `23505`→`409` там, где ожидался успешный transfer; единственный существующий adversarial-тест (5 параллельных bind) не ловил это, потому что все 5 карточек стартовали НЕзанятыми (`cleared` матчил 0 строк вне зависимости от порядка) — сценарий «снять с уже занятой карточки» никогда не проверялся до нового transfer-теста в `me-bind.test.ts`. Фикс — принудительная data-зависимость `WHERE id=$2 AND (SELECT count(*) FROM cleared) >= 0`, воспроизведён и подтверждён вручную (`node --input-type=module` против локального Postgres) до и после фикса. Session lifecycle: `softDeactivate()` (уже внутри `withTransaction`) теперь снимает `identities(provider='telegram')` (зеркалит `telegram_id=NULL`; phone НЕ трогается — переживает деактивацию, как и раньше `employees.phone`) и `DELETE FROM employee_sessions WHERE employee_id=$1` — деактивация инвалидирует browser-сессии немедленно, не полагаясь на то, что `requireActive` поймает это на следующем запросе; `/auth/reset/:token` тем же принципом отзывает ВСЕ существующие сессии сотрудника ДО выдачи новой (устройство A украдено → пароль меняют на B → токен на A не остаётся рабочим). Новые `GET /auth/sessions` (список — только `id/created_at/last_seen_at/current`, без IP/User-Agent/геолокации, PII вне объёма), `DELETE /auth/sessions/:id` (ownership-scoped запросом; отзыв ТЕКУЩЕЙ сессии чистит оба cookie на ответе — API безопасен сам по себе, не полагается на то, что фронтенд не покажет кнопку на своей строке), `POST /auth/sessions/revoke-others` («выйти на всех других устройствах») — работают для ЛЮБОГО provider'а (Telegram-пользователь тоже видит и может отозвать свою browser-сессию, `current:false` на всех строках закономерно). CSRF — двухслойно: `Sec-Fetch-Site:cross-site` отклоняется сразу, до проверки токена; без этого заголовка (старые браузеры) `Origin` сверяется с уже существующим `MINI_APP_URL` (не изобретали параллельную env-переменную), не реконструируется из `Host`/`X-Forwarded-Host`; double-submit cookie `t2_csrf` (криптографически случайный, тот же генератор, что `sessionsRepo.generateToken()`, НЕ `httpOnly`, та же область видимости, что `t2_session`) ротируется вместе с ней на каждый логин. `auth/csrf.ts::requireCsrf` — глобальный `preHandler` сразу после `authPlugin`, срабатывает только при наличии cookie `t2_session` на мутирующем запросе (Telegram-путь не затронут вообще, без явного списка исключений по роутам) — `/auth/login`/`/auth/register`/`/auth/reset/:token` явно исключены отдельно (`EXEMPT_PATHS`): в браузере уже может лежать старая/подставная `t2_session` cookie (session fixation, протухшая сессия после рестарта БД), и без явного исключения честный вход падал бы в CSRF-отказ раньше, чем дошёл бы до собственной логики логина — найдено и исправлено собственным session-fixation-тестом до пуша. `POST /auth/logout` теперь чистит оба cookie, не только `t2_session`. Rate-limit `/auth/login` — ключ `sha256(normalizePhone(phone))`, не IP (закрывает distributed brute-force по одному номеру через много IP и через разные форматы записи одного номера), работает вместе с уже существующим глобальным 300/мин-по-IP лимитом, не вместо него. `app.ts::Fastify({trustProxy:1})` — Railway один reverse-proxy хоп, доверяем ровно ему, не `true` (принял бы любую цепочку `X-Forwarded-For`) — точность IP-based rate-limit. `backend/src/utils/phone.ts::normalizePhone()`/`validatePhone()` — строго RU-профиль (проект целиком русскоязычный, `Europe/Moscow`, международные номера сознательно вне объёма, `libphonenumber` не тянули), заменили нестрогий `PHONE_RE` на всех write-путях (`/auth/register`, `/auth/login`, `/me/link-phone`); миграция 0020 нормализует существующие `employees.phone` ДО backfill теми же правилами (`regexp_replace` + `8XXXXXXXXXX`→`+7XXXXXXXXXX`) с preflight-проверкой на дубли — `RAISE EXCEPTION` откатывает ВСЮ миграцию (`db/migrate.ts` уже оборачивает каждый файл в `BEGIN/COMMIT/ROLLBACK`), не молча теряет строку. Новый экран «Активные сессии» в `my-plan/index.ts` (рядом с уже существующим `logoutSelf()`) — список/revoke/revoke-others, `api-client.ts::request()` читает `t2_csrf` из `document.cookie` и добавляет `X-CSRF-Token` на мутирующих запросах. ADR-005 дописан разделом «порог пройден» с формулировкой инварианта transfer/reject-семантики дословно от владельца продукта; `SECURITY.md` §2 расширен (identities, transfer/reject, CSRF, session-revoke-on-credential-change, `trustProxy`), новая строка в «Известные компромиссы» про RU-only нормализацию телефона. 9 новых backend isolation-тестов (`identities.test.ts`, `session-lifecycle.test.ts`, `sessions-admin.test.ts`, `csrf.test.ts`, `login-rate-limit.test.ts`) + 1 unit (`phone.test.ts`, табличные кейсы нормализации) + расширенный adversarial race-тест на `claimTelegramId` (identities-консистентность после гонки) + новый transfer-тест (нашёл и зафиксировал регрессию на CTE-баг выше) — 383→416 backend. `tsc --noEmit` (backend+frontend), `vitest run --config frontend/vitest.config.ts` (404, без изменений в счёте — фронтенд-часть только CSRF-заголовок в `request()` + список сессий в `my-plan`), `smoke:frontend`, `check:no-direct-sql` (`sessions-admin.ts`/`csrf.ts` добавлены в ratchet-список) — зелёные. 4 заведомо нерелевантных RU-locale теста в `org-scoping-fk.test.ts` по-прежнему падают, не тронуты, задокументировано с прошлых версий |
| **20.49.0** | **Web Security & Trust Layer, часть 2 — Browser Security.** Владелец продукта сказал «делай всё сразу» на выбор между аудитом+CI-gate сначала или полным CSP-рефакторингом одним куском — исследование (3 параллельных research-агента, все результаты перепроверены выборочно вручную на реальном коде, не приняты на веру) вскрыло два факта, которые меняют, что именно значит «всё»: найдены реальные, эксплуатируемые XSS-дыры (не гипотетические), и закрытие `unsafe-inline` для `script-src-attr`/`style-src-attr` — не «немного CSS», а 265+ `onclick=`/`onchange=`/`oninput=` в 21 TS-файле + 108 в `index.html`, плюс ~400 `style=`-атрибутов; рефакторинг переезда с `js/*.js` на `src/**/*.ts` (20.30.0, тот же проект) растянулся на ~27 версий именно потому, что переносил такой же объём разметки инкрементально с тестами на каждом шаге, не одним диффом. Решение (соответствует уже устоявшемуся в проекте паттерну — большие механические переделки не делаются одним диффом, откладываются явно, не молча): в этот проход — всё, что можно сделать безопасно и полно прямо сейчас; отдельно, явно зафиксировано как следующий шаг, не забыто — удаление `unsafe-inline`. **Реальные XSS-фиксы** (`esc()`, уже существующий, bare global в 19+ файлах): `progressHTML()` (`app/nav.ts:74`) — самый широкий охват, метка кастомной метрики (создаётся любым manager'ом через `POST /metrics`, только `label: Type.String({minLength:1})`, без ограничения символов) без экранирования рендерилась в `home`/`my-plan`/`schedule`/`shift`/`store-profile`/`team` (8+ мест защищены одной правкой); store name без `esc()` в `schedule`/`cash-metrics`/`network-admin`/`plans-bfq`/`shift`/`add-sale`/`home` (7 файлов, тот же однострочный паттерн на каждом месте). **Attribute-breakout класс** (более тонкий баг, не просто «забыли esc()»): `jsEsc()` (`dealers/index.ts:13-14`) экранировал только JS-string-literal контекст (`'`), но результат подставлялся в `onclick="..."` — HTML-атрибут-контекст — без HTML-экранирования; имя сектора/дилера/сотрудника (self-chosen при регистрации) с `"` разрывало атрибут и внедряло произвольный обработчик на элемент — второго порядка stored XSS против **admin**-аккаунта, просматривающего экран «Дилеры/Секторы». Тот же класс бага в `cash-metrics/index.ts:150` — `JSON.stringify(m.label)` даёт JS-safe строку, но не HTML-attribute-safe. Fix для обоих: весь onclick-value оборачивается в `esc()` ПОВЕРХ уже существующего `jsEsc()`/`JSON.stringify()` — `jsEsc` остаётся ответственным за JS-string safety, `esc()` — за HTML-attribute safety, слои не путаются (задокументировано новым комментарием у `jsEsc()`). `promos/index.ts:115-117` (`data.code`/`note`/`created_by_name`) — самый низкий барьер входа из всех находок: `POST /promos` требует только активного сотрудника (`resolveEmployeeFromRequest`), НЕ manager — любой сотрудник может внедрить payload в `note` (лимит 200 символов, без HTML-очистки), читает вся сеть при следующем открытии промокода. `support/index.ts:110,113` (FAQ question/answer) — без активного write-роута в backend, defense-in-depth/консистентность, не эксплуатируемо сегодня. **CI-gate** — новый `scripts/check-dangerous-js-patterns.mjs`, модель — `check-no-direct-sql.mjs` (allowlist+`FORBIDDEN`-regex-массив+`fs.readFileSync`+`process.exit(1)`, без ESLint/AST), но без allowlist «уже мигрированных» файлов (нет естественного DAL-подобного подмножества) — обход `frontend/src/**/*.ts` целиком через `fs.readdirSync(dir,{recursive:true})` (Node 22.x, без новых зависимостей). `FORBIDDEN`: `document.write`/`eval`/`new Function`/строковый `setTimeout`/`setInterval` — все 4 паттерна дают 0 совпадений сегодня (фиксирует уже достигнутую чистоту как регресс-барьер). Осознанно НЕ включена эвристика «innerHTML без esc() поблизости» — слишком расплывчато для чистого regex без AST, высокий false-positive rate; реальные найденные дыры закрыты адресно, не через несовершенный паттерн-мэтчинг. Новый `npm run check:dangerous-js` + шаг в `.github/workflows/ci.yml` рядом с DAL-шагом. **`Cache-Control: no-store`** — глобальный `onSend`-хук (`app.ts`, рядом с уже существующими metrics-хуками) — `if (!reply.getHeader('cache-control')) reply.header('Cache-Control', 'no-store')`; единственное условие покрывает оба существующих исключения без явного списка путей: `GET /avatars/:id` уже ставит свой `private, max-age=300` до `onSend` (пропускается), `@fastify/static` (`cacheControl:true` по умолчанию) тоже успевает выставить `public, max-age=0` до глобального хука — раньше auth/session/employee-PII роуты не несли Cache-Control вообще, проверено живым curl на локальном dev-сервере (не только предположением о порядке хуков): статический ассет и `index.html` сохранили `public, max-age=0`, `/healthz` получил `no-store`. `X-Content-Type-Options: nosniff`/`Referrer-Policy: no-referrer` — уже корректны на helmet-дефолтах (`useDefaults:false` в текущей CSP-регистрации влияет ТОЛЬКО на CSP-под-конфиг, не на остальные helmet-мидлвары — подтверждено чтением исходников `helmet`/`@fastify/helmet` в `node_modules`), правка не потребовалась, добавлен только regression-тест. Подтверждено чистым без изменений (research-агенты, каждый вывод перепроверен): `postMessage` — не используется этим кодом нигде (Telegram SDK делает это внутри себя, вне репозитория); clickjacking — `X-Frame-Options` уже осознанно `false` (CSP `frame-ancestors` вместо него), frame-busting JS не добавлен (устаревшая техника при поддержке `frame-ancestors`); `localStorage`/`sessionStorage` — 5 ключей всего (тема, tutorial-флаги, streak-кэш), ничего auth-adjacent, `sessionStorage` не используется вообще; open-redirect/`javascript:`-URI — 0 мест построения URL из интерполированной строки. `styleSrc: 'unsafe-inline'` (block-level, не `-Attr`) — проверено ВРУЧНУЮ перед потенциальным удалением (не принято на веру заключение research-агента «нет инлайн style-блоков»): `shift/index.ts:499-506` реально создаёт `<style>`-блок через `document.createElement('style')` для keyframe-анимации конфетти при закрытии смены — оставлено как есть, находка задокументирована в этой записи, не молча пропущена. Заодно найдено и исправлено: 2 существующих frontend-теста (`promos.test.ts`, `cash-metrics-page.test.ts`) не стабили `esc()` вообще — с новым `esc()`-вызовом внутри падали в `ReferenceError`, тихо давая пустой рендер вместо явной ошибки теста; `cash-metrics-page.test.ts` также содержал assertion на СЫРУЮ (неэкранированную) подстроку onclick-атрибута — переписан на проверку через `getAttribute()` (значение после HTML-decode, не зависит от деталей экранирования). **Явно отложено, не забыто**: `script-src-attr`/`style-src-attr: 'unsafe-inline'` — закрытие требует перевода 265+ мест на event-delegation через `data-*`-атрибуты и ~400 `style=` на CSS-классы/custom properties (паттерн для динамических уже есть — `access-supervisor/index.ts:755`, `--sv-p`/`--sv-h`) — отдельная, сопоставимая по объёму с Frontend rewrite (20.3.0-20.30.0, ~27 версий) эпоха, не один дифф; найденные конкретные XSS-дыры закрыты адресно уже сейчас (устраняют саму инъекцию, не только её исполнение), что снижает срочность, но не ценность этого CSP-шага как defense-in-depth. Новый `security-headers.test.ts` (3 backend isolation-теста: заголовки на любом ответе, `no-store` на типичном API-роуте, avatar сохраняет свой Cache-Control) + расширенный `app-nav.test.ts` (`progressHTML` XSS-payload) + 2 новых frontend security-регресс-теста (`dealers-page.test.ts`, `cash-metrics-page.test.ts`) — оба используют РЕАЛЬНУЮ `esc()` вместо no-op стаба (иначе тест ничего не доказывал бы про реальное экранирование) и настоящий `onmouseover="..."` payload, jsdom реально парсит HTML — если бы `"` разорвал атрибут, тест поймал бы настоящий `onmouseover` на элементе. `tsc --noEmit` (backend+frontend), `vitest run` — 416→421 backend (те же 4 нерелевантных RU-locale теста), `vitest run --config frontend/vitest.config.ts` — 404→411, `smoke:frontend`, `check:no-direct-sql`, новый `check:dangerous-js` — все зелёные |
| **20.50.0** | **Web Security & Trust Layer, часть 3 — API Abuse Protection.** Части 1 (Auth & Session Security) и 2 (Browser Security) уже отгружены. Часть 3 — по списку владельца продукта: разные лимиты по категориям роутов, лимиты не только по IP, body/pagination limits, защита дорогих analytics/forecast эндпоинтов, idempotency там, где повтор реально опасен. Аудит (3 параллельных research-агента + ручная перепроверка каждого спорного вывода на реальном коде) показал: часть инфраструктуры уже корректна — глобальный body limit (1MB Fastify-дефолт, ничем не переопределён), лимит на аватарку (1.5MB + перепроверка в хендлере), пагинация `/audit` (≤500) и `/sales/history` (≤2000) уже ограничены на уровне репозитория, `trustProxy:1` не дрейфовал. **Два вывода research-агентов оказались ложными — перепроверены и отклонены до попадания в код, не тихо забыты**: (1) `/sales/audit` агент посчитал неограниченным — прямое чтение `data/repositories/sales.ts:320` показало уже существующий `LIMIT 500` в SQL (`findSalesAudit()`); (2) `POST /stores` агент сообщил, что двойной сабмит даёт необработанный `500` — прямое чтение `app.ts:50-55,183-200` показало, что глобальный `setErrorHandler` УЖЕ мапит Postgres `23505` в чистый `409 {error:'conflict'}` для любого роута, `stores.id` как primary key уже ловится этим существующим механизмом без единой правки. **Rate-limit** (`config: {rateLimit:{max,timeWindow:'1 minute'}}`, идентичный паттерн, что уже в `shifts.ts:98`) добавлен на 16 роутов, ранее полагавшихся только на глобальный 300/мин-по-IP (`app.ts`) — тиры по реальной стоимости, не по «страшному» имени (`GET /heatmap/precise`, `/me/insight`, `/stats/daily` проверены и оказались дешёвыми одно-двухзапросными эндпоинтами, лимит НЕ добавлен, чтобы не выдумывать проблему): `GET /forecast/:storeId` (20/мин) и `POST /shifts/close` (30/мин, симметрично уже лимитированному `/shifts/open`) — оба дёргают Groq, реальные деньги за вызов; `GET /staffing-hints` (20/мин, цикл `forecastStore()` по каждой точке сети) и `GET /network/live` (20/мин, 5 последовательных запросов НА КАЖДУЮ точку, `core/analytics/live-map.ts:17-34`); `POST /admin/rebuild-hour-profiles` и `POST /alerts/run` (оба 5/мин — полный пересчёт/проход по всей сети, редкие admin-действия); `POST /metrics` (5/мин — `ALTER TABLE` на 3 таблицах за вызов через `ensureColumn`); `POST /me/avatar` (10/мин — write-сторона уже лимитированного `GET /avatars/:id`, асимметрия чинится); 5 export-роутов (`GET /sales/audit` 20/мин для консистентности — уже был `LIMIT 500`; `GET /export/sales.csv` 10/мин — см. ниже; `GET /export/bfq.csv`/`/export/schedules.csv` 20/мин — уже естественно ограничены месяцем; `GET /export/bi/daily` 10/мин — дамп продаж + N+1 live-map в одном ответе); `POST /schedule/what-if` (20/мин) и `/schedule/what-if/apply` (10/мин, реальные записи); единственная АНОНИМНАЯ (без сессии вообще) поверхность API — `GET /access/orgs`, `/access/employees-directory`, `/access/status` (все 30/мин, тот же тир, что уже public `GET /avatars/:employeeId`) — задокументированный в SECURITY.md trade-off, но раньше вообще без лимита. **`GET /export/sales.csv`** (`ops/export.ts:84`) — единственная export-функция без `LIMIT` вообще (`data/repositories/sales.ts::findForCsvExport`, сравнено с `findSalesAudit()`, у которой уже `LIMIT 500`); тихая обрезка строк была бы хуже для файла, которым реально пользуется бухгалтерия (неполные данные без предупреждения) — вместо неё явная валидация ширины диапазона: `to − from > 400 дней` → `400 {error:'range_too_wide', message:'Сузьте диапазон дат — максимум 400 дней за один экспорт'}`, не тихая обрезка строк в SQL; `/sales/audit` не тронут — его `LIMIT 500` уже отгружен и протестирован. **`POST /schedule/what-if(/apply)`** (`analytics/what-if.ts:19-22`) — `moves: Type.Optional(Type.Array(WhatIfMove))` не имел `maxItems` (`WhatIfMove` сам `additionalProperties:true` осознанно, не тронут — гетерогенные объекты сценария), единственной границей была мягкая Fastify 1MB body limit — слишком мягкая для роута, где каждый move — 2 последовательных запроса в `simulateScheduleMoves`; добавлен `maxItems:200`, с запасом на реальный сценарий (менеджер переставляет команду на день). **`GET /forecast/:storeId`** (`analytics/forecast.ts:23-38`) — AI-сводка (`generateForecastSummary`, Groq) кэшируется в `ai_audit` по `(storeId, from)`, но `from` был полностью клиентским (`String(query.from \|\| todayMoscow()).slice(0,10)`) — разные `from` на каждый запрос давали разные кэш-ключи и свежий Groq-вызов на КАЖДЫЙ запрос вместо заявленного в комментарии «раз в день на точку» (комментарий был написан ДО этого cache-busting-поведения, не соответствовал коду). Fix соответствует уже заявленному намерению — AI-сводка генерируется только когда `from===todayMoscow()`, для любой другой даты — `ai_summary:null` без вызова Groq вообще; расчёт числового прогноза (`forecastStore()`) не тронут. Регресс-тест мокает сам `integrations/ai/client.js` (не только `data/repositories/ai.js`, как `tests/unit/observability.test.ts`) — в тестовом окружении нет `GROQ_API_KEY`, поэтому `callGroq()` и так вернул бы `null` что для дырявого, что для починенного кода; только прямой мок `generateForecastSummary` отличает «не вызвали вообще» от «вызвали и получили null». **`POST /employees`** (`org/employees.ts:58-77`) — двойной тап/ретрай молча создавал двух сотрудников с одинаковым `full_name`/`role`/`org_id`: на `employees` нет `UNIQUE` на `(full_name, org_id)`, `id` — обычный serial, каждый `INSERT` проходит независимо (в отличие от `/stores`, где `id` — primary key и коллизия уже ловится). Тот же переиспользуемый примитив, что уже в `POST /tasks` (`claimIdempotencyKey`, `data/repositories/sync-log.ts` — race-safe `UNIQUE(client_id)` + `ON CONFLICT DO NOTHING`, `offline_sync_log_client_id_key`) — `PostEmployeeBody` получил опциональный `client_id`, тот же 5-строчный guard, что уже в `tasks.ts:121-125`; клиенты, ещё не шлющие `client_id`, продолжают работать по-старому (обратная совместимость, зафиксирована regression-тестом). **Сознательно вне объёма**: identity-based `keyGenerator` (ключ по `employee_id`, не IP) для новых лимитов выше — прецедент `POST /auth/login` (20.48.0, ключ по `sha256(normalizePhone(phone))`) специально узкий, для pre-auth момента, где IP вообще единственная альтернатива; обобщение на все уже аутентифицированные роуты — отдельное архитектурное решение (например, что считать identity для Telegram-гостя без `employee_id`), не запрошенное в этом списке владельца продукта, если понадобится — отдельным шагом. 13 новых/расширенных backend isolation-тестов: 2 показательных rate-limit-теста (`api-abuse-rate-limits.test.ts` — `/forecast/:storeId` и `/export/sales.csv`, не все 16 роутов по отдельности — механизм `@fastify/rate-limit` уже покрыт существующими тестами на других роутах), `export.test.ts` +2 (диапазон >400 дней → 400, ровно 400 → проходит), `alerts-whatif.test.ts` +2 (`moves` из 201 → 400 TypeBox validation, 200 → проходит), новый `forecast-ai-cache.test.ts` +2 (`from=today` вызывает мок ровно раз, `from`≠today не вызывает вообще), `employees.test.ts` +2 (с `client_id` — дедуп ровно один сотрудник, без — обратная совместимость: два сотрудника, как раньше), новый `stores-idempotency.test.ts` (двойной `POST /stores` — `409`, не `500`, фиксирует уже корректное поведение, чтобы вопрос не переоткрывался). `tsc --noEmit`, `vitest run` — 421→432 backend (те же 4 нерелевантных RU-locale теста в `org-scoping-fk.test.ts`, не тронуты, задокументировано с прошлых версий), `smoke:frontend`, `check:no-direct-sql`, `check:dangerous-js` — все зелёные. Фронтенд не тронут ни строкой — всё изменение backend-only |
| **20.50.1** | **Хотфикс — 4 реальных XSS-пробела, найденных repo-wide документационным аудитом, не закрытых 20.49.0.** Тот же класс, что уже чинили в `dealers.ts`/`cash-metrics.ts` в 20.49.0 (attribute-breakout: `jsEsc()` экранирует только JS-string `'`, не HTML-атрибут-контекст) и «без esc() у источника» (store name/promo note), но пропущенный в других файлах того же прохода. Найдено: (1) `pages/plans-bfq/index.ts:174,265-266` — `onclick="editEmployeeMonthPlan(...)"` (карточка И desktop-строка месячного плана) экранировал `full_name` только `.replace(/'/g,"\\'")`, весь onclick-value не был обёрнут в `esc()`; (2) `features/promos/index.ts:58` — `created_by_name` в списке промокодов (`.promo-meta`) рендерился без `esc()`, хотя то же поле в карточке того же промокода (строка 117) уже экранировалось с 20.49.0 — пропуск одного из двух мест рендера; (3) `pages/schedule/index.ts:252` и (4) `pages/my-plan/index.ts:252` — `store_name`/`shift_text` в `title="..."` без `esc()`, при том что соседние места в тех же файлах уже экранированы. Fix — тот же паттерн, что уже в `dealers.ts`: локальный `jsEsc()` (JS-string safety) + весь onclick/title-value обёрнут в `esc()` (HTML-attribute safety) поверх него. Заодно найдено и починено: `plans-bfq-page.test.ts`/`schedule-page.test.ts`/`my-plan-page.test.ts` стабили `esc()` как no-op (`(s) => String(s ?? '')`) — тот же класс проблемы, что уже чинили в `promos.test.ts`/`cash-metrics-page.test.ts` в 20.49.0 (no-op стаб не поймал бы ни старую, ни новую регрессию); переведены на реальную реализацию (`import { esc } from '../src/app/core.js'`), все существующие ассершены на этих 3 файлах остались зелёными без изменений (тестовые фикстуры не содержали спецсимволов). 4 новых regression-теста с настоящим XSS-payload (`"` в `onclick`/`title`, `<img onerror>` в тексте) — jsdom реально парсит HTML, тест проверяет отсутствие исполненного обработчика (`document.querySelectorAll('[onmouseover]')`/`img[onerror]`), не только текст ошибки. `tsc --noEmit` (backend+frontend), `build:frontend`, `smoke:frontend`, `check:dangerous-js` — зелёные; `vitest run --config frontend/vitest.config.ts` — 411→415; backend `vitest run` — 428/432 без изменений (те же 4 нерелевантных RU-locale теста в `org-scoping-fk.test.ts`, не тронуты этим фиксом). Найдено в рамках repo-wide documentation audit (см. `docs/SECURITY.md` — известные компромиссы), не отдельным security-проходом |
| **20.51.0** | **Application-Level Envelope Encryption — крипто-слой для support-тикетов.** Владелец продукта запросил спроектировать hybrid-ratcheting крипто-архитектуру (X25519+PQ KEM→ratchet→AEAD) с явным запретом писать примитивы самостоятельно. Аудит репозитория не нашёл ни одной фичи, которая была бы приватной перепиской 1:1 — все freeform-text места (`channels`/`task_comments`/`announcements`/`shift_sessions.handover_note`) team/org-wide broadcast или admin-видимые по дизайну; `support_tickets`/`support_messages` — единственный реальный кандидат (admin легитимно должен читать тикет по запросу, но at-rest дамп БД — избыточная поверхность утечки). Три уровня защиты разведены явно: Level 1 (TLS, уже был) — не тронут; **Level 2 (application-level envelope encryption, реализовано)** — backend расшифровывает по требованию; **Level 3 (true E2EE, NOT IMPLEMENTED)** — нет продуктового кандидата, см. `docs/ADR/008` (device identity/handshake/ratchet/PQ осознанно не строятся ради несуществующей приватной переписки, подтверждено с владельцем продукта явным выбором «не строить сейчас»). Новый слой `backend/src/security/crypto/**` — AES-256-GCM (`node:crypto` built-in, не XChaCha20-Poly1305 — Node не даёт extended-nonce ChaCha20 из коробки), HKDF-SHA256 (`crypto.hkdfSync`) для domain-separated wrap-key, CSPRNG (`crypto.randomBytes`) для DEK — ноль новых npm-зависимостей, тот же принцип, что `auth/password.ts`'s `crypto.scrypt` вместо bcrypt/argon2. Key hierarchy: Master Key/KEK только в env (`ENCRYPTION_KEKS`, версионирован, никогда в PostgreSQL) → DEK (CSPRNG, один на объект, хранится только wrapped) → ciphertext поля; versioned envelope (`{v,alg,kid,dek,data}`) переживает key rotation без re-encryption всего хранилища. AAD (`{type,id,...}`, canonical-сериализация) криптографически связывает ciphertext с конкретной строкой — `id` резервируется явным `nextval()` до `INSERT` именно для этого; перенос ciphertext между записями ломает AEAD tag. Интеграция в `data/repositories/support.ts` полностью прозрачна для роутов — `message`/`admin_reply`/`body` расшифровываются на чтение автоматически; `DATA_ENCRYPTION_ENABLED` гейтит только новые записи, чтение старых зашифрованных строк не зависит от флага (downgrade в plaintext невозможен по конструкции). Production-гвард в `index.ts` (тот же приём, что уже у `BOT_TOKEN`) не даёт серверу стартовать со сломанным `ENCRYPTION_KEKS`/`ENCRYPTION_ACTIVE_KEY_VERSION`. Попутно найден и исправлен реальный пред-существующий баг: `markAnsweredByAdmin`/`markReopenedByUser`/`appendAdminReplyFallback` писали `updated_at` в колонку, которой у `support_tickets` никогда не было — необработанный `500` на каждый ответ сотрудника в своём же тикете, ни разу не пойманный тестами раньше, обнаружен первым же isolation-тестом на этот путь. 44 новых crypto unit-теста (`tests/unit/crypto-envelope.test.ts` — encrypt/decrypt, wrong KEK, wrong AAD, tampered ciphertext/nonce/tag, key rotation, parser fail-closed на malformed/truncated входные данные, env-конфигурация) + 6 isolation-тестов через реальные роуты и Postgres (`tests/isolation/support-envelope-encryption.test.ts` — raw DB dump не содержит plaintext, key rotation end-to-end, повреждённый конверт даёт явный `[ошибка расшифровки]`-маркер, не 500 и не тихий plaintext-фолбэк; cross-employee IDOR не обходится шифрованием). `tsc --noEmit`, `vitest run` — 432→482 backend (те же 4 нерелевантных RU-locale теста в `org-scoping-fk.test.ts`), `smoke:frontend`, `check:no-direct-sql`, `check:dangerous-js` — все зелёные. Фронтенд не тронут ни строкой — новая миграция `0021_support_envelope_encryption.sql` (два nullable jsonb-столбца на `support_tickets`, один на `support_messages`, старые text-колонки не удалены) |
| **20.52.0** | **Principal Security Audit — MFA & Step-Up, IDOR-фиксы.** Владелец продукта запросил полный аудит по 10 доверительным поверхностям (Telegram Mini App, браузер, PWA, backend API, Postgres, admin/supervisor, фоновые джобы, бот, AI-интеграции, CI/CD) с позиции principal application security engineer — реальный разбор кода/миграций/тестов/конфигурации, не чек-лист. Объём брифа — многонедельный, сделан осознанный триаж: P0 (IDOR) и P1-центр (MFA/step-up/session hardening) закрыты полностью с тестами, остальное честно задокументировано как не реализованное. Обязательный сильный MFA для admin/supervisor — WebAuthn/passkey приоритетно (`@simplewebauthn/server` v13), TOTP fallback (`otplib` v13), recovery codes, явно без SMS как первичного фактора; новые таблицы `employee_totp`/`employee_webauthn_credentials`/`employee_recovery_codes`/`mfa_pending_logins`/`mfa_webauthn_challenges`/`mfa_step_up_tickets` (`0022_mfa.sql`). TOTP-секрет хранится через уже существующий Application-Level Envelope Encryption слой (20.51.0), переиспользован не продублирован. Channel-agnostic step-up: Telegram initData не имеет server-side сессии вообще (ADR-005), поэтому «свежесть» MFA-подтверждения не привязать к состоянию сессии — решение — непрозрачный bearer-тикет (`mfa_step_up_tickets`, TTL 10 минут, `X-Step-Up-Token`), привязанный только к `employee_id`, работает одинаково для Telegram- и browser-пути. MFA обязателен для admin/supervisor без route-interceptor на каждый роут: `POST /auth/mfa/step-up` сам отказывается выдавать тикет без подтверждённого фактора — это транзитивно перекрывает сброс чужого пароля/MFA и назначение роли admin одной точкой. Session hardening: idle timeout 14 дней + сокращённый абсолютный TTL для privileged-ролей (7 дней против 30). IDOR-аудит закрыл реальные пробелы без org-скоупинга: `GET /plans/stores/:id/month`, `GET /employee/progress/:id` (manager-ветка), `POST /alerts/:id/read`, `POST /announcements/:id/read`, плюс явные admin-only проверки на `POST /alerts/run`, `/reports/send-micro`/`/send-final`, `/admin/rebuild-hour-profiles`. Найден и починен воспроизводимый на 100% Fastify-баг (бисекцией, не угадан): второй async `onSend`-хук даёт гонку `ERR_HTTP_HEADERS_SENT` с `@fastify/multipart` на `POST /me/avatar` при неаутентифицированном запросе — новый заголовок Permissions-Policy слит в уже существующий `onSend`-хук, не зарегистрирован отдельно. Попутно: таймаут на вызове Groq (`AbortSignal.timeout()`, 15с), экранирование комментария сотрудника в Telegram-отчёте (`cron/reports.ts`, ранее без `esc()`), CI закреплён по SHA (`actions/checkout`/`actions/setup-node`) + `permissions:contents:read`. Осознанно НЕ реализовано: frontend UI для MFA-enrollment, security-observability события, fuzz-тесты, SBOM, legacy-plaintext-backfill, полный набор runbook'ов из брифа. 35 новых MFA isolation-тестов + обновления существующих. `tsc --noEmit`, `vitest run` — 482→519 backend (515 проходят, те же 4 нерелевантных RU-locale теста), `smoke:frontend` (415/415, фронтенд не тронут), `check:no-direct-sql`, `check:dangerous-js`, `npm audit` (0 high) — все зелёные |
| **20.52.1** | **Auth Assurance Hardening.** Фокусированный проход по уже реализованной MFA/session/crypto-подсистеме (20.52.0) — не новая архитектура, ужесточение существующей. Ключевой найденный и закрытый пробел (PRIV-MFA-1/2): admin/supervisor без MFA получал полноценную AAL1-сессию, которая проходила ВСЕ обычные privileged-роуты — только step-up-gated опасные действия (выдача роли, сброс чужого пароля/MFA) были недостижимы без второго фактора; `GET /employees`, Command Center и т.п. оставались открыты. Новый `auth/assurance.ts` + `auth/guards.ts::requireActive()` — единая точка (`checkPrivilegedAssurance()`, считается раз в `authPlugin`) блокирует `403 mfa_enrollment_required` для privileged-роли без фактора на КАЖДОМ защищённом роуте, кроме явно помеченных enrollment/status/logout; работает одинаково для Telegram (пересчитывается на каждый запрос) и browser-сессий. Найден и закрыт реальный обход MFA через сброс пароля (RESET-1) — `POST /auth/reset/:token` выдавал сессию сразу, даже для аккаунта с настроенным вторым фактором; теперь та же MFA-ветка, что у логина. Найден и закрыт ROLE-1 — эскалация роли до admin/supervisor (`PATCH /employees/:id/role`, `POST /access/requests/:id/approve`) теперь отзывает существующие сессии сотрудника, иначе уже открытая AAL1-сессия молча наследовала бы privileged-доступ без MFA (role читается заново из БД на каждый запрос). Idle-таймаут сессии стал раздельным по роли — 18 часов для admin/supervisor вместо общих 14 дней (при абсолютном TTL уже 7 дней 14-дневный idle был для них бессмысленным параметром). WebAuthn `userVerification` теперь строго `required` для privileged-ролей с обеих сторон (генерация опций и серверная проверка — раньше было несогласовано: опции просили `discouraged`, а проверка уже по умолчанию требовала UV). Step-up-тикет теперь привязывается к конкретной browser/phone-сессии, если она есть (`mfa_step_up_tickets.session_token_hash`, `0023_auth_assurance_hardening.sql`) — украденный тикет не работает из другой сессии того же сотрудника. TOTP-секрет больше не имеет plaintext-фолбэка при выключенном шифровании (`upsertPendingTotp` бросает `EncryptionDisabledError`) — проверено на реальной проде: 0 существующих TOTP/WebAuthn-строк, миграция данных не потребовалась. Production обязан стартовать с `DATA_ENCRYPTION_ENABLED=true` (`assertProductionEncryptionRequired()`, только под `RAILWAY_ENVIRONMENT=production`) — **требует ручной настройки `ENCRYPTION_KEKS`/`ENCRYPTION_ACTIVE_KEY_VERSION` в Railway перед деплоем**, иначе сервер не стартует. Строгая base64-валидация (`strictBase64Decode`) для KEK/AEAD-полей — `Buffer.from(str,'base64')` тихо принимал non-canonical вход. Минимальный frontend MFA UI (TOTP-путь): login-challenge экран, обязательный enrollment (QR → код → recovery-коды один раз) — без него 20.52.1 заблокировала бы всех сегодняшних admin/supervisor полностью (ни один ещё не настроил MFA); WebAuthn browser-ceremony UI сознательно отложен, TOTP уже закрывает mandatory-политику. Исправлены неверные ссылки на ADR (`docs/ADR/010-mfa.md`, `docs/ADR/009-mfa.md` — не существовали, файл называется `009-mfa-step-up.md`), задокументировано ADR-009 разделом "20.52.1 revision" вместо нового дублирующего ADR. 16 новых isolation-тестов (`auth-assurance.test.ts`) + 8 новых crypto-тестов (`strictBase64Decode`, `assertProductionEncryptionRequired`) + обновления существующих под новую политику (централизованный `TestFixtures.createEmployee()` теперь авто-enrolls TOTP для admin/supervisor-фикстур, `mfa:false` — явный opt-out для тестов самого enrollment-гейта). `tsc --noEmit`, `vitest run` — 520→544 backend (540 проходят, те же 4 нерелевантных RU-locale теста), `smoke:frontend` 415/415, `check:no-direct-sql`, `check:dangerous-js`, `npm audit` — все зелёные |
| **20.52.2** | **Independent audit follow-up — gate coverage gap, atomic races, duplicate route.** Владелец продукта передал результат независимого стороннего security-аудита 20.52.1; каждая находка перепроверена на реальном коде/воспроизведена локально, не принята на слово. Подтверждено и исправлено: (1) новый MFA-гейт из 20.52.1 (`requireActive()`) фактически не покрывал ~15 route-файлов (`command-center.ts`, `forecast.ts`, `comms.ts`, `shifts.ts`, `tasks.ts`, `support.ts`, `supervisor.ts`, `export.ts` и др.), использовавших более слабый `requireAuth` — Command Center и другая privileged-функциональность оставались доступны admin/supervisor без MFA; ~31 вызов заменён на `requireActive()`, неиспользуемый `requireAuth()` удалён из `auth/guards.ts`; `promos.ts`'s собственный дублирующий resolver заменён тем же гейтом. (2) TOCTOU-гонки в трёх местах MFA/session-кода (resolve-и-consume раздельными запросами вместо одного atomic claim): TOTP replay-защита, pending-login consumption, password-reset token consumption — все три теперь `UPDATE...WHERE...IS NULL...RETURNING`, тот же паттерн, что уже был у recovery-codes/WebAuthn-challenges; подтверждено новыми тестами с реальным `Promise.all()` (не последовательными await) — ровно один из двух конкурентных запросов побеждает. Password-reset заодно обёрнут в одну DB-транзакцию (claim+пароль+отзыв сессий), раньше падение между шагами могло сжечь токен без смены пароля. (3) `GET /metrics` был зарегистрирован дважды — Prometheus-эндпоинт (`app.ts`, 20.32.0) и бизнес-каталог кастомных метрик (`api/routes/metrics.ts`, существует с ранних версий); Fastify бросает на дубле роута, `registerAllRoutes()` ловил это в try/catch и молча продолжал — business-модуль (GET/POST/DELETE каталога метрик) не регистрировался НИ РАЗУ с момента появления Prometheus-эндпоинта, воспроизведено локально, ни одного теста на этот роут не существовало. Prometheus переехал на `/metrics/system`; `registerAllRoutes()` теперь бросает при любой ошибке регистрации модуля вместо тихого продолжения — `index.ts` использует тот же `alertAndExit`, что уже у миграций/BOT_TOKEN/шифрования. Осознанно НЕ тронуто в этом проходе (см. остальные пункты стороннего аудита): Telegram AAL2-семантика (уже документированный, обсуждённый архитектурный trade-off ADR-005/009, не новая находка), полная WebAuthn browser-ceremony, offline-очередь/IndexedDB, CSP unsafe-inline, CSV-инъекция формул, AI data retention, ~241 `innerHTML`/~272 inline-обработчиков во фронтенде — переданы владельцу продукта для приоритизации отдельным списком, не решены единолично. Новые тесты: `tests/isolation/metrics-catalog-route.test.ts` (5), `Concurrency races` в `tests/isolation/auth-assurance.test.ts` (3). `tsc --noEmit`, `vitest run` — 544→552 backend (548 проходят, те же 4 нерелевантных RU-locale теста), `smoke:frontend` 415/415, `check:no-direct-sql`, `check:dangerous-js`, `npm audit` — все зелёные |
| **20.53.0** | **Full Security & Reliability Hardening — Часть 1 (P0): Telegram AAL2 grant + route-auth CI ratchet.** Владелец продукта передал результат второго независимого стороннего security-аудита, 50 разделов, с явным execution order (P0 сначала, не переходить к P1/P2, пока P0 не доказан) — этот релиз закрывает P0 целиком, P1/P2 сознательно оставлены на следующий заход, честно перечислены как нерешённые. Перепроверка каждой находки против актуального HEAD (не на слово) показала: 5 из 7 P0-пунктов аудита УЖЕ были закрыты в 20.52.2 (`requireAuth`-обход, `promos.ts`-bypass, fail-open route registration, дубль `/metrics`, TOTP/pending-login race) — зафиксировано как "already fixed", не переделано повторно. Реально новый P0-пункт — архитектурный пробел, который 20.52.1/20.52.2 не закрывали: `checkPrivilegedAssurance()` трактовал "у Telegram-запроса нет server-side сессии" (ADR-005) как основание принять "у аккаунта ЕСТЬ настроенный фактор" за достаточное доказательство AAL2, БЕЗ реальной проверки этого фактора для текущего Telegram-доступа — украденное устройство/Telegram-сессия с валидной initData HMAC давали privileged-доступ без предъявления второго фактора атакующим. Новый Telegram AAL2 grant (`mfa_telegram_grants`, `0024_telegram_aal2_grant.sql`, `auth/mfa/telegram-grant.ts`) — короткоживущий (12ч) server-side opaque-токен в HttpOnly Secure cookie (`t2_tg_aal2`, тот же origin, что уже используют CSRF/WebAuthn RP), выдаётся только через новый `POST /auth/mfa/telegram/verify` после реальной TOTP/WebAuthn/recovery-code проверки — не второй "логин", initData HMAC остаётся единственным identity-доказательством, грант — чисто AAL2-слой поверх него. `checkPrivilegedAssurance()` больше не различает каналы: обоим нужен один и тот же класс доказательства (конкретный timestamp, не просто "фактор существует") — устранён специальный случай `undefined` в сигнатуре. Step-up-тикеты теперь привязываются и к Telegram-гранту (не только browser-сессии, как раньше); грант отзывается при MFA reset и эскалации роли — те же триггеры, что уже отзывали browser-сессии. Минимальный frontend-экран для лёгкой Telegram-реверификации (не полный enrollment — фактор уже есть, нужен только код). Второй P0-пункт (route security matrix, ~156 роутов) собран через AST-подобный grep-скрипт и закреплён как `check:route-auth` CI-ратчет (`scripts/check-route-auth-policy.mjs`) — новый business-роут без вызова централизованного guard теперь падает в CI, если явно не занесён в allowlist публичных роутов с причиной; тот же класс инструмента, что `check-no-direct-sql.mjs`. Сознательно НЕ тронуто в этой части (см. §47 брифа — P1/P2, следующий заход): MFA distributed rate limits, WebAuthn ceremony/challenge-context hardening, tenant/supervisor scope model (`getUserStoreIds()`'s `null`-семантика), support confidentiality (legacy plaintext backfill, Telegram plaintext leak, fallback corruption), crypto key rotation rewrap, XSS/CSP (~241 `innerHTML`/~272 inline-обработчиков — тот же объём, что уже трижды документирован как отдельная эпоха), CSV formula injection, AI data governance, offline queue namespacing, PWA semantics, version display consistency, input validation audit, avatar hardening, observability/log redaction, централизованный config, DB least-privilege/migration lock, CI/supply-chain, TypeScript strict. 29 новых тестов (5 Telegram AAL2 + обновления ~20 существующих файлов под новую фикстуру `TestFixtures.createEmployee()`'s auto-provisioned grant). `tsc --noEmit` (backend+frontend), `vitest run` — 552→557 backend (553 проходят, те же 4 нерелевантных RU-locale теста), `smoke:frontend` 415/415, `check:no-direct-sql`, `check:dangerous-js`, `check:route-auth` (новый), `npm audit` — все зелёные |
| **20.54.0** | **Security Hardening & Defense in Depth — Часть 2 (P1): distributed rate limiting, CSV injection, offline-queue isolation, config validation.** Владелец продукта передал третий по счёту security-бриф (58 разделов) с явным приоритетом P1-A первым и явным предпочтением честного документированного P2 вместо фиктивных частичных фиксов по всем пунктам сразу — этот релиз закрывает 4 пункта P1 полностью, с тестами, остальное честно перечислено как next в `docs/security/20.54-baseline.md`, не тронуто частично. **§P1-A** — распределённый (Postgres-backed, не Redis — прод и так 1 реплика из-за long-polling бота, `grammy`) rate-лимит слой (`security/rate-limit.ts`, `security_rate_limit_counters`, `0025_security_rate_limit.sql`) поверх уже существующего in-memory `@fastify/rate-limit`: `consume()` проверяет/инкрементит несколько осей identity разом (IP + аккаунт/телефон-хеш) атомарно, fail-closed при сбое хранилища (блокирует только текущий запрос, ничего не пишет — без риска бессрочной блокировки при восстановлении БД). Применён на `POST /auth/login`, `/auth/reset/:token`, `/auth/login/mfa`, `/auth/mfa/step-up`, `/auth/mfa/telegram/verify`, `/auth/mfa/totp/confirm` — самая brute-force-чувствительная поверхность; остальные rate-limited роуты остаются на in-memory лимите, это явный, задокументированный скоуп, не случайный пропуск. **§P1-F** — CSV formula injection: `csvEscape()` экранировал только разделитель/кавычки/перенос строки, никогда — ведущие `=`/`+`/`-`/`@` (employee/store display-имена — admin-контролируемые, не платформенные, реальный вектор через `full_name` при онбординге). Новый `security/csv-safe.ts::csvSafeCell()` добавляет ведущий апостроф (стандартная OWASP-митигация, Excel/Sheets рендерят его как литеральный текст) перед опасным префиксом; подключён во все три CSV-экспорта (`/export/sales.csv`, `/export/bfq.csv`, `/export/schedules.csv`), плюс `safeFilenameSegment()` для ранее не валидированных `from`/`to`/`month` в `Content-Disposition`. **§P1-G** — офлайн-очередь (`frontend/offline-queue.js`, единая IndexedDB на весь браузер) не была привязана к сессии/identity, и ни `logoutSelf()` (`my-plan`), ни `logoutFromMfaGate()` (`access-supervisor`) не чистили её перед `location.reload()`: на общем устройстве сотрудник A мог выйти с несинканной офлайн-продажей в очереди, а вошедший следом менеджер B получил бы её тихий автореплей под своей текущей сессией (для менеджера серверная проверка `employee_id` в `/sync/batch` не блокирует чужой — только для не-менеджера). Серверная сторона `/sync/batch` перепроверена и подтверждена уже корректной (`tests/isolation/quick-sale-sync.test.ts` уже покрывает это с более ранней версии) — фикс только клиентский: новый `OfflineQueue.clear()` + best-effort `flush()` перед фактическим вызовом `/auth/logout` в обоих logout-путях. **§P1-H** — `MINI_APP_URL` не был обязателен при старте: `auth/csrf.ts::expectedOrigin()` тихо отдаёт `''` без него, и Origin-слой CSRF-проверки молча выключается (double-submit токен продолжает работать независимо — не полный обход CSRF, но тихая деградация defense-in-depth слоя). Новый `config/validate.ts::validateProductionConfig()` требует валидный `https://` `MINI_APP_URL` в production — тот же fail-closed `alertAndExit()`, что уже у `BOT_TOKEN`/шифрования. **Важно для деплоя**: read-only проверка Railway-переменных в рамках этого прохода показала, что `MINI_APP_URL` в проде **сейчас не задан** — деплой этого релиза без его установки не даст серверу стартовать (см. финальный отчёт сессии за точным значением и инструкцией по установке через Railway). **§P1-B** (WebAuthn ceremony hardening) — аудит `auth/mfa/webauthn.ts` + challenge-репозитория не нашёл кода-бага: CSPRNG/TTL(5 мин)/атомарный single-use consume/register-vs-authenticate разделение уже корректны, clone-detection по counter — библиотечное поведение, не переизобретено. Единственная находка: `authenticate`-kind шарится между login-MFA/step-up/Telegram-verify без отдельного flow-id — доказано тестом, что это НЕ байпас (криптографическая проверка challenge отклоняет несовпадение независимо), только edge-case двух одновременных церемоний. Закрыт реальный пробел в тестовом покрытии (было — ноль тестов на всю WebAuthn-церемонию) — 5 новых, включая «parallel ceremony» из брифа. **§P1-C** (tenant/supervisor IDOR-аудит) — систематический проход не нашёл новых пробелов; два кандидата (`GET /stores/:id/profile`, `GET /employees/:id/profile` не ограничивают supervisor его сектором) оказались согласованы с остальным приложением — `GET /employees`/`GET /stores`/`/sales/history` уже сегодня отдают supervisor данные всей сети, сектор ограничивает только dashboard-агрегаты; точечный фикс двух роутов ничего не закрыл бы и создал бы непоследовательность — подтверждено с владельцем продукта, задокументировано как есть. **§P1-D** (support confidentiality + key rotation) — read-only проверка прода нашла реальный пробел: все 8 `support_tickets` и 2 `support_messages` — plaintext (историческая граница, не баг — `employee_totp` уже корректно зашифрован). Новый `backend/src/scripts/backfill-support-encryption.ts` — resumable/idempotent/batched backfill + `--rewrap` режим ротации ключа (`ENCRYPTION_KEKS` уже поддерживал несколько версий — не хватало инструмента переложить существующие строки на новый активный ключ). 4 новых isolation-теста, включая полную симуляцию ротации. Намеренно НЕ запущено на проде. **§P1-E** (frontend XSS-аудит) — нашёл повторяющийся паттерн: `shift_text`/`store_name` уже экранировались в `pages/schedule/index.ts`, но рендерились НЕэкранированными в `home`/`my-plan`/`team`/`shift` — те же поля API-ответа, разные точки рендера. Плюс `home.ts` greeting `firstName` и `promos.ts` `mask`. 8 `esc()`-фиксов. Попутно найден пробел в самой тестовой инфраструктуре: 3 jsdom-теста стабили `esc()` как no-op passthrough (регрессия экранирования физически не могла быть поймана) — переведены на реальный `esc()`, добавлено 7 XSS-regression тестов. Осознанно НЕ реализовано: CSP `unsafe-inline` tightening (отдельный, гораздо больший эпик — ~265+ `onclick`, ~400 inline `style`), полный перевод всех rate-limited роутов на распределённый слой, весь P2 — см. `docs/security/20.54-baseline.md`. `tsc --noEmit`, `vitest run` (backend) — 578 проходят (те же 4 нерелевантных RU-locale теста), `vitest run` (frontend) — 422/422, `smoke:frontend`, `check:no-direct-sql`, `check:dangerous-js`, `check:route-auth`, `npm audit --omit=dev` (0 vulnerabilities) — все зелёные. MINI_APP_URL выставлен в Railway перед пушем, задеплоено |
| **20.54.1** | **Хотфикс: миграция 0025 без схема-квалификации (CI failure).** Каждая другая миграция явно квалифицирует CREATE-операторы как public.<table> — 0025_security_rate_limit.sql была единственным исключением: прошла на уже существующей локальной test-БД (накопленный за сессии schema-контекст), но упала в CI на действительно свежем контейнере Postgres 18 (`no schema selected to create in`). Воспроизведено локально созданием свежей БД той же версии Postgres ДО фикса (подтверждена сама причина, не угадана), затем подтверждён чистый прогон всей цепочки миграций 0001→0025 после фикса. Полный локальный прогон (tsc --noEmit, vitest run — те же 574/578, CI-ратчеты, npm audit) — все зелёные |


---

**[README.md](./README.md)** — продукт и витрина · **[docs/README.md](./docs/README.md)** — техническая документация · **[docs/ADR/](./docs/ADR/)** — почему решения приняты именно так.
