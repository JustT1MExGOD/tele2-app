package ru.t2sales.shared.theme

import androidx.compose.ui.text.font.FontFamily

/** Android: the system font (Roboto), the closest to the web fallback chain on a phone. */
internal actual val t2FontFamily: FontFamily? = null
