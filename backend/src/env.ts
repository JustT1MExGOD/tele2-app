/**
 * Должен быть ПЕРВЫМ импортом в index.ts, до всего остального.
 *
 * ES-модули хоистят импорты: `import { x } from './a.js'` полностью
 * выполняет модуль a.js (и всё, что он импортирует) раньше, чем
 * последующий код текущего файла — включая dotenv.config(), даже если
 * он текстуально стоит выше. db/index.ts и bot/index.ts читают
 * process.env.DATABASE_URL / BOT_TOKEN на верхнем уровне при импорте,
 * так что раньше локальный `.env` до них просто не долетал: в Railway
 * это не заметно (там переменные уже есть в окружении до старта node),
 * а вот `npm run dev` тихо цеплялся не за ту БД.
 *
 * Если этот файл — первый импорт index.ts, его тело (в т.ч. dotenv.config())
 * гарантированно отработает до того, как Node начнёт резолвить следующий
 * импорт и всё, что тот тянет за собой.
 */
import dotenv from 'dotenv';
dotenv.config();
