/**
 * Единая конфигурация весов/констант для core/schedule/schedule-generator.ts.
 * См. план (synthetic-roaming-mountain) для полного обоснования каждой.
 */

/** Веса последних 6 полных месяцев истории, от новейшего к самому старому. */
export const HIST_WEIGHTS_6: number[] = [0, 1, 2, 3, 4, 5].map((i) => Math.pow(0.7, i));

/** Shrinkage-порог (в часах) — ~4 полные смены. confidence = hours/(hours+K). */
export const K_HOURS = 40;

/** Хард-минимум рабочих часов в месяце для НЕ-стажёров. */
export const MONTHLY_HOURS_MIN = 179;

/** Хард-минимум смен на КАЖДОЙ активной точке сети для НЕ-стажёров. */
export const MIN_SHIFTS_PER_STORE = 5;

/** Хард-потолок смен в месяц (стажёры и обычные сотрудники одинаково). */
export const MAX_SHIFTS_PER_MONTH = 20;

/** Длительность смены стажёра, часов (всегда от открытия точки). */
export const TRAINEE_SHIFT_HOURS = 8;

/**
 * Небольшая стоимость назначения стажёра — без неё положительный EV смены
 * стажёра (даже минимальный) заставлял бы солвер заполнять КАЖДЫЙ
 * разрешённый слот стажёра просто потому что это formally выгодно.
 * Значение маленькое и безразмерное относительно productivity/preference
 * термов (которые в диапазоне ~[-1..1] на кандидата).
 */
export const TRAINEE_ASSIGN_COST = 0.05;

/**
 * Честно названный вес: минимизирует ТОЛЬКО суммарные часы сверх
 * MONTHLY_HOURS_MIN по всем обычным сотрудникам — НЕ гарантирует равное
 * распределение избыточных часов между сотрудниками (это отдельная,
 * сознательно отложенная во вторую итерацию задача fairness).
 */
export const EXCESS_HOURS_WEIGHT = 0.01;

/** Вес soft-терма предпочтения дня недели (день-офф/предпочитаемый рабочий день). */
export const DAY_PREFERENCE_WEIGHT = 0.3;

/** Вес продуктивности в объективе (доминирующий терм). */
export const PRODUCTIVITY_WEIGHT = 1;

/** Максимальное время работы солвера (мс) на одну стадию. */
export const SOLVER_TIMEOUT_MS = 15000;
