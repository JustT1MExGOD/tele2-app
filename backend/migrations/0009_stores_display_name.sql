-- 19.7 (батч 3, п.7): кастомное название точки для руководителей/старших
-- продавцов — подменяет "сырое" name везде в мини-аппе, если задано.
ALTER TABLE public.stores ADD COLUMN display_name text;
