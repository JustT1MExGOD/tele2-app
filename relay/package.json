{
  "name": "t2sales-relay",
  "version": "20.56.0",
  "private": true,
  "type": "module",
  "scripts": {
    "relay:typecheck": "tsc --noEmit",
    "relay:test": "vitest run",
    "relay:build": "tsc",
    "relay:start": "node dist/index.js",
    "relay:dev": "tsx src/index.ts",
    "test": "vitest run",
    "test:network-acceptance": "vitest run --config vitest.network-acceptance.config.mts"
  },
  "dependencies": {
    "fastify": "^5.1.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2",
    "vitest": "^4.1.10"
  }
}
